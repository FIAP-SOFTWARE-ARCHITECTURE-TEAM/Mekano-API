# --- Cache Caffeine: users (D-13) ---
quarkus:
  cache:
    caffeine:
      users:
        initial-capacity: 10
        maximum-size: 100
        expire-after-write: 60s
      servicos:
        initial-capacity: 10
        maximum-size: 200
        expire-after-write: 120s
