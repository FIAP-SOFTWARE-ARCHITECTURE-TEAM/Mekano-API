apiVersion: v1
kind: Secret
metadata:
  name: evolution-secret
  namespace: mekano-system
type: Opaque
# TEMPLATE ONLY. Never commit real credentials.
# The CD pipeline recreates this Secret from GitHub Actions Secrets.
# DATABASE_CONNECTION_URI is injected by the CD pipeline
# (depends on the RDS endpoint — not a static value).
stringData:
  AUTHENTICATION_API_KEY: "REPLACE_WITH_EVOLUTION_API_KEY"