# --- OpenAPI / Swagger UI ---

quarkus:
  swagger-ui:
    always-include: true

mp:
  openapi:
    info:
      title: Mekano API
      version: 2.0.0
      description: Clean Architecture REST API — FIAP Software Architecture
