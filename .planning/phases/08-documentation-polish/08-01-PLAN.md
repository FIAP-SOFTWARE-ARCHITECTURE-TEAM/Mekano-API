---
phase: 08-documentation-polish
plan: 01
type: execute
wave: 1
depends_on:
  - 07-01   # endpoint verification results
  - 07-02   # OS priority ordering API
files_modified:
  - README.md
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/MekanoApiApplication.java
  - mekano-rest/src/main/resources/openapi-config.yml
  - "Mekano API v1.0.postman_collection.json"
autonomous: true
requirements:
  - DOC-04
  - DOC-05
  - DOC-06
  - DOC-08
  - DOC-09
  - DOC-10
  - DOC-11
user_setup: []

must_haves:
  truths:
    - "README.md contém descrição da solução, objetivos v2.0, stack, quick start, execução local, JWT, deploy (K8s+Terraform), arquitetura, HPA, fluxo OS, testes e estrutura do projeto"
    - "README.md contém diagrama de sequência Mermaid do fluxo OS completo (criar OS → diagnosticar → orçar → aprovar → executar → finalizar → pagar → entregar)"
    - "Swagger UI exibe versão 2.0.0 em /q/swagger-ui"
    - "Collection Postman renomeada e link disponível no README"
    - "Miro atualizado em relação à API atual"
    - "Documentação de componentes (4 módulos Maven), infraestrutura provisionada (Docker, K8s, RDS) e fluxo de deploy adicionada ao README"
    - "README.md explica HPA (CPU 70%, Memory 80%, min 2, max 10) e comando para simular aumento de carga"
    - "Descrição da tarefa de gravação do vídeo demonstrativo (até 15 min) documentada para referência"
  artifacts:
    - path: "README.md"
      provides: "README v2.0 reestruturado com todas as seções obrigatórias"
      min_lines: 300
    - path: "mekano-rest/src/main/java/com/fiap/mekano/rest/api/MekanoApiApplication.java"
      provides: "Swagger version bump from 1.0.0 to 2.0.0"
      contains: 'version = "2.0.0"'
    - path: "mekano-rest/src/main/resources/openapi-config.yml"
      provides: "OpenAPI config version bump from 1.0.0 to 2.0.0"
      contains: 'version: 2.0.0'
    - path: "README.md"
      provides: "Mermaid sequence diagram of complete OS lifecycle embedded inline"
      contains: "sequenceDiagram"
    - path: "README.md"
      provides: "HPA explanation with CPU 70%, Memory 80%, min 2, max 10"
      contains: "70%"
    - path: "README.md"
      provides: "Load simulation command using busybox wget"
      contains: "busybox"
  key_links:
    - from: "README.md"
      to: "mekano-rest/AGENTS.md"
      via: "Resource roles table"
      pattern: "RolesAllowed"
    - from: "README.md"
      to: "docs/MEKANO_DOCUMENTATION.md"
      via: "Component docs reference"
      pattern: "MEKANO_DOCUMENTATION"
    - from: "README.md"
      to: "docs/EventStorming_Mermaid.md"
      via: "Architecture diagrams reference"
      pattern: "EventStorming_Mermaid"
    - from: "Mekano API v1.0.postman_collection.json"
      to: "README.md"
      via: "Link in Postman section"
      pattern: "postman"
---

<objective>
Restructure README.md for v2.0 with full project documentation including solution description, architecture, deploy instructions, Mermaid diagrams, Swagger version bump, Postman collection update, Miro, component docs, HPA explanation, and video task description.

**Purpose:** Transform the existing v1.0 README into a comprehensive project homepage covering all aspects of the v2.0 system — from quick start to production deploy, architecture to scalability.

**Output:** Updated README.md, bumped Swagger version, updated Postman collection, documentation of video task.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@docs/MEKANO_DOCUMENTATION.md
@docs/EventStorming_Mermaid.md
@docs/sequence-diagrams/fluxo-completo-os-lifecycle.md
@mekano-rest/AGENTS.md
@mekano-rest/src/main/java/com/fiap/mekano/rest/api/MekanoApiApplication.java
@mekano-rest/src/main/resources/openapi-config.yml
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure README.md + add Mermaid sequence diagram + bump Swagger version + update Postman collection</name>
  <files>
    README.md,
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/MekanoApiApplication.java,
    mekano-rest/src/main/resources/openapi-config.yml,
    Mekano API v1.0.postman_collection.json
  </files>
  <action>
    Per D-01, D-02, D-03, D-04, D-06, D-07:

    **1. README.md — complete restructure:**

    Rewrite README.md with the following sections in order. Keep all existing content that is still accurate (test commands, JWT auth, Postman link, project structure table) but reorganize and expand:

    **(1) Title + Badges**
    ```
    # Mekano API
    ![Java](https://img.shields.io/badge/Java-17-blue)
    ![Quarkus](https://img.shields.io/badge/Quarkus-3.36.0-purple)
    ```

    **(2) Descrição da Solução**
    - Extract from docs/MEKANO_DOCUMENTATION.md §1 — mechanic shop OS lifecycle management
    - Add v2.0 objectives: infrastructure (Docker revisado, K8s, Terraform, CD pipeline), WhatsApp notifications, 80% test coverage, Clean Code refactoring, OS priority ordering, documentation complete

    **(3) Stack**
    Table: Java 17, Quarkus 3.36.0, PostgreSQL 16, Maven multi-module, MapStruct, Lombok, Flyway, REST Assured, SmallRye JWT, Docker Compose, H2 (test)

    **(4) Quick Start (docker-compose)**
    - Keep existing "Dev (Docker Compose + keygen automático)" section — already correct
    - Add note: Swagger UI at `/q/swagger-ui`

    **(5) Execução Local (quarkus:dev)**
    - Keep existing "Dev local (sem Docker)" section

    **(6) Autenticação JWT**
    - Keep existing JWT section verbatim (Ed25519, keygen, seed admin)

    **(7) API / Swagger**
    - Link to Swagger UI: `http://localhost:8080/q/swagger-ui`
    - Link to OpenAPI JSON: `http://localhost:8080/q/openapi?format=json`
    - Resource roles table from mekano-rest/AGENTS.md:
      | Resource | Roles | Description |
      |----------|-------|-------------|
      | UserResource | user | CRUD usuários |
      | VeiculoResource | admin, atendente | CRUD veículos |
      | ServicoResource | admin | CRUD serviços |
      | PecaResource | admin | CRUD peças |
      | RequisicaoCompraResource | admin | CRUD requisições de compra |
      | NfEntradaResource | admin | CRUD notas fiscais entrada |
      | AlertaResource | admin, atendente | Alertas de estoque mínimo |
    - Postman section: rename reference from "v1.0" to "v2.0", keep import instructions

    **(8) Deploy (K8s + Terraform)**
    - **K8s:** Reference manifests in `k8s/` directory. Document after Elias (INF-04). For now, write: "Os manifestos K8s (Deployment, Service, ConfigMap, Secret, HPA, Ingress) estão em `k8s/`. Para aplicar: `kubectl apply -f k8s/` (após configuração do cluster)."
    - **Terraform:** Reference scripts in `terraform/` directory. Document after Elias. "Os scripts Terraform para provisionamento do cluster EKS e banco RDS estão em `terraform/`. Para aplicar: `cd terraform && terraform init && terraform apply` (após configurar backend S3)."

    **(9) Arquitetura**
    - 4 Maven modules table (keep existing)
    - Reference `docs/MEKANO_DOCUMENTATION.md` for detailed component documentation (per D-09)
    - Reference `docs/EventStorming_Mermaid.md` for class diagrams of all 3 bounded contexts
    - Mermaid state machine diagram — copy from `docs/sequence-diagrams/fluxo-completo-os-lifecycle.md` directly into README (embed `stateDiagram-v2` block)

    **(10) Fluxo OS (Mermaid Sequence Diagram)**
    Per D-03 and D-04: Add complete OS lifecycle Mermaid sequence diagram inline. Use the template from `.planning/phases/08-documentation-polish/08-RESEARCH.md` §Mermaid Sequence Diagram Template. Actors: Atendente, Mecânico, Cliente. Participants: API (OrdemDeServicoResource), App (OrdemDeServicoService), Domain, Repo, DB (PostgreSQL), Payment (MockPaymentService). Steps: (1) Criar OS, (2) Iniciar Diagnóstico, (3) Finalizar Diagnóstico → Orçamento, (4) Cliente Aprova Orçamento, (5) Finalizar Execução → Cobrança, (6) Confirmar Pagamento, (7) Entregar Veículo.

    **(11) Escalabilidade (HPA + Simulação de Carga)**
    Per D-10:
    - HPA parameters: CPU 70%, Memory 80%, min 2 replicas, max 10 replicas
    - Load simulation command:
      ```bash
      kubectl run -i --tty --image=busybox --restart=Never -- /bin/sh -c "while true; do wget -q -O- http://mekano-service:8080/api/v1/servicos; done"
      ```
    - Explain that HPA auto-scales pods based on CPU/Memory metrics; `kubectl get hpa` shows current status

    **(12) CI/CD**
    Placeholder section: "O diagrama do fluxo de CI/CD será adicionado quando a pipeline de CD for implementada (INF-04)." (Per D-05 — DOC-07 is deferred to Plan 08-02.)

    **(13) Testes**
    Keep existing test section verbatim — all commands are correct.

    **(14) Estrutura do Projeto**
    Keep existing project structure table.

    **(15) Troubleshooting**
    Per D-02: Document common issues (port conflicts, Docker network, JWT key generation) — reference Phase 4 docker-compose refinement.

    **2. Swagger version bump — MekanoApiApplication.java (per D-06):**
    - Change `version = "1.0.0"` to `version = "2.0.0"`
    - Change `description = "Clean Architecture REST API — FIAP Software Architecture"` to `description = "Clean Architecture REST API — FIAP Software Architecture v2"`

    **3. Swagger version bump — openapi-config.yml (per D-06, Pitfall 1):**
    - Change `version: 1.0.0` to `version: 2.0.0`
    - Change `description: Clean Architecture REST API — FIAP Software Architecture` to `description: Clean Architecture REST API — FIAP Software Architecture v2`

    **4. Postman collection update (per D-06):**
    - Rename file from `Mekano API v1.0.postman_collection.json` to `Mekano API v2.0.postman_collection.json`
    - Read the existing collection JSON, update the `info.name` and `info.description` fields from "v1.0" to "v2.0"
    - Verify all folders/endpoints are still correct; add note about ClienteResource if it now exists (check codebase)
    - Add explicit Miro link to collection description (per D-08 context)

    **Avoid:**
    - Do NOT delete or alter any existing content that is still accurate (test commands, JWT keygen, project structure)
    - Do NOT add CI/CD Mermaid diagram (deferred to Plan 08-02 per D-05)
    - Do NOT use external image files for diagrams (Mermaid only per D-03)
    - Do NOT include video file in repository (external link only per D-11)
  </action>
  <verify>
    <automated>
      # Verify Swagger version bump
      grep -c 'version = "2.0.0"' mekano-rest/src/main/java/com/fiap/mekano/rest/api/MekanoApiApplication.java
      grep -c 'version: 2.0.0' mekano-rest/src/main/resources/openapi-config.yml
      # Verify Mermaid sequence diagram present
      grep -c 'sequenceDiagram' README.md
      # Verify state machine diagram present
      grep -c 'stateDiagram-v2' README.md
      # Verify HPA content
      grep -c '70%' README.md
      grep -c 'busybox' README.md
      # Verify Postman collection renamed
      Test-Path "Mekano API v2.0.postman_collection.json"
    </automated>
  </verify>
  <done>
    README.md has all 15 sections; Mermaid sequence diagram + state machine render inline; Swagger shows v2.0.0; Postman collection renamed to v2.0; HPA explanation present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add component documentation, Miro update, and HPA/load simulation doc to README</name>
  <files>
    README.md
  </files>
  <action>
    Per D-08, D-09, D-10:

    **1. Component documentation (D-09):**
    Add a subsection under "Arquitetura" named "### Componentes da Aplicação" that documents:
    - **mekano-domain:** Entities (OS, Cliente, Veiculo, Servico, Peca), Value Objects (Placa, CPF, CNPJ, Email, Telefone, ItemOrcamento), Ports (22 interfaces for repositories, services, events), Domain Events (OrcamentoGerado, PagamentoConfirmado, OSFinalizada)
    - **mekano-application:** Use cases organized by bounded context — user (CreateUser, ListUsers), cliente (CreateCliente, UpdateCliente), vehicle, servico, peca, nfentrada, requisicao
    - **mekano-infrastructure:** 7 JPA entities, 14 repository implementations, 13 MapStruct mappers, 11 Flyway migrations (V1-V24 after renumbering), JWT auth (BcryptPasswordHasher, JwtTokenManager)
    - **mekano-rest:** 7 REST resources (User, Veiculo, Servico, Peca, RequisicaoCompra, NfEntrada, Alerta), 20+ DTOs, 7 mappers, exception handling (ApiExceptionMapper → RFC 7807 Problem Details)

    **2. Infrastructure provisioned (D-09):**
    Add a subsection "### Infraestrutura Provisionada" under "Deploy" or under "Arquitetura":
    - **Docker:** Dockerfile.jvm (multi-stage build), docker-compose.yml (dev with keygen), docker-compose.prod.yml (prod bind mount)
    - **Kubernetes:** Manifests in k8s/ — Deployment (3 replicas, resource limits), Service (ClusterIP, port 8080), ConfigMap (datasource config), Secret (JWT private key, DB password), HPA (CPU 70%, Memory 80%, min 2, max 10), Ingress (optional)
    - **RDS:** PostgreSQL via Terraform (terraform/ — after INF-04)

    **3. Fluxo de deploy (D-09):**
    Add a subsection "### Fluxo de Deploy" under "Deploy":
    - CI: push → GitHub Actions → build → test (mvnw verify) → package → Docker image build
    - CD (após INF-04): push to registry → apply K8s manifests → rolling update

    **4. HPA + Load Simulation (D-10):**
    Ensure the "Escalabilidade" section (added in Task 1) includes:
    - HPA configuration: `kubectl apply -f k8s/hpa.yaml`
    - Watching HPA: `kubectl get hpa -w`
    - Load simulation command from D-10
    - Expected behavior: pods scale up when CPU exceeds 70%, scale down when load drops
    - Prerequisites: metrics-server must be deployed (for Kind, install via `kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml`)

    **5. Miro board update reference (D-08):**
    - Add a reference under "API / Swagger" section pointing to the Miro board: `https://miro.com/app/board/uXjVHD4vUnU=/`
    - Note: "O board do Miro foi atualizado para refletir a API atual (responsável: atualizar manualmente após cada mudança significativa de API)"
    - Keep the existing Miro link from docs/MEKANO_DOCUMENTATION.md §5.2

    **Avoid:**
    - Do NOT add code examples that don't exist yet (no fake K8s commands for non-existent manifests)
    - Do NOT duplicate information already in docs/ (cross-reference instead)
    - Do NOT remove existing Miro board link — add to it
  </action>
  <verify>
    <automated>
      # Verify component docs section
      grep -c 'Componentes da Aplicação' README.md
      # Verify Miro link
      grep -c 'miro.com' README.md
      # Verify infra provisioned section
      grep -c 'Infraestrutura Provisionada' README.md
      # Verify HPA section has metrics-server note
      grep -c 'metrics-server' README.md
      # Verify deploy fluxo
      grep -c 'Fluxo de Deploy' README.md
    </automated>
  </verify>
  <done>
    Component documentation for all 4 Maven modules in README; infrastructure provisioned (Docker, K8s, RDS) documented; deploy flow described; HPA/load simulation detailed; Miro link with update note.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create video recording task description (DOC-04)</name>
  <files>
    README.md
  </files>
  <action>
    Per D-11:

    Add a new section "## Vídeo Demonstrativo" near the end of README.md (before Troubleshooting) with:

    **Objective:** Gravar vídeo demonstrativo de até 15 minutos mostrando o ambiente Mekano em execução.

    **4 segments with time budget:**
    - **(1) Fluxo OS completo (5 min):** Demonstrar o ciclo completo de uma OS — criar cliente/veículo, criar OS, iniciar diagnóstico, finalizar diagnóstico (orçamento gerado), aprovar orçamento, executar, finalizar, pagar, entregar. Mostrar transições de status via API calls (Postman ou curl).
    - **(2) Chamadas de API (4 min):** Navegar pelos endpoints principais: CRUD de clientes, veículos, serviços, peças, alertas de estoque, consulta pública de status.
    - **(3) Infraestrutura / Containers (3 min):** Mostrar `docker ps` com os containers rodando (postgres, app), `docker logs` da aplicação, Swagger UI em `/q/swagger-ui`.
    - **(4) Testes (3 min):** Executar `./mvnw verify -pl mekano-rest -am` e mostrar 517+ testes passando, cobertura JaCoCo ≥80%.

    **Recommended tools:** OBS Studio (free, cross-platform, scene switching for segment recording).

    **Post-production:** Edit segments together (remove pauses, mistakes), keep total ≤15 min.

    **Delivery:** Upload to YouTube/Google Drive and add link to README: `[Assistir vídeo demonstrativo](<LINK>)` — placeholder `#` until video is recorded.

    **Avoid:**
    - Do NOT record video during this task — only document the task description
    - Do NOT store video in the git repository
    - Do NOT commit placeholder links to external storage
  </action>
  <verify>
    <automated>
      # Verify video section exists
      grep -c 'Vídeo Demonstrativo' README.md
      # Verify 4 segments documented
      grep -c '15 min' README.md
      # Verify placeholder link
      grep -c 'Assistir vídeo demonstrativo' README.md
    </automated>
  </verify>
  <done>
    README.md has "Vídeo Demonstrativo" section documenting the 4-segment video plan (15 min budget), recommended tools, and placeholder link.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → external services | Links to Swagger UI and Miro board — no sensitive data exposed |
| README.md → environment variables | Documents env var names but not actual values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | README.md · env var names | mitigate | Only document env var NAMES (e.g., `DB_PASSWORD`), never values or example secrets. Seed admin credentials (admin@mekano.com.br / Mekano@2024) are already documented and are test-only credentials. |
| T-08-02 | Tampering | README.md · build commands | mitigate | All documented commands are copy-paste safe: no `curl | bash`, no eval, no sudo without explanation. |
| T-08-SC | Tampering | No package installs | accept | Phase is documentation-only — zero npm/pip/cargo dependencies. Package legitimacy gate not applicable. |
</threat_model>

<verification>
1. README.md has all sections: Descrição, Stack, Quick Start, Local Dev, JWT, API/Swagger, Deploy, Arquitetura, Escalabilidade, Fluxo OS, Testes, Estrutura, Vídeo Demonstrativo, Troubleshooting
2. Swagger version = 2.0.0 in both MekanoApiApplication.java and openapi-config.yml
3. Postman collection renamed to v2.0 with updated metadata
4. Mermaid sequence diagram renders on GitHub (verify via git push preview)
5. All grep verify commands pass
</verification>

<success_criteria>
- [ ] README.md is comprehensive v2.0 documentation with ≥300 lines
- [ ] Swagger UI shows "Mekano API v2.0.0" after rebuild
- [ ] Mermaid sequence diagram + state machine render inline in README
- [ ] Postman collection accessible as "Mekano API v2.0.postman_collection.json"
- [ ] Component docs, Miro link, HPA explanation, and video task description all present
</success_criteria>

<output>
Create `.planning/phases/08-documentation-polish/08-01-SUMMARY.md` when done
</output>