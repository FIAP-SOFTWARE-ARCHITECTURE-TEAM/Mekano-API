---
phase: 08-documentation-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
  - 04-02   # Elias INF-04 (CD pipeline) — BLOCKING dependency
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-07
user_setup: []

must_haves:
  truths:
    - "README.md contém diagrama Mermaid do fluxo de CI/CD completo (build → test → package → Docker → registry → deploy K8s)"
    - "Diagrama Mermaid é consistente com o pipeline YAML real em .github/workflows/"
    - "Task está documentada como dependente de INF-04 (Elias)"
  artifacts:
    - path: "README.md"
      provides: "CI/CD Mermaid flowchart in README"
      contains: "graph LR"
  key_links:
    - from: "README.md"
      to: ".github/workflows/"
      via: "CI/CD Mermaid reflects actual pipeline YAML"
      pattern: "ci.yml"
---

<objective>
Add CI/CD Mermaid flowchart to README.md — deferred until Elias implements CD pipeline (INF-04).

**Purpose:** Document the complete CI/CD pipeline from code push to production deployment, ensuring operators understand the delivery flow.

**Output:** CI/CD Mermaid diagram in README.md (only after INF-04 dependency is resolved).
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.github/workflows/ci.yml
@.planning/phases/08-documentation-polish/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CI/CD Mermaid diagram to README (after INF-04 is complete)</name>
  <files>
    README.md
  </files>
  <action>
    Per D-05:

    **IMPORTANT: This task MUST NOT be executed until Elias has completed INF-04 (CD pipeline implementation).**

    When ready:

    **1. Read the actual pipeline YAML files:**
    - `.github/workflows/ci.yml` — current CI pipeline
    - Any new CD workflow files added by Elias (INF-04) — likely `.github/workflows/cd.yml` or additions to ci.yml

    **2. Add Mermaid flowchart to README's "CI/CD" section:**
    Replace the placeholder text from Plan 08-01 with an inline Mermaid `graph LR` or `graph TD` flowchart.

    Expected structure (adjust based on actual pipeline):
    ```mermaid
    graph LR
        A[push: main] --> B[Checkout]
        B --> C[Setup JDK 17]
        C --> D[Build & Test]
        D --> E{Quality Gate?}
        E -->|OK| F[Package JAR]
        F --> G[Build Docker Image]
        G --> H[Push to Registry]
        H --> I[Deploy to K8s]
        E -->|Fail| J[Notify Team]
    ```

    **3. Verify consistency:**
    - Every step in the Mermaid diagram must have a corresponding job/step in the YAML
    - Branch triggers must match (main, tags, etc.)
    - Notify/reporting steps must reflect actual pipeline notifications

    **4. Validate Mermaid syntax:**
    - Paste into https://mermaid.live/ before committing
    - Verify GitHub renders it correctly in a PR preview

    **Avoid:**
    - Do NOT create a diagram with fictional CD steps — wait for INF-04
    - Do NOT hardcode stages that don't exist in the actual pipeline YAML
    - Do NOT implement this task until Elias has confirmed INF-04 is complete
  </action>
  <verify>
    <automated>
      # Verify CI/CD Mermaid exists
      grep -c 'CI/CD' README.md
      # Verify it's a Mermaid graph (not placeholder text)
      grep -c 'graph' README.md
      # Verify consistency with pipeline YAML — check for branch trigger mention
      grep -c 'push.*main' README.md
    </automated>
  </verify>
  <done>
    CI/CD Mermaid diagram in README accurately reflects the complete pipeline (build → test → Docker → registry → deploy K8s). All stages are consistent with actual YAML files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → CI/CD YAML | Documentation reflects pipeline implementation — no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | README.md · pipeline details | accept | Pipeline YAML already public in repository; README diagram only summarizes what's in source |
| T-08-SC | Tampering | No package installs | accept | Phase is documentation-only — zero npm/pip/cargo dependencies. |
</threat_model>

<verification>
1. INF-04 (Elias CD pipeline) is complete before executing this task
2. CI/CD Mermaid diagram matches actual pipeline YAML step-for-step
3. Diagram renders correctly on GitHub (validated via mermaid.live)
4. Placeholder text from Plan 08-01 is replaced
</verification>

<success_criteria>
- [ ] INF-04 is verified complete before execution
- [ ] CI/CD Mermaid diagram added to README.md
- [ ] All pipeline stages in diagram exist in actual YAML
- [ ] Mermaid syntax validates cleanly on mermaid.live
</success_criteria>

<output>
Create `.planning/phases/08-documentation-polish/08-02-SUMMARY.md` when done
</output>