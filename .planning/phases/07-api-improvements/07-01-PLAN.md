---
phase: 07-api-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-api-improvements/07-01-API-VERIFICATION.md
  - .planning/phases/07-api-improvements/07-CONTEXT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03

must_haves:
  truths:
    - "Professor tem documentação clara de ambos os cenários para 'APIs' (endpoints vs microsserviços) — pendente de resposta"
    - "POST /api/v1/os é verificado como existente com evidência de código e teste"
    - "GET /api/v1/os/{id}/status é verificado como existente com evidência de código e teste"
  artifacts:
    - path: .planning/phases/07-api-improvements/07-01-API-VERIFICATION.md
      provides: "Verification report for API-01, API-02, API-03"
      min_lines: 50
    - path: .planning/REQUIREMENTS.md
      provides: "Updated traceability for API-01, API-02, API-03"
      min_lines: 197
  key_links:
    - from: "07-01-API-VERIFICATION.md"
      to: "mekano-rest/OrdemDeServicoResource.java:72-85"
      via: "evidence for POST /api/v1/os"
    - from: "07-01-API-VERIFICATION.md"
      to: "mekano-rest/OrdemDeServicoResource.java:101-111"
      via: "evidence for GET /api/v1/os/{id}/status"
    - from: "07-01-API-VERIFICATION.md"
      to: "mekano-rest/OrdemDeServicoResourceTest.java"
      via: "evidence of E2E test coverage"
---

<objective>
Verificar e documentar a existência dos endpoints de OS (API-02, API-03) e esclarecer ambiguidade sobre "APIs" (API-01).

**Purpose:** Estabelecer documentação de referência sobre o que já existe na API antes de qualquer modificação. API-01 fica pendente do esclarecimento do professor — ambos os cenários são documentados.

**Output:** `07-01-API-VERIFICATION.md` com evidências de código, testes e análise de cada endpoint.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-api-improvements/07-CONTEXT.md
@.planning/phases/07-api-improvements/07-RESEARCH.md
@mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document API-01 — "APIs" ambiguity pending professor clarification</name>
  <files>.planning/phases/07-api-improvements/07-01-API-VERIFICATION.md</files>
  <action>
    Create `07-01-API-VERIFICATION.md` in the phase directory. This is the single output document for all three verification tasks.

    For API-01, write a section titled "## API-01: 'APIs' — Endpoints ou Múltiplas APIs?" with:
    - **Status:** PENDENTE — aguardando esclarecimento do professor (per D-01)
    - **Contexto:** O requisito pergunta se "APIs" no título da fase refere-se a endpoints individuais (e.g., GET /api/v1/os) ou a múltiplos serviços/sistemas (microsserviços separados)
    - **Cenário A (Endpoints):** A fase refere-se a verificar e melhorar endpoints específicos da API REST existente. Evidência: o código atual é um monolito modular (mekano-rest), não múltiplos serviços.
    - **Cenário B (Múltiplas APIs/Microsserviços):** A fase poderia referir-se à separação em APIs independentes (auth, OS, estoque). Evidência: o termo "APIs" no plural pode indicar essa intenção.
    - **Recomendação:** Ambos os cenários estão documentados. NENHUMA implementação será feita até o esclarecimento do professor.
    - **Anexar referência:** D-01 de CONTEXT.md
  </verify>
  <verify>
    <automated>grep -c "API-01" .planning/phases/07-api-improvements/07-01-API-VERIFICATION.md</automated>
  </verify>
  <done>API-01 section exists in verification document with both scenarios documented and status PENDENTE</done>
</task>

<task type="auto">
  <name>Task 2: Verify and document API-02 — POST /api/v1/os exists</name>
  <files>.planning/phases/07-api-improvements/07-01-API-VERIFICATION.md</files>
  <action>
    Append to `07-01-API-VERIFICATION.md` a section titled "## API-02: Endpoint de Abertura de OS" with:

    - **Status:** VERIFICADO — endpoint existe (per D-02)
    - **Endpoint:** `POST /api/v1/os`
    - **Localização:** `OrdemDeServicoResource.java` linha 72-85
    - **Assinatura:** `public Response create(@Valid CreateOrdemDeServicoRequest request, @Context UriInfo uriInfo)`
    - **Roles:** `@RolesAllowed({"admin", "atendente"})`
    - **Comportamento:** Cria OS com status inicial RECEBIDA, retorna 201 + Location header
    - **Evidência de teste:** Teste E2E `create_asAdmin_returns201` em `OrdemDeServicoResourceTest.java` (confirmado passando)
    - **DTO de entrada:** `CreateOrdemDeServicoRequest` com `clienteId` (UUID), `veiculoId` (UUID), `descricaoProblema` (String)
    - **DTO de saída:** `OrdemDeServicoResponse` (record)
    - **Conclusão:** Endpoint existe e está funcional. Nenhuma modificação necessária para API-02.
  </verify>
  <verify>
    <automated>grep -c "API-02" .planning/phases/07-api-improvements/07-01-API-VERIFICATION.md</automated>
  </verify>
  <done>API-02 section exists with code evidence (line references), role annotations, and test evidence</done>
</task>

<task type="auto">
  <name>Task 3: Verify and document API-03 — GET /api/v1/os/{id}/status exists</name>
  <files>.planning/phases/07-api-improvements/07-01-API-VERIFICATION.md</files>
  <action>
    Append to `07-01-API-VERIFICATION.md` a section titled "## API-03: Endpoint de Consulta de Status da OS" with:

    - **Status:** VERIFICADO — endpoint existe (per D-03)
    - **Endpoint:** `GET /api/v1/os/{id}/status`
    - **Localização:** `OrdemDeServicoResource.java` linhas 101-111
    - **Assinatura:** `public Response getStatus(@PathParam("id") UUID id)`
    - **Segurança:** `@PermitAll` — endpoint público (não requer autenticação, per AUTH-03)
    - **Comportamento:** Retorna UUID da OS, nome do status enum e createdAt
    - **DTO de saída:** `OrdemDeServicoStatusResponse` (record) com campos `id` (UUID), `status` (String), `createdAt` (LocalDateTime)
    - **Evidência de teste:** Teste E2E `getStatus_anonimo_retorna200` em `OrdemDeServicoResourceTest.java` (confirmado passando)
    - **Rationale (D-02):** UUID da OS é a chave de acesso — sem autenticação porque o cliente não tem login
    - **Conclusão:** Endpoint existe e está funcional. Nenhuma modificação necessária para API-03.

    After appending, add a final "## Summary" section listing all three API items with their verification status.

    Then update `.planning/REQUIREMENTS.md`:
    - Change API-01 status from `Pending` to `Pendente (doc created, awaiting professor)`
    - Change API-02 status from `Pending` to `Complete (verified)`
    - Change API-03 status from `Pending` to `Complete (verified)`

    Also update `.planning/phases/07-api-improvements/07-CONTEXT.md` — add a line at the top:
    ```
    **Plan 07-01 completed:** API-01 documented (pending professor), API-02 verified, API-03 verified
    ```
  </verify>
  <verify>
    <automated>
      grep -c "API-03" .planning/phases/07-api-improvements/07-01-API-VERIFICATION.md && grep -c "Complete (verified)" .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <done>API-03 section exists with code evidence and `@PermitAll` annotation; REQUIREMENTS.md updated for all three API items; CONTEXT.md updated with plan completion note</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Plan 07-01 is documentation-only. No code changes. No new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | N/A | N/A | accept | Documentation-only tasks — no code paths altered, no attack surface introduced |
</threat_model>

<verification>
- [ ] `07-01-API-VERIFICATION.md` exists with sections for API-01, API-02, API-03
- [ ] API-01 section documents both scenarios and status PENDENTE
- [ ] API-02 section has exact line references and role annotations
- [ ] API-03 section has `@PermitAll` evidence
- [ ] REQUIREMENTS.md updated for API-01, API-02, API-03 status
</verification>

<success_criteria>
1. Verification document `07-01-API-VERIFICATION.md` created with all three API items analyzed
2. Each analysis includes code evidence (line numbers), security annotations, DTO contracts, and test evidence
3. REQUIREMENTS.md reflects current status: API-02 and API-03 marked verified
4. API-01 clearly documented as pending professor clarification with both scenarios listed
</success_criteria>

<output>
Create `.planning/phases/07-api-improvements/07-01-SUMMARY.md` when done
</output>