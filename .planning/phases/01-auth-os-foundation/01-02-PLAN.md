---
phase: 01-auth-os-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - mekano-domain/src/main/java/com/fiap/mekano/domain/model/Cliente.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Cpf.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Endereco.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Telefone.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/ClienteServicePort.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/CreateClienteCommand.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/UpdateClienteCommand.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/ClienteRepositoryPort.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/exception/ClienteNotFoundException.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/event/ClienteCriadoEvent.java (NEW)
  - mekano-application/src/main/java/com/fiap/mekano/application/service/cliente/ClienteService.java (NEW)
  - mekano-application/src/main/java/com/fiap/mekano/application/service/cliente/CreateClienteResponse.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/ClienteEntity.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ClientePanacheRepository.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ClienteRepositoryImpl.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/ClienteEntityMapper.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/ClienteEntityMapperImpl.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/CpfMapper.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/EnderecoMapper.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/TelefoneMapper.java (NEW)
  - mekano-infrastructure/src/main/resources/db/migration/V7__create_clientes_table.sql (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/ClienteResource.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreateClienteRequest.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/UpdateClienteRequest.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/ClienteResponse.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/ClientePageResponse.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/ClienteDtoMapper.java (NEW)
  - mekano-rest/src/main/resources/seed-data.sql (MODIFY — add test admin user)
autonomous: true
requirements: [OS-01, OS-02]

must_haves:
  truths:
    - "Admin/atendente can create a client with nome, CPF (validated, unique), email, telefone, endereco"
    - "Admin/atendente can edit a client (nome, email, telefone, endereco) — CPF not updatable"
    - "Admin/atendente can list clients with pagination"
    - "Admin/atendente can get a client by UUID"
    - "Admin/atendente can delete a client (soft delete)"
    - "CPF with invalid checksum is rejected with 400"
    - "Duplicate CPF returns 409"
  artifacts:
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/model/Cliente.java"
      provides: "Cliente domain entity with create/reconstitute"
      contains: "Cliente.create"
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Cpf.java"
      provides: "CPF value object with checksum validation"
      contains: "class Cpf"
    - path: "mekano-infrastructure/src/main/resources/db/migration/V7__create_clientes_table.sql"
      provides: "Clientes table with endereco flattening"
      contains: "clientes"
    - path: "mekano-rest/src/main/java/com/fiap/mekano/rest/api/ClienteResource.java"
      provides: "REST endpoints for cliente CRUD"
      contains: "@RolesAllowed"
  key_links:
    - from: "ClienteResource"
      to: "ClienteServicePort"
      via: "CDI injection"
    - from: "ClienteService"
      to: "ClienteRepositoryPort"
      via: "CDI injection"
    - from: "ClienteRepositoryImpl"
      to: "ClientePanacheRepository"
      via: "CDI injection"
---

<objective>
Implement the Cliente (Client) CRUD aggregate end-to-end: domain model with CPF/Endereco/Telefone Value Objects, application service, JPA persistence with Flyway migration, and REST endpoints.

Purpose: Admin and atendente users can manage clients (create, read, list, soft-delete) with CPF uniqueness validation. This is the first domain entity that downstream aggregates (Veiculo, OrdemDeServico) will reference.

Output: Working Cliente CRUD API at /api/v1/clientes with full test coverage across all layers.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-os-foundation/01-CONTEXT.md
@.planning/phases/01-auth-os-foundation/01-RESEARCH.md
@.planning/phases/01-auth-os-foundation/01-PATTERNS.md
@.planning/phases/01-auth-os-foundation/01-VALIDATION.md

# Pattern references — replicate exactly
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Email.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/UserServicePort.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/CreateUserCommand.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/UserRepositoryPort.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-application/src/main/java/com/fiap/mekano/application/service/user/UserService.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/BaseEntity.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapperImpl.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/EmailMapper.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/UserResource.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/UserDtoMapper.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreateUserRequest.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/UserResponse.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create domain layer — Cliente model, VOs (Cpf/Endereco/Telefone), ports, exceptions, events</name>
  <files>
    mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Cpf.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Endereco.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Telefone.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/Cliente.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/ClienteServicePort.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/CreateClienteCommand.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/ClienteRepositoryPort.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/exception/ClienteNotFoundException.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/event/ClienteCriadoEvent.java (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Email.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 91-163 for Cliente model template, lines 247-296 for VO templates, lines 334-388 for port templates, lines 431-454 for event templates)
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-RESEARCH.md (lines 245-270 for Cliente model fields)
  </read_first>
  <behavior>
    - Cpf("52998224725") creates valid CPF, Cpf("11111111111") throws AppException(400)
    - Cpf(null) throws AppException(400), Cpf("") throws AppException(400)
    - Endereco validates UF length==2, CEP length==8
    - Telefone strips non-digits, validates DDD+number pattern
    - Cliente.create() with valid args creates entity with UUID and createdAt
    - Cliente.reconstitute() preserves exact UUID and createdAt
    - ClienteServicePort has CRUD methods matching UserServicePort pattern
    - ClienteCriadoEvent is a record with static of() factory
  </action>
  Create domain layer following PATTERNS.md templates exactly (User.java and Email.java analogs):

  1. **`Cpf.java`** in `valueobject/`:
     - `final class` with `@EqualsAndHashCode`, `@Getter`, `@ToString`
     - `static final Pattern CPF_PATTERN = Pattern.compile("\\d{11}")` (11 digits only validation)
     - `static final int[] PESO_1 = {10,9,8,7,6,5,4,3,2}` and `PESO_2 = {11,10,9,8,7,6,5,4,3,2}` for checksum
     - Constructor: null/blank → `AppException(400)`, not 11 digits → `AppException(400)`, invalid checksum → `AppException(400)`
     - Checksum: strip non-digits with `replaceAll("\\D", "")`, validate both verifier digits using standard CPF algorithm
     - Normalize: strip non-digits only (no lowercase needed, CPF is digits-only)
     - Use `import com.fiap.mekano.domain.exception.AppException`

  2. **`Endereco.java`** in `valueobject/`:
     - `final class` with fields: `String logradouro, numero, bairro, cidade, uf, cep`
     - No `@EqualsAndHashCode` on the class (flattened pattern — use individual fields)
     - Validate `uf` length == 2 (uppercase), `cep` length == 8 (digits only)
     - Accept as individual constructor params or multi-param constructor (not a single string)

  3. **`Telefone.java`** in `valueobject/`:
     - `final class` with `@EqualsAndHashCode`, `@Getter`, `@ToString`
     - Constructor: null/blank → `AppException(400)`, normalize by `replaceAll("\\D", "")`, validate Brazilian format (DDD 2 digits + number 8-9 digits)
     - Normalize: strip non-digits only

  4. **`Cliente.java`** in `model/`:
     - Exact User.java pattern: `@Builder(access=PRIVATE)`, `@Getter`, `@ToString`
     - Fields: `UUID id`, `String nome`, `Cpf cpf`, `Email email` (reuse existing), `Telefone telefone`, `LocalDateTime createdAt`, plus optional: `String enderecoLogradouro, enderecoNumero, enderecoBairro, enderecoCidade, enderecoUf, enderecoCep`
     - Factory `create(nome, cpfValue, emailValue, telefoneValue, ...enderecoFields...)` — receives strings, validates via VO constructors, generates UUID + timestamp
     - Factory `reconstitute(id, nome, cpfValue, emailValue, telefoneValue, ...enderecoFields..., createdAt)` — preserves exact UUID + timestamp

  5. **`ClienteServicePort.java`** in `port/in/`:
     - Interface with methods: `Cliente execute(CreateClienteCommand)`, `Cliente update(UUID, UpdateClienteCommand)`, `Cliente findById(UUID)`, `List<Cliente> findAll(int, int, String)`, `long countAll()`, `void delete(UUID)`
     - No framework annotations

  6. **`CreateClienteCommand.java`** in `port/in/`:
     - Java record with all Cliente creation fields as Strings (names match the factory method params)

  7. **`UpdateClienteCommand.java`** in `port/in/`:
     - Java record with all updatable Cliente fields: String nome, String email, String telefone, String enderecoLogradouro, String enderecoNumero, String enderecoBairro, String enderecoCidade, String enderecoUf, String enderecoCep
     - CPF is NOT updatable (immutable identifier per D-17)

  7. **`ClienteRepositoryPort.java`** in `port/out/`:
     - Interface with: `Cliente save(Cliente)`, `Optional<Cliente> findById(UUID)`, `Optional<Cliente> findByCpf(String)`, `boolean existsByCpf(String)`, `List<Cliente> findAll(int, int, String)`, `long countAll()`, `void markAsDeleted(UUID)`

  8. **`ClienteNotFoundException.java`** in `exception/` — extends `AppException`? Actually per domain CLAUDE.md, exceptions extend `DomainException` or `BusinessException`. Create `ClienteNotFoundException extends DomainException` (unchecked). But PATTERNS.md shows `AppException(404, "...")` usage — check the existing exception pattern. Looking at UserRepositoryImpl, it throws `AppException(404, ...)` directly, no specific exception class. So skip creating specific exception — throw `AppException(404, "Cliente não encontrado")` inline, matching User pattern.

     Actually, the domain CLAUDE.md mentions specific exceptions like `UserNotFoundException`. Let's create `ClienteNotFoundException extends DomainException` to match the existing pattern. Constructor takes `String message` and optional `Throwable cause`.

  9. **`ClienteCriadoEvent.java`** in `event/`:
     - Record matching `UserCreatedEvent` pattern: `Cliente cliente, LocalDateTime occurredAt` + static `of(Cliente)` factory
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-domain -am -Dtest="*CpfTest,*Cliente*" -q 2>&1 | Select-String -NotMatch "(BUILD SUCCESS|Tests run)"</automated>
  </verify>
  <acceptance_criteria>
    - Cpf VO: valid CPF (52998224725) accepted, repeated digits (11111111111) rejected, null/blank rejected
    - Cpf: checksum algorithm validates both verifier digits correctly
    - Telefone VO: valid phone accepted, invalid pattern rejected
    - Endereco: validates UF length and CEP format
    - Cliente.create() returns entity with non-null UUID and createdAt
    - Cliente.reconstitute() preserves exact input UUID and createdAt
    - ClienteServicePort interface compiles with all CRUD methods
    - ClienteCriadoEvent record compiles with of() factory
    - Domain tests all pass (JUnit 5, no Quarkus needed)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create application service + infrastructure layer (entity, repositories, mappers, migration)</name>
  <files>
    mekano-application/src/main/java/com/fiap/mekano/application/service/cliente/ClienteService.java (NEW)
    mekano-application/src/main/java/com/fiap/mekano/application/service/cliente/CreateClienteResponse.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/ClienteEntity.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ClientePanacheRepository.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ClienteRepositoryImpl.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/ClienteEntityMapper.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/ClienteEntityMapperImpl.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/CpfMapper.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/EnderecoMapper.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/TelefoneMapper.java (NEW)
    mekano-infrastructure/src/main/resources/db/migration/V7__create_clientes_table.sql (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-application/src/main/java/com/fiap/mekano/application/service/user/UserService.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 456-600 for service template, lines 604-707 for entity template, lines 749-864 for repository template, lines 866-953 for mapper templates, lines 1271-1291 for Flyway migration template)
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapperImpl.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/EmailMapper.java
  </read_first>
  <behavior>
    - ClienteService.execute with valid command creates and persists cliente
    - ClienteService.execute with duplicate CPF throws AppException(409)
    - ClienteService.findById with unknown UUID throws AppException(404)
    - ClienteService.delete marks as inactive (soft delete)
    - V7 migration creates clientes table with endereco_flattened columns
    - ClienteEntityMapper maps domain<->entity via CpfMapper/EnderecoMapper/TelefoneMapper
  </behavior>
  <action>
    Create all infrastructure + application files:

    1. **`ClienteService.java`** in `application/service/cliente/`:
       - `@ApplicationScoped`, implements `ClienteServicePort`
       - Constructor injection: `ClienteRepositoryPort`, `EventPublisher`
       - `execute()`: validates nome non-blank, checks CPF duplicate, calls `Cliente.create()`, saves, publishes event
       - `update(UUID, UpdateClienteCommand)`: loads existing cliente, updates mutable fields (nome, email, telefone, endereco), saves
       - `findById()`, `findAll()`, `countAll()`, `delete()` — match UserService pattern exactly
       - `@Transactional` on `execute()` and `update()`

    2. **`CreateClienteResponse.java`** — record: `UUID id, String nome, String cpf, String email, String telefone, LocalDateTime createdAt` (no password hash, no sensitive fields)

    3. **`V7__create_clientes_table.sql`** — create `clientes` table:
       - `id BIGINT GENERATED BY DEFAULT AS IDENTITY` (not BIGSERIAL — H2 compat)
       - `uuid UUID UNIQUE NOT NULL`
       - `nome VARCHAR(100) NOT NULL`
       - `cpf VARCHAR(11) UNIQUE NOT NULL`
       - `email VARCHAR(255) NOT NULL`
       - `telefone VARCHAR(20) NOT NULL`
       - `endereco_logradouro VARCHAR(255)`, `endereco_numero VARCHAR(20)`, `endereco_bairro VARCHAR(100)`, `endereco_cidade VARCHAR(100)`, `endereco_uf VARCHAR(2)`, `endereco_cep VARCHAR(8)`
       - `created_at TIMESTAMP NOT NULL DEFAULT NOW()`, `updated_at TIMESTAMP`, `deleted_at TIMESTAMP`, `is_active BOOLEAN DEFAULT TRUE`
       - Separate ALTER TABLE statements for multiple ADD COLUMN (H2 compat rule)

    4. **`ClienteEntity.java`** in `infrastructure/entity/`:
       - `@Entity @Table(name = "clientes")`, `extends BaseEntity`
       - Fields: `UUID uuid` (unique), `String nome, cpf, email, telefone`, endereco fields (flattened)
       - `@Column(unique=true)` on cpf

    5. **`ClientePanacheRepository.java`** — `extends PanacheRepositoryBase<ClienteEntity, Long>`

    6. **`ClienteRepositoryImpl.java`** — exact match of `UserRepositoryImpl` pattern with:
       - `@CacheResult/@CacheInvalidate` for clientes cache
       - `@Retry(maxRetries=3)` on reads, `@Timeout(5s)` on save
       - HQL with `isActive = true` filter on all reads
       - `handleConstraintViolation()` for CPF duplicate detection

    7. **VO Mappers** (`CpfMapper`, `EnderecoMapper`, `TelefoneMapper`) — exact match of `EmailMapper` pattern:
       - `@ApplicationScoped`, `@Named` methods: `*ToString(VO)` and `stringTo*(String)`
       - Each `stringTo*()` calls VO constructor (which validates)

    8. **`ClienteEntityMapper.java`** interface + **`ClienteEntityMapperImpl.java`**:
       - Interface declares `toEntity(Cliente)` and `toDomain(ClienteEntity)`
       - Impl: `@ApplicationScoped`, injects VO mappers, follows `UserEntityMapperImpl` pattern
       - `toEntity()` maps domain fields + flattens VOs (cpf→cpfMapper.cpfToString(), etc.)
       - `toDomain()` maps entity fields + calls `Cliente.reconstitute()` with VO constructors
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-infrastructure -am -Dtest="*Cliente*" -q 2>&1 | Select-String -NotMatch "(BUILD SUCCESS|Tests run)"</automated>
  </verify>
  <acceptance_criteria>
    - ClienteService.execute() persists cliente and returns saved entity
    - ClienteService.update() updates mutable fields (nome, email, telefone)
    - Duplicate CPF in execute() throws AppException(409)
    - findById() returns Cliente for existing UUID, throws AppException(404) for unknown
    - markAsDeleted() sets isActive=false without physically deleting
    - V7 migration creates clientes table with all endereco columns
    - ClienteEntityMapper.toDomain() calls Cliente.reconstitute() (preserves UUID + createdAt)
    - Infrastructure tests pass with DevServices PostgreSQL
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create REST resource — ClienteResource + DTOs + DtoMapper</name>
  <files>
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/ClienteResource.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreateClienteRequest.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/ClienteResponse.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/ClientePageResponse.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/ClienteDtoMapper.java (NEW)
    mekano-rest/src/main/resources/seed-data.sql (MODIFY — add admin user + role for manual testing)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/UserResource.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreateUserRequest.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/UserResponse.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/UserDtoMapper.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 1010-1155 for resource template, lines 1157-1240 for DTO template, lines 1244-1270 for DTO mapper template)
  </read_first>
  <behavior>
    - POST /api/v1/clientes with valid data returns 201 + location header
    - PUT /api/v1/clientes/{id} with valid data returns 200 (update)
    - POST /api/v1/clientes with duplicate CPF returns 409
    - POST /api/v1/clientes with invalid CPF returns 400
    - GET /api/v1/clientes returns paginated list
    - GET /api/v1/clientes/{id} returns single cliente
    - DELETE /api/v1/clientes/{id} returns 204 (soft delete)
  </behavior>
  <action>
    Create REST layer following UserResource pattern exactly:

    1. **`ClienteResource.java`** in `rest/api/`:
       - `@Path("/clientes")`, `@RequestScoped`, `@RolesAllowed({"admin", "atendente"})` per D-11
       - `@Tag(name = "Clientes", description = "CRUD de clientes")`
       - Inject `ClienteServicePort`, `ClienteDtoMapper`
       - Endpoints: POST (create with @Valid CreateClienteRequest), PUT /{id} (update with @Valid UpdateClienteRequest), GET (list paginated), GET /{id} (find), DELETE /{id} (soft delete)
       - `@APIResponse` annotations for 200, 201, 400, 404, 409 on appropriate methods
       - Return `Response.created(location)` on POST with `uriInfo.getAbsolutePathBuilder()`
       - Return `Response.ok(response)` on PUT
       - No `@Transactional` (D-01)

    2. **`CreateClienteRequest.java`** in `dto/`:
       - Class with Lombok `@Getter @Setter @NoArgsConstructor`
       - Bean Validation: `@NotBlank` on nome, cpf, email, telefone
       - `@Pattern(regexp = "\\d{11}")` on cpf (11 digits)
       - `@Email` on email
       - `@Schema` annotations for OpenAPI

    3. **`UpdateClienteRequest.java`** in `dto/`:
       - Same fields as CreateClienteRequest minus CPF (CPF not updatable)
       - `@NotBlank` on nome, email, telefone
       - `@Schema` annotations for OpenAPI

    4. **`ClienteResponse.java`** in `dto/`:
       - Java record: `UUID id, String nome, String cpf, String email, String telefone, LocalDateTime createdAt`
       - `@Schema` annotations for OpenAPI

    4. **`ClientePageResponse.java`** in `dto/`:
       - Java record: `List<ClienteResponse> content, int page, int size, long totalElements, int totalPages`

    5. **`ClienteDtoMapper.java`** in `mapper/`:
       - `@Mapper(componentModel = "cdi")` (NOT "spring" — G9)
       - `toCommand(CreateClienteRequest)` → `CreateClienteCommand`
       - `toResponse(Cliente)` with `@Mapping` expressions for VO extraction: `java(cliente.getCpf().getValue())`, `java(cliente.getEmail().getValue())`, `java(cliente.getTelefone().getValue())`

    6. Update `seed-data.sql` with INSERT for admin user (BCrypt password hash) + user_roles entry for manual dev testing. Use a comment noting the password hash must be generated.
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-rest -am -Dtest="*Cliente*" -q 2>&1 | Select-String -NotMatch "(BUILD SUCCESS|Tests run)"</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/v1/clientes with valid data returns 201 with location header pointing to /api/v1/clientes/{uuid}
    - PUT /api/v1/clientes/{id} with valid data returns 200 with updated fields
    - POST /api/v1/clientes with same CPF returns 409
    - POST /api/v1/clientes with invalid CPF (wrong checksum) returns 400
    - GET /api/v1/clientes returns 200 with paginated ClientePageResponse
    - GET /api/v1/clientes/{id} returns 200 with single ClienteResponse
    - DELETE /api/v1/clientes/{id} returns 204 (soft delete)
    - GET /api/v1/clientes/{id} after delete returns 404
    - All REST tests pass with @TestSecurity(roles = {"admin"})
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### New Domain Classes
- `Cliente` — entity with create()/reconstitute() factory methods
- `Cpf` — value object with checksum validation (11 digits, 2 verifier digits)
- `Endereco` — value object (logradouro, numero, bairro, cidade, uf, cep)
- `Telefone` — value object (Brazilian phone with DDD)
- `ClienteServicePort` — input port interface with execute/update/findById/findAll/delete
- `CreateClienteCommand` — command record for creation
- `UpdateClienteCommand` — command record for update (no CPF)
- `ClienteRepositoryPort` — output port interface
- `ClienteNotFoundException` — extends DomainException
- `ClienteCriadoEvent` — domain event record

### New Application Classes
- `ClienteService` — @ApplicationScoped service with @Transactional execute()
- `CreateClienteResponse` — response record

### New Infrastructure Classes
- `ClienteEntity` — JPA entity extending BaseEntity (table clientes)
- `ClientePanacheRepository` — PanacheRepositoryBase
- `ClienteRepositoryImpl` — implements ClienteRepositoryPort
- `ClienteEntityMapper` (interface) + `ClienteEntityMapperImpl` (impl)
- `CpfMapper` — VO <-> String mapper
- `EnderecoMapper` — flattened field mapper
- `TelefoneMapper` — VO <-> String mapper

### New REST Classes
- `ClienteResource` — @Path("/clientes"), @RolesAllowed({"admin", "atendente"})
- `CreateClienteRequest` — input DTO with Bean Validation
- `UpdateClienteRequest` — input DTO (no CPF)
- `ClienteResponse` — output record
- `ClientePageResponse` — paginated output record
- `ClienteDtoMapper` — MapStruct componentModel="cdi"

### New Migrations
- `V7__create_clientes_table.sql` — clientes table with flattened endereco

### Seed Script
- `seed-data.sql` — reference INSERT for admin user + role

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→ClienteResource | Untrusted CPF/email/telefone input crosses here |
| ClienteService→ClienteRepositoryPort | Persistence boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | CPF input | mitigate | Cpf VO validates checksum in constructor; `@Pattern(regexp = "\\d{11}")` on request DTO |
| T-01-07 | Info Disclosure | UUID enumeration | mitigate | Hybrid ID: Long PK internal, UUID exposed in API — prevents sequential guessing |
| T-01-08 | Tampering | Duplicate CPF | mitigate | `cpf UNIQUE` constraint in DB; duplicate check via `existsByCpf()` in service layer; handleConstraintViolation catches `PersistenceException` |
| T-01-09 | Tampering | SQL injection | mitigate | Panache HQL with parameterized queries (`?1`, `?2`) — no string concatenation in HQL |
| T-01-10 | Spoofing | Unauthorized access | mitigate | `@RolesAllowed({"admin", "atendente"})` on ClienteResource |
</threat_model>

<verification>
Commands to run after plan execution:
1. `./mvnw test -pl mekano-domain -am -Dtest="*CpfTest,*ClienteTest,*EnderecoTest,*TelefoneTest"` — domain VO tests
2. `./mvnw test -pl mekano-application -am -Dtest="*ClienteService*"` — application tests with Mockito
3. `./mvnw test -pl mekano-infrastructure -am -Dtest="*ClienteRepository*"` — infrastructure integration tests
4. `./mvnw test -pl mekano-rest -am -Dtest="*ClienteResource*"` — REST integration tests
5. `./mvnw verify -pl mekano-rest -am` — full suite
</verification>

<success_criteria>
- [ ] Cpf, Endereco, Telefone VOs validate correctly (domain tests pass)
- [ ] ClienteService CRUD operations work with proper exception handling
- [ ] V7 migration creates clientes table
- [ ] ClienteRepositoryImpl persists and retrieves clientes with soft delete filtering
- [ ] Cliente REST endpoint returns correct status codes for all CRUD operations
- [ ] Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/01-auth-os-foundation/01-02-SUMMARY.md` when done.
</output>
