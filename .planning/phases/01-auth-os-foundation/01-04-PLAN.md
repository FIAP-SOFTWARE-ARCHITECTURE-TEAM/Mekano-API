---
phase: 01-auth-os-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - mekano-domain/src/main/java/com/fiap/mekano/domain/model/Servico.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/ServicoServicePort.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/CreateServicoCommand.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/ServicoRepositoryPort.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/event/ServicoCriadoEvent.java (NEW)
  - mekano-application/src/main/java/com/fiap/mekano/application/service/servico/ServicoService.java (NEW)
  - mekano-application/src/main/java/com/fiap/mekano/application/service/servico/CreateServicoResponse.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/ServicoEntity.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ServicoPanacheRepository.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ServicoRepositoryImpl.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/ServicoEntityMapper.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/ServicoEntityMapperImpl.java (NEW)
  - mekano-infrastructure/src/main/resources/db/migration/V9__create_servicos_table.sql (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/ServicoResource.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreateServicoRequest.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/ServicoResponse.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/ServicoPageResponse.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/ServicoDtoMapper.java (NEW)
autonomous: true
requirements: [OS-05, OS-06]

must_haves:
  truths:
    - "Admin can create a service type with nome, descricao, valor (value > 0)"
    - "Admin can list service types with pagination"
    - "Admin can get a service type by UUID"
    - "Admin can edit a service type (nome, descricao, valor)"
    - "Admin can delete a service type (soft delete)"
    - "Service with valor <= 0 is rejected"
    - "Only admin can access Servico endpoints (atendente blocked)"
  artifacts:
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/model/Servico.java"
      provides: "Servico domain entity with valor validation"
      contains: "class Servico"
    - path: "mekano-infrastructure/src/main/resources/db/migration/V9__create_servicos_table.sql"
      provides: "Servicos table with CHECK valor > 0"
      contains: "CHECK.*valor"
    - path: "mekano-rest/src/main/java/com/fiap/mekano/rest/api/ServicoResource.java"
      provides: "REST endpoints for servico CRUD (admin-only)"
      contains: "RolesAllowed.*admin"
  key_links:
    - from: "ServicoResource"
      to: "ServicoServicePort"
      via: "CDI injection"
    - from: "ServicoService"
      to: "ServicoRepositoryPort"
      via: "CDI injection"
---

<objective>
Implement the Servico (Service Type) aggregate end-to-end: domain model with valor > 0 validation, application service, JPA persistence, and REST endpoints. Admin-only access.

Purpose: Admin can manage service types (name, description, unit price) that will be used as line items in OrdemDeServico.

Output: Working Servico CRUD API at /api/v1/servicos with admin-only access and full test coverage.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-os-foundation/01-CONTEXT.md
@.planning/phases/01-auth-os-foundation/01-RESEARCH.md
@.planning/phases/01-auth-os-foundation/01-PATTERNS.md
@.planning/phases/01-auth-os-foundation/01-VALIDATION.md

# Pattern references
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-application/src/main/java/com/fiap/mekano/application/service/user/UserService.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/UserResource.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/UserDtoMapper.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create domain layer — Servico model, ports, events</name>
  <files>
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/Servico.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/ServicoServicePort.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/CreateServicoCommand.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/UpdateServicoCommand.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/ServicoRepositoryPort.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/event/ServicoCriadoEvent.java (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 91-163 for model template, lines 334-388 for port templates, lines 431-454 for event templates)
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-RESEARCH.md (lines 272-278 for Servico model fields)
  </read_first>
  <behavior>
    - Servico.create() with nome, descricao, valor > 0 creates entity
    - Servico.create() with valor <= 0 throws AppException(400)
    - Servico can be updated (nome, descricao, valor)
    - ServicoServicePort has full CRUD including update
  </behavior>
  <action>
    Create domain layer. Servico is simpler than Cliente — no custom VOs (all fields are primitive/String/BigDecimal).

    1. **`Servico.java`** in `model/`:
       - `@Builder(access=PRIVATE)`, `@Getter`, `@ToString`
       - Fields: `UUID id`, `String nome`, `String descricao`, `BigDecimal valor`, `LocalDateTime createdAt`
       - Factory `create(nome, descricao, valor)` — validates valor > 0 (throws AppException(400) if not), generates UUID + createdAt
       - Factory `reconstitute(id, nome, descricao, valor, createdAt)` — preserves exact values, validates valor > 0
       - Method `atualizar(nome, descricao, valor)` — updates mutable fields with validation (valor > 0). Note: `valor` field can be non-final (updatable).

    2. **`ServicoServicePort.java`** — extends UserServicePort pattern with additional `update()` method: execute, updateById, findById, findAll, countAll, delete

    3. **`CreateServicoCommand.java`** — record: String nome, String descricao, BigDecimal valor

    4. **`UpdateServicoCommand.java`** — record: UUID id, String nome, String descricao, BigDecimal valor

    5. **`ServicoRepositoryPort.java`** — save, findById, findByNome (for duplicate check), existsByNome, findAll, countAll, markAsDeleted. Add also `save(Servico)` for both create and update (upsert pattern).

    6. **`ServicoCriadoEvent.java`** — record matching UserCreatedEvent pattern
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-domain -am -Dtest="*Servico*" -q 2>&1 | Select-String -NotMatch "(BUILD SUCCESS|Tests run)"</automated>
  </verify>
  <acceptance_criteria>
    - Servico.create with valor > 0 succeeds
    - Servico.create with valor <= 0 throws AppException(400)
    - Servico.create with null nome throws AppException(400)
    - Servico.atualizar() updates fields with validation
    - Servico.reconstitute() preserves exact input values
    - All domain tests pass
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create application service + infrastructure layer</name>
  <files>
    mekano-application/src/main/java/com/fiap/mekano/application/service/servico/ServicoService.java (NEW)
    mekano-application/src/main/java/com/fiap/mekano/application/service/servico/CreateServicoResponse.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/ServicoEntity.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ServicoPanacheRepository.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ServicoRepositoryImpl.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/ServicoEntityMapper.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/ServicoEntityMapperImpl.java (NEW)
    mekano-infrastructure/src/main/resources/db/migration/V9__create_servicos_table.sql (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-application/src/main/java/com/fiap/mekano/application/service/user/UserService.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapperImpl.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 456-600 for service, lines 604-707 for entity, lines 749-864 for repository)
  </read_first>
  <behavior>
    - ServicoService.execute with valid command persists servico
    - ServicoService.execute with valor <= 0 throws AppException(400)
    - ServicoService.updateById updates existing servico fields
    - V9 migration creates servicos table with CHECK valor > 0
  </behavior>
  <action>
    1. **`ServicoService.java`** in `application/service/servico/`:
       - `@ApplicationScoped`, implements `ServicoServicePort`
       - Constructor injection: `ServicoRepositoryPort`, `EventPublisher`
       - `execute()`: validates nome non-blank, checks duplicate nome, creates Servico, saves, publishes event
       - `updateById(UpdateServicoCommand)`: finds existing, calls `servico.atualizar()`, saves
       - `findById()`, `findAll()`, `countAll()`, `delete()` — standard pattern
       - `@Transactional` on `execute()`, `updateById()`, `delete()`

    2. **`CreateServicoResponse.java`** — record: UUID id, String nome, String descricao, BigDecimal valor, LocalDateTime createdAt

    3. **`V9__create_servicos_table.sql`**:
       - `id BIGINT GENERATED BY DEFAULT AS IDENTITY`
       - `uuid UUID UNIQUE NOT NULL`
       - `nome VARCHAR(100) NOT NULL`
       - `descricao TEXT`
       - `valor DECIMAL(10,2) NOT NULL CHECK (valor > 0)`
       - Audit fields: created_at, updated_at, deleted_at, is_active DEFAULT TRUE

    4. **`ServicoEntity.java`** — `@Entity @Table(name = "servicos")`, extends BaseEntity. Fields: UUID uuid, String nome, String descricao, BigDecimal valor. Use `@Column(precision = 10, scale = 2)` on valor.

    5. **`ServicoPanacheRepository.java`** — PanacheRepositoryBase

    6. **`ServicoRepositoryImpl.java`** — standard pattern with @CacheResult/@CacheInvalidate, @Retry, @Timeout. No VO mappers needed (all primitive fields).

    7. **`ServicoEntityMapper.java`** + **`ServicoEntityMapperImpl.java`** — direct field mapping, no VO mappers needed.
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-infrastructure -am -Dtest="*Servico*" -q 2>&1 | Select-String -NotMatch "(BUILD SUCCESS|Tests run)"</automated>
  </verify>
  <acceptance_criteria>
    - ServicoService.save() persists servico and returns saved entity
    - ServicoService.save with valor <= 0 throws AppException(400)
    - ServicoService.updateById updates nome, descricao, valor
    - V9 migration creates servicos table with CHECK valor > 0
    - Infrastructure tests pass with DevServices
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create REST resource — ServicoResource + DTOs + DtoMapper (admin-only)</name>
  <files>
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/ServicoResource.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreateServicoRequest.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/UpdateServicoRequest.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/ServicoResponse.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/ServicoPageResponse.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/ServicoDtoMapper.java (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/UserResource.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-CONTEXT.md (D-13: Serviços CRUD: só admin)
  </read_first>
  <behavior>
    - POST /api/v1/servicos with valid data returns 201 (admin only)
    - POST /api/v1/servicos with valor <= 0 returns 400
    - PUT /api/v1/servicos/{id} updates servico fields
    - GET /api/v1/servicos returns paginated list
    - GET /api/v1/servicos/{id} returns single servico
    - DELETE /api/v1/servicos/{id} returns 204
    - atendente role gets 403 on all Servico endpoints
  </behavior>
  <action>
    1. **`ServicoResource.java`** — `@Path("/servicos")`, `@RequestScoped`, `@RolesAllowed({"admin"})` per D-13 (admin ONLY). Full CRUD + update endpoint:
       - POST create: @Valid CreateServicoRequest
       - PUT /{id} update: @Valid UpdateServicoRequest
       - GET list: paginated
       - GET /{id}: single
       - DELETE /{id}: soft delete (204)

    2. **`CreateServicoRequest.java`** — `@NotNull @Size(min=1) String nome`, `String descricao`, `@NotNull @DecimalMin("0.01") BigDecimal valor`

    3. **`UpdateServicoRequest.java`** — same fields as CreateServicoRequest (all required)

    4. **`ServicoResponse.java`** — record: UUID id, String nome, String descricao, BigDecimal valor, LocalDateTime createdAt

    5. **`ServicoPageResponse.java`** — standard paginated record

    6. **`ServicoDtoMapper.java`** — `@Mapper(componentModel = "cdi")`, toCommand(req), toCommand(updateReq), toResponse(servico). No VO extraction expressions needed (no custom VOs).
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-rest -am -Dtest="*ServicoResource*" -q 2>&1 | Select-String -NotMatch "(BUILD SUCCESS|Tests run)"</automated>
  </verify>
  <acceptance_criteria>
    - POST 201 with valid data, 400 with valor <= 0
    - PUT 200 update with valid data
    - GET /servicos returns 200 paginated
    - GET /servicos/{id} returns 200/404
    - DELETE returns 204
    - @TestSecurity(roles = {"atendente"}) returns 403
    - All REST tests pass
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### New Domain Classes
- `Servico` — entity with create()/reconstitute()/atualizar()
- `ServicoServicePort` — input port interface with update
- `CreateServicoCommand` — command record
- `UpdateServicoCommand` — command record
- `ServicoRepositoryPort` — output port interface
- `ServicoCriadoEvent` — domain event record

### New Application Classes
- `ServicoService` — @ApplicationScoped with @Transactional execute/update
- `CreateServicoResponse` — response record

### New Infrastructure Classes
- `ServicoEntity` — JPA entity extending BaseEntity (table servicos)
- `ServicoPanacheRepository` — PanacheRepositoryBase
- `ServicoRepositoryImpl` — implements ServicoRepositoryPort
- `ServicoEntityMapper` + `ServicoEntityMapperImpl`

### New REST Classes
- `ServicoResource` — @Path("/servicos"), @RolesAllowed({"admin"})
- `CreateServicoRequest` — input DTO with Bean Validation
- `UpdateServicoRequest` — update DTO
- `ServicoResponse` — output record
- `ServicoPageResponse` — paginated output record
- `ServicoDtoMapper` — MapStruct componentModel="cdi"

### New Migrations
- `V9__create_servicos_table.sql` — servicos table with CHECK valor > 0

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→ServicoResource | Untrusted valor/nome/descricao input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Tampering | Valor <= 0 | mitigate | Domain validates `valor > 0` in constructor/atualizar(); `@DecimalMin("0.01")` on DTO; DB CHECK constraint |
| T-01-16 | Spoofing | Non-admin access | mitigate | `@RolesAllowed({"admin"})` on ServicoResource — only admin can manage service types |
</threat_model>

<verification>
1. `./mvnw test -pl mekano-domain -am -Dtest="*ServicoTest*"` — domain tests
2. `./mvnw test -pl mekano-application -am -Dtest="*ServicoService*"` — application tests
3. `./mvnw test -pl mekano-infrastructure -am -Dtest="*ServicoRepository*"` — infrastructure tests
4. `./mvnw test -pl mekano-rest -am -Dtest="*ServicoResource*"` — REST tests
5. `./mvnw verify -pl mekano-rest -am` — full suite
</verification>

<success_criteria>
- [ ] Servico domain validates valor > 0
- [ ] CRUD + Update work at service layer
- [ ] V9 migration creates servicos table with CHECK constraint
- [ ] REST endpoints admin-only (atendente gets 403)
- [ ] Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/01-auth-os-foundation/01-04-SUMMARY.md` when done.
</output>
