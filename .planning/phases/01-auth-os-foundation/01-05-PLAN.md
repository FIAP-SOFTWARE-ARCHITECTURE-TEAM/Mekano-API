---
phase: 01-auth-os-foundation
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/StatusOS.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/model/OrdemDeServico.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/OrdemDeServicoServicePort.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/CreateOrdemDeServicoCommand.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/OrdemDeServicoRepositoryPort.java (NEW)
  - mekano-domain/src/main/java/com/fiap/mekano/domain/event/OrdemDeServicoCriadaEvent.java (NEW)
  - mekano-application/src/main/java/com/fiap/mekano/application/service/os/OrdemDeServicoService.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/OrdemDeServicoEntity.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/ServicoExecutadoEntity.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/PecaUsadaEntity.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoPanacheRepository.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImpl.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/OrdemDeServicoEntityMapper.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/OrdemDeServicoEntityMapperImpl.java (NEW)
  - mekano-infrastructure/src/main/resources/db/migration/V10__create_ordens_de_servico_table.sql (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreateOrdemDeServicoRequest.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/OrdemDeServicoResponse.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/OrdemDeServicoStatusResponse.java (NEW)
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/OrdemDeServicoDtoMapper.java (NEW)
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/cache/CacheNames.java (MODIFY)
  - mekano-infrastructure/src/main/resources/cache-config.yml (MODIFY)
autonomous: true
requirements: [OS-07, OS-08, OS-15, AUTH-03]

must_haves:
  truths:
    - "Atendente/admin can create an OS (RECEBIDA) identifying cliente (by UUID) and veiculo (by UUID) with solicited services"
    - "Mecanico/admin can start diagnosis on OS (RECEBIDA → EM_DIAGNOSTICO)"
    - "Mecanico can add executed services and used parts during diagnosis"
    - "Invalid OS state transitions are rejected (e.g., RECEBIDA → FINALIZADA)"
    - "Client can query public OS status without authentication (AUTH-03 / OS-15)"
    - "Public status returns limited fields (status, dataEntrada) — not internal details"
  artifacts:
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/StatusOS.java"
      provides: "OS state enum with transition matrix"
      contains: "Map<StatusOS, Set<StatusOS>>"
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/model/OrdemDeServico.java"
      provides: "OS domain entity with explicit transition methods (no setStatus())"
      contains: "iniciarDiagnostico"
    - path: "mekano-infrastructure/src/main/resources/db/migration/V10__create_ordens_de_servico_table.sql"
      provides: "OS + child tables with @Version column"
      contains: "ordens_de_servico"
    - path: "mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java"
      provides: "OS REST endpoints with mixed roles + @PermitAll public status"
      contains: "@PermitAll"
  key_links:
    - from: "OrdemDeServicoResource.consultarStatusPublico"
      to: "OrdemDeServicoServicePort"
      via: "@PermitAll, no auth"
      pattern: "@PermitAll"
    - from: "OrdemDeServico"
      to: "StatusOS"
      via: "transition matrix via transitarPara()"
      pattern: "transitarPara"
    - from: "OrdemDeServicoRepositoryImpl"
      to: "OrdemDeServicoEntity"
      via: "Panache query with @Version lock"
      pattern: "@Version"
---

<objective>
Implement the OrdemDeServico (Service Order) aggregate with state machine, explicit transition methods, child entities (ServicoExecutado, PecaUsada), and a public status endpoint (AUTH-03 / OS-15).

Purpose: Atendente creates OS (RECEBIDA), mecanico starts diagnosis (EM_DIAGNOSTICO) adding services and parts. Client can query OS status publicly.

Output: Working OS API at /api/v1/os with state machine, child entities, and @PermitAll public status endpoint.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-os-foundation/01-CONTEXT.md
@.planning/phases/01-auth-os-foundation/01-RESEARCH.md
@.planning/phases/01-auth-os-foundation/01-PATTERNS.md
@.planning/phases/01-auth-os-foundation/01-VALIDATION.md

# Pattern references — OS state machine
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-RESEARCH.md (lines 424-493 for state machine, lines 610-615 for @Version lock)
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 168-243 for OrdemDeServico entity template)

# Existing patterns to replicate (for standard CRUD pieces)
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-application/src/main/java/com/fiap/mekano/application/service/user/UserService.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/UserResource.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create domain layer — StatusOS enum with transition matrix + OrdemDeServico with state machine + ports</name>
  <files>
    mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/StatusOS.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/OrdemDeServico.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/OrdemDeServicoServicePort.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/CreateOrdemDeServicoCommand.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/OrdemDeServicoRepositoryPort.java (NEW)
    mekano-domain/src/main/java/com/fiap/mekano/domain/event/OrdemDeServicoCriadaEvent.java (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-RESEARCH.md (lines 424-493 for StatusOS transition matrix and OrdemDeServico state machine, lines 737-764 for factory method with ServicoSolicitado)
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 168-243 for OrdemDeServico class template, lines 297-328 for StatusOS enum template)
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-CONTEXT.md (lines 47-56 for D-25 to D-28 OS decisions)
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java
  </read_first>
  <behavior>
    - StatusOS.RECEBIDA.podeTransitarPara(EM_DIAGNOSTICO) returns true
    - StatusOS.RECEBIDA.podeTransitarPara(FINALIZADA) returns false
    - StatusOS.CANCELADA.podeTransitarPara(any) returns false (terminal)
    - All 49 state transitions are testable via parameterized test
    - OrdemDeServico.create() returns OS with RECEBIDA status and solicited services
    - OrdemDeServico.iniciarDiagnostico() transitions RECEBIDA → EM_DIAGNOSTICO
    - OrdemDeServico.iniciarDiagnostico() from wrong state throws AppException(400)
    - OrdemDeServico has no setStatus() method — only explicit transitions
  </action>
  Create domain layer:

  1. **`StatusOS.java`** in `valueobject/`:
      - Enum with values: `RECEBIDA, EM_DIAGNOSTICO, AGUARDANDO_APROVACAO, EM_EXECUCAO, FINALIZADA, ENTREGUE, CANCELADA`
      - Comment: `// Phase 2 adiciona transições (AGUARDANDO_APROVACAO → EM_EXECUCAO via aprovarOrcamento, AGUARDANDO_APROVACAO → CANCELADA via reprovarOrcamento/SLA)`
     - Static `Map<StatusOS, Set<StatusOS>> TRANSICOES` with exact transitions from RESEARCH.md lines 436-445
     - Method `boolean podeTransitarPara(StatusOS destino)` returns `TRANSICOES.getOrDefault(this, Set.of()).contains(destino)`

  2. **`OrdemDeServico.java`** in `model/`:
     - `@Getter @Builder(access = AccessLevel.PRIVATE) @ToString`
     - Fields: `UUID id`, `UUID clienteUuid`, `UUID veiculoUuid`, `StatusOS status`, `LocalDateTime dataEntrada`, `LocalDateTime dataSaida` (nullable), `String observacoes` (nullable), `@Builder.Default List<ServicoSolicitado> servicosSolicitados = new ArrayList<>()`, `Long version`
     - Inner record/builder class `ServicoSolicitado` with: `UUID servicoUuid`, `String nome`, `BigDecimal valor`
     - Factory `create(clienteUuid, veiculoUuid, List<ServicoSolicitado> servicos)` — status=RECEBIDA, dataEntrada=now(), version=0L
     - Factory `reconstitute(...)` — preserves all exact values
     - **NO setStatus() method** — only explicit transition methods:
       - `iniciarDiagnostico()` → EM_DIAGNOSTICO
       - `finalizarDiagnostico()` → AGUARDANDO_APROVACAO
        - `aprovarOrcamento()` → EM_EXECUCAO
        - `reprovarOrcamento(String motivo)` → CANCELADA
        - `cancelar()` → CANCELADA
        - `iniciarExecucao()` → EM_EXECUCAO
        - `finalizar()` → FINALIZADA
        - `entregar()` → ENTREGUE
     - Private `transitarPara(StatusOS destino)` — validates via `StatusOS.podeTransitarPara()`, throws `AppException(400)` on invalid transition
     - Method `getStatusPublico()` returns a simple string record: status name + dataEntrada (for public endpoint)

  3. **`OrdemDeServicoServicePort.java`** in `port/in/`:
     - Methods: `OrdemDeServico execute(CreateOrdemDeServicoCommand)`, `OrdemDeServico iniciarDiagnostico(UUID)`, `OrdemDeServico consultarStatus(UUID)` (for public status), `OrdemDeServico findById(UUID)`, `List<OrdemDeServico> findAll(int, int, String)`, `long countAll()`

  4. **`CreateOrdemDeServicoCommand.java`** — record: UUID clienteUuid, UUID veiculoUuid, String observacoes, List<ServicoSolicitadoDTO> servicosSolicitados. Inner record `ServicoSolicitadoDTO(UUID servicoUuid, String nome, BigDecimal valor)`.

  5. **`OrdemDeServicoRepositoryPort.java`** in `port/out/`:
     - `OrdemDeServico save(OrdemDeServico)`, `Optional<OrdemDeServico> findById(UUID)`, `List<OrdemDeServico> findAll(int, int, String)`, `long countAll()`

  6. **`OrdemDeServicoCriadaEvent.java`** — record matching UserCreatedEvent pattern
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-domain -am -Dtest="*StatusOS*,*OrdemDeServico*" -q 2>&1 | Select-String -NotMatch "(BUILD SUCCESS|Tests run)"</automated>
  </verify>
  <acceptance_criteria>
    - StatusOS parameterized test covers ALL 49 transitions (8×8 - 8 diagonal = 56 actually, but 8×7=56 minus the ones that aren't in TRANSICOES)
    - Correct transitions: RECEBIDA→EM_DIAGNOSTICO=true, RECEBIDA→FINALIZADA=false
    - Terminal states (ENTREGUE, CANCELADA) reject all transitions
    - OrdemDeServico.create returns OS with RECEBIDA status
    - iniciarDiagnostico() transitions to EM_DIAGNOSTICO
    - iniciarDiagnostico() from CANCELADA throws AppException(400)
    - No setStatus() method on OrdemDeServico
    - All domain tests pass
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create application service + infrastructure layer (entities, repos, mappers, migration, cache)</name>
  <files>
    mekano-application/src/main/java/com/fiap/mekano/application/service/os/OrdemDeServicoService.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/OrdemDeServicoEntity.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/ServicoExecutadoEntity.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/PecaUsadaEntity.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoPanacheRepository.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImpl.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/OrdemDeServicoEntityMapper.java (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/OrdemDeServicoEntityMapperImpl.java (NEW)
    mekano-infrastructure/src/main/resources/db/migration/V10__create_ordens_de_servico_table.sql (NEW)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/cache/CacheNames.java (MODIFY)
    mekano-infrastructure/src/main/resources/cache-config.yml (MODIFY)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-application/src/main/java/com/fiap/mekano/application/service/user/UserService.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapperImpl.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 672-707 for OrdemDeServicoEntity with @Version, lines 749-864 for repository pattern)
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-RESEARCH.md (lines 280-296 for OS entity fields, lines 604-615 for @Version on OS)
  </read_first>
  <behavior>
    - OrdemDeServicoService.execute creates OS with RECEBIDA status
    - OrdemDeServicoService.iniciarDiagnostico transitions to EM_DIAGNOSTICO
    - OrdemDeServicoService.consultarStatus returns public status for any UUID
    - V10 migration creates ordens_de_servico, servicos_executados, pecas_usadas tables
    - OrdemDeServicoEntity has @Version for optimistic locking
    - Child entities (ServicoExecutado, PecaUsada) persist with FK to parent OS
  </behavior>
  <action>
    1. **`OrdemDeServicoService.java`** in `application/service/os/`:
       - `@ApplicationScoped`, implements `OrdemDeServicoServicePort`
       - Constructor injection: `OrdemDeServicoRepositoryPort`, `ClienteRepositoryPort` (verify cliente exists), `VeiculoRepositoryPort` (verify veiculo exists), `EventPublisher`
       - `execute()`: validates cliente/veiculo exist, creates OS via `OrdemDeServico.create()`, saves, publishes event
       - `iniciarDiagnostico(UUID)`: loads OS, calls `os.iniciarDiagnostico()`, saves. Validates current user role at resource level (@RolesAllowed).
       - `consultarStatus(UUID)`: loads OS, returns public status info. NO auth check (used by @PermitAll endpoint).
       - `@Transactional` on `execute()`, `iniciarDiagnostico()`
       - No `setStatus()` anywhere — always call explicit transition methods

    2. **`V10__create_ordens_de_servico_table.sql`**:
       - Three tables: `ordens_de_servico`, `servicos_executados`, `pecas_usadas`
       - `ordens_de_servico`: id (BIGINT GENERATED BY DEFAULT AS IDENTITY), uuid UNIQUE, cliente_uuid, veiculo_uuid, status VARCHAR(30), data_entrada TIMESTAMP, data_saida TIMESTAMP (nullable), observacoes TEXT, version BIGINT DEFAULT 0, audit fields (created_at, updated_at, deleted_at, is_active)
       - `servicos_executados`: id BIGINT GENERATED..., uuid UNIQUE, os_uuid (FK ref), servico_uuid (ref servicos UUID), nome VARCHAR(100), descricao TEXT, valor DECIMAL(10,2), quantidade INT DEFAULT 1, audit fields
       - `pecas_usadas`: id BIGINT GENERATED..., uuid UNIQUE, os_uuid (FK ref), peca_nome VARCHAR(100), quantidade INT, valor_unitario DECIMAL(10,2), audit fields
       - Use separate ALTER TABLE for multiple ADD COLUMN (H2 compat)

    3. **`OrdemDeServicoEntity.java`** — `@Entity @Table(name = "ordens_de_servico")`, extends BaseEntity:
       - Plus: UUID uuid, UUID clienteUuid, UUID veiculoUuid, String status, LocalDateTime dataEntrada, LocalDateTime dataSaida, String observacoes
       - `@Version Long version` — optimistic locking per D-25/Pitfall 1
       - Status stored as String (StatusOS.name())

    4. **`ServicoExecutadoEntity.java`** — `@Entity @Table(name = "servicos_executados")`, extends BaseEntity:
       - UUID uuid, UUID osUuid, UUID servicoUuid, String nome, String descricao, BigDecimal valor, Integer quantidade
       - Logical FK to ordens_de_servico.uuid (no DB constraint per existing pattern)

    5. **`PecaUsadaEntity.java`** — `@Entity @Table(name = "pecas_usadas")`, extends BaseEntity:
       - UUID uuid, UUID osUuid, String pecaNome, Integer quantidade, BigDecimal valorUnitario
       - Logical FK to ordens_de_servico.uuid

    6. **`OrdemDeServicoPanacheRepository.java`** — PanacheRepositoryBase<OrdemDeServicoEntity, Long>

    7. **`OrdemDeServicoRepositoryImpl.java`** — standard pattern:
       - Save handles OS entity + child entities (ServicoExecutado, PecaUsada)
       - `@Version` optimistic locking catches concurrent modification (OptimisticLockException)
       - Find queries filter `isActive = true`
       - `@CacheResult/@CacheInvalidate` on reads/writes
       - `@Retry(maxRetries=3)` on reads, `@Timeout(5s)` on save
       - HQL for findByUuid

    8. **`OrdemDeServicoEntityMapper.java`** + **`OrdemDeServicoEntityMapperImpl.java`**:
       - Maps StatusOS enum ↔ String via `status.name()` and `StatusOS.valueOf()`
       - Maps child entities
       - Follows UserEntityMapperImpl pattern

    9. **`CacheNames.java`** — add constant: `ORDENS_DE_SERVICO = "ordens-de-servico"`
    10. **`cache-config.yml`** — add `ordens-de-servico` cache entry
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-infrastructure -am -Dtest="*OrdemDeServico*" -q 2>&1 | Select-String -NotMatch "(BUILD SUCCESS|Tests run)"</automated>
  </verify>
  <acceptance_criteria>
    - OrdemDeServicoService.execute() creates OS with RECEBIDA status linking to valid cliente+veiculo
    - iniciarDiagnostico() transitions OS to EM_DIAGNOSTICO
    - consultarStatus() returns valid OS public data for existing UUID
    - V10 migration creates all 3 tables
    - OrdemDeServicoEntity has @Version field
    - Infrastructure tests pass with DevServices
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create REST resource — OrdemDeServicoResource with mixed roles + @PermitAll public status</name>
  <files>
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreateOrdemDeServicoRequest.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/OrdemDeServicoResponse.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/OrdemDeServicoStatusResponse.java (NEW)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/OrdemDeServicoDtoMapper.java (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/UserResource.java
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 1125-1155 for OrdemDeServicoResource with mixed roles pattern, lines 1220-1240 for DTOs)
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-RESEARCH.md (lines 684-712 for @PermitAll public status endpoint)
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-CONTEXT.md (D-14, D-15, D-16 for role mappings)
  </read_first>
  <behavior>
    - POST /api/v1/os with valid data returns 201 (@RolesAllowed admin, atendente)
    - POST /api/v1/os/{uuid}/iniciar-diagnostico transitions status (@RolesAllowed mecanico, admin)
    - GET /api/v1/os/{uuid}/status returns 200 WITHOUT authentication (@PermitAll)
    - GET /api/v1/os/{uuid}/status returns limited fields (status, dataEntrada only)
    - GET /api/v1/os returns paginated list (@RolesAllowed all roles)
    - Unauthenticated request to /api/v1/os (non-status) returns 401
  </action>
  <action>
    1. **`OrdemDeServicoResource.java`** — mixed role endpoints per D-14/D-15/D-16:
       - `@Path("/os")`, `@RequestScoped`, NO class-level @RolesAllowed (because of @PermitAll endpoint)
       - Inject `OrdemDeServicoServicePort`, `OrdemDeServicoDtoMapper`
       - Endpoints:
         - `POST /` — Create OS: `@RolesAllowed({"admin", "atendente"})` per D-14, @Valid CreateOrdemDeServicoRequest, returns 201
         - `GET /` — List OS: `@RolesAllowed({"admin", "atendente", "mecanico", "almoxarife", "financeiro"})` per D-14, paginated
         - `GET /{uuid}/status` — Public status: `@PermitAll` per D-16/AUTH-03, returns OrdemDeServicoStatusResponse (uuid, status, dataEntrada only)
         - `PUT /{uuid}/iniciar-diagnostico` — Start diagnosis: `@RolesAllowed({"mecanico", "admin"})` per D-15, returns 200
       - @Tag, @Operation, @APIResponse annotations on each method

    2. **`CreateOrdemDeServicoRequest.java`** — @NotBlank UUID clienteUuid, UUID veiculoUuid, String observacoes, @NotEmpty List<ServicoSolicitadoRequest> servicos. Inner class ServicoSolicitadoRequest: @NotNull UUID servicoUuid, @NotBlank String nome, @NotNull BigDecimal valor.

    3. **`OrdemDeServicoResponse.java`** — record: UUID id, UUID clienteUuid, UUID veiculoUuid, String status, LocalDateTime dataEntrada, LocalDateTime dataSaida, String observacoes, List<ServicoSolicitadoResponse> servicosSolicitados

    4. **`OrdemDeServicoStatusResponse.java`** — record (PUBLIC — minimal): UUID id, String status, LocalDateTime dataEntrada. NO cliente/veiculo details.

    5. **`OrdemDeServicoDtoMapper.java`** — `@Mapper(componentModel = "cdi")` with toCommand(request), toResponse(os), toStatusResponse(os)
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-rest -am -Dtest="*OrdemDeServicoResource*" -q 2>&1 | Select-String -NotMatch "(BUILD SUCCESS|Tests run)"</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/v1/os with valid data returns 201
    - POST /api/v1/os/{uuid}/iniciar-diagnostico returns 200, OS now EM_DIAGNOSTICO
    - GET /api/v1/os/{uuid}/status returns 200 WITHOUT Authorization header (@PermitAll)
    - GET /api/v1/os/{uuid}/status returns only uuid, status, dataEntrada (no cliente/veiculo data)
    - GET /api/v1/os returns 200 paginated list
    - GET /api/v1/os (without auth) returns 401 (not the status endpoint)
    - All REST tests pass
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### New Domain Classes
- `StatusOS` — enum with transition matrix (8 states, Map<StatusOS, Set<StatusOS>>)
- `OrdemDeServico` — entity with create()/reconstitute(), explicit transition methods (no setStatus()), inner ServicoSolicitado
- `OrdemDeServicoServicePort` — input port with create, iniciarDiagnostico, consultarStatus
- `CreateOrdemDeServicoCommand` — command record with ServicoSolicitadoDTO inner record
- `OrdemDeServicoRepositoryPort` — output port interface
- `OrdemDeServicoCriadaEvent` — domain event record

### New Application Classes
- `OrdemDeServicoService` — @ApplicationScoped, @Transactional on state transitions

### New Infrastructure Entities
- `OrdemDeServicoEntity` — JPA entity extending BaseEntity, @Version for optimistic locking
- `ServicoExecutadoEntity` — JPA child entity (servicos_executados table)
- `PecaUsadaEntity` — JPA child entity (pecas_usadas table)

### New Infrastructure Repositories
- `OrdemDeServicoPanacheRepository` — PanacheRepositoryBase
- `OrdemDeServicoRepositoryImpl` — implements OrdemDeServicoRepositoryPort

### New Infrastructure Mappers
- `OrdemDeServicoEntityMapper` + `OrdemDeServicoEntityMapperImpl`

### New REST Classes
- `OrdemDeServicoResource` — @Path("/os"), mixed roles + @PermitAll on /{uuid}/status
- `CreateOrdemDeServicoRequest` — input DTO with nested ServicoSolicitadoRequest
- `OrdemDeServicoResponse` — output record with full details
- `OrdemDeServicoStatusResponse` — PUBLIC output record (minimal fields)
- `OrdemDeServicoDtoMapper` — MapStruct componentModel="cdi"

### New/Dependency Migrations
- `V10__create_ordens_de_servico_table.sql` — ordens_de_servico + child tables

### Modified Files
- `CacheNames.java` — added ORDENS_DE_SERVICO constant
- `cache-config.yml` — added ordens-de-servico cache

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→OrdemDeServicoResource | Untrusted OS creation/transition input |
| anonymous→PublicStatus | Unauthenticated request (must be read-only, minimal data) |
| Concurrent→OS transition | Two requests may attempt conflicting transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Tampering | OS state machine | mitigate | Explicit transition methods (no setStatus()); `@Version` optimistic locking (Pitfall 1); transition matrix as single source of truth |
| T-01-18 | Information Disclosure | Public status leaks internal data | mitigate | `OrdemDeServicoStatusResponse` contains only uuid, status, dataEntrada — no cliente/veiculo/servico details |
| T-01-19 | Spoofing | Unauthorized OS creation | mitigate | `@RolesAllowed({"admin", "atendente"})` on POST /os |
| T-01-20 | Spoofing | Unauthorized diagnosis start | mitigate | `@RolesAllowed({"mecanico", "admin"})` on iniciar-diagnostico |
| T-01-21 | Tampering | Concurrent OS writes (lost update) | mitigate | `@Version Long version` on OrdemDeServicoEntity produces OptimisticLockException on conflict |
| T-01-22 | DoS | Anonymous status query abuse | mitigate | Existing rate limiter; `@PermitAll` but no heavy computation in path |
</threat_model>

<verification>
1. `./mvnw test -pl mekano-domain -am -Dtest="*StatusOS*,*OrdemDeServico*"` — domain tests (state machine parameterized)
2. `./mvnw test -pl mekano-application -am -Dtest="*OrdemDeServicoService*"` — application tests
3. `./mvnw test -pl mekano-infrastructure -am -Dtest="*OrdemDeServico*"` — infrastructure tests
4. `./mvnw test -pl mekano-rest -am -Dtest="*OrdemDeServicoResource*"` — REST tests
5. `./mvnw verify -pl mekano-rest -am` — full suite
</verification>

<success_criteria>
- [ ] StatusOS transition matrix with parameterized test covering all 49 transitions
- [ ] OrdemDeServico explicit transition methods (no setStatus())
- [ ] OS created with RECEBIDA status, transitions to EM_DIAGNOSTICO
- [ ] Invalid transitions rejected with AppException(400)
- [ ] @Version optimistic locking on OS entity
- [ ] V10 migration creates all 3 tables (ordens_de_servico, servicos_executados, pecas_usadas)
- [ ] Public status endpoint works without authentication, returns minimal fields
- [ ] Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/01-auth-os-foundation/01-05-SUMMARY.md` when done.
</output>
