---
phase: 01-auth-os-foundation
plan: 06
type: execute
wave: 4
depends_on: [01-05]
files_modified:
  - docs/sequence-diagrams/criar-os.md (NEW)
  - docs/sequence-diagrams/iniciar-diagnostico.md (NEW)
  - docs/sequence-diagrams/consulta-publica-status.md (NEW)
  - docs/sequence-diagrams/fluxo-completo-os-lifecycle.md (NEW)
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "Sequence diagrams document the main OS flows"
    - "Diagrams show actor interactions with each layer (resource → service → domain → infrastructure → DB)"
    - "Diagrams are in Mermaid format (renders in GitHub/Markdown viewers)"
  artifacts:
    - path: "docs/sequence-diagrams/criar-os.md"
      provides: "Criar OS (creation) sequence diagram"
      contains: "sequenceDiagram"
    - path: "docs/sequence-diagrams/iniciar-diagnostico.md"
      provides: "Iniciar diagnóstico sequence diagram"
      contains: "sequenceDiagram"
    - path: "docs/sequence-diagrams/consulta-publica-status.md"
      provides: "Public OS status query sequence diagram"
      contains: "sequenceDiagram"
---

<objective>
Create Mermaid sequence diagrams documenting the main OS lifecycle flows for Phase 1, following Clean Architecture layer boundaries.

Purpose: Visual documentation of key user interactions — OS creation, diagnosis start, and public status query. These diagrams serve as developer reference for understanding how requests flow through the Clean Architecture stack.

Output: Mermaid `.md` files in `docs/sequence-diagrams/` covering all Phase 1 OS flows.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (DOC-01: diagramas de sequência dos fluxos principais)
@.planning/ROADMAP.md (Phase 1 success criteria point 5)

# Implementation artifacts — reflect actual architecture
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-application/src/main/java/com/fiap/mekano/application/service/os/OrdemDeServicoService.java
@C:/Users/victo/Desktop/fiap-software-architecture/mekano/mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImpl.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sequence diagram — Criar OS (OS creation flow)</name>
  <files>
    docs/sequence-diagrams/criar-os.md (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-PATTERNS.md (lines 1125-1155 for resource endpoint pattern)
  </read_first>
  <action>
    Create `docs/sequence-diagrams/criar-os.md` with Mermaid sequence diagram for:
    Atendente → POST /api/v1/os → OrdemDeServicoResource → OrdemDeServicoService → validates Cliente + Veiculo exist → OrdemDeServico.create() (RECEBIDA) → OrdemDeServicoRepository.save() → DB INSERT → publish OrdemDeServicoCriadaEvent → return 201 with Location header

    Actors: Atendente, API Gateway (auth filter)
    Layers: Resource (JAX-RS), Service (Application), Domain (model), Infrastructure (repository/JPA), Database (PostgreSQL)

    Format: Standard Mermaid `sequenceDiagram` with `participant` and `->>` arrows. Include `note over` for validation steps. Include status code annotations (201, 400, 404, 409).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('docs/sequence-diagrams/criar-os.md','utf8'); process.exit(c.includes('sequenceDiagram')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/sequence-diagrams/criar-os.md`
    - Contains `sequenceDiagram` keyword (valid Mermaid)
    - Shows Atendente → Resource → Service → Domain → Repository → DB flow
    - Shows 201 response with Location header
    - Shows validation steps (check cliente exists, check veiculo exists)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create sequence diagram — Iniciar Diagnóstico (diagnosis start)</name>
  <files>
    docs/sequence-diagrams/iniciar-diagnostico.md (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-RESEARCH.md (lines 424-493 for state machine transitions)
  </read_first>
  <action>
    Create `docs/sequence-diagrams/iniciar-diagnostico.md` with Mermaid sequence diagram for:
    Mecânico → PUT /api/v1/os/{uuid}/iniciar-diagnostico → OrdemDeServicoResource (@RolesAllowed mecanico/admin) → OrdemDeServicoService → loads OS → calls os.iniciarDiagnostico() → StatusOS.podeTransitarPara(EM_DIAGNOSTICO) = true → saves with @Version → DB UPDATE → return 200

    Show the transition matrix validation step explicitly. Show the CANCELADA → EM_DIAGNOSTICO invalid transition returning 400.

    Also show the alt/else branch for the transition matrix check:
    - Valid transition → update status → save → return 200
    - Invalid transition → throw AppException(400) → ApiExceptionMapper → 400 Problem Details
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('docs/sequence-diagrams/iniciar-diagnostico.md','utf8'); process.exit(c.includes('sequenceDiagram')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/sequence-diagrams/iniciar-diagnostico.md`
    - Contains `sequenceDiagram`
    - Shows Transition Matrix validation
    - Shows `@Version` optimistic locking on save
    - Shows both valid (200) and invalid (400) transition outcomes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create sequence diagrams — Consulta Pública + Fluxo Completo OS Lifecycle</name>
  <files>
    docs/sequence-diagrams/consulta-publica-status.md (NEW)
    docs/sequence-diagrams/fluxo-completo-os-lifecycle.md (NEW)
  </files>
  <read_first>
    @C:/Users/victo/Desktop/fiap-software-architecture/mekano/.planning/phases/01-auth-os-foundation/01-RESEARCH.md (lines 684-712 for @PermitAll public status endpoint)
  </read_first>
  <action>
    1. `docs/sequence-diagrams/consulta-publica-status.md`:
       Cliente (unauthenticated) → GET /api/v1/os/{uuid}/status → OrdemDeServicoResource (@PermitAll) → OrdemDeServicoService.consultarStatus() → loads OS → returns OrdemDeServicoStatusResponse (uuid, status, dataEntrada only) → 200

       Show that no JWT filter checks the request (skips auth). Show the response only contains 3 fields.

    2. `docs/sequence-diagrams/fluxo-completo-os-lifecycle.md`:
       End-to-end lifecycle: RECEBIDA → EM_DIAGNOSTICO → AGUARDANDO_APROVACAO → APROVADA → EM_EXECUCAO → FINALIZADA → ENTREGUE

       Show each transition as a `State diagram` in Mermaid (`stateDiagram-v2`), NOT a sequence diagram. Use `note` annotations for which role triggers each transition. This is the state machine documentation reference.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c1=fs.readFileSync('docs/sequence-diagrams/consulta-publica-status.md','utf8'); const c2=fs.readFileSync('docs/sequence-diagrams/fluxo-completo-os-lifecycle.md','utf8'); process.exit(c1.includes('sequenceDiagram')&&c2.includes('stateDiagram')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - consulta-publica-status.md shows @PermitAll filter skip and minimal response
    - fluxo-completo-os-lifecycle.md uses stateDiagram-v2 (not sequence) for the full state machine
    - Each transition annotated with the role that triggers it
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### New Documentation Files
- `docs/sequence-diagrams/criar-os.md` — Mermaid sequence diagram for OS creation
- `docs/sequence-diagrams/iniciar-diagnostico.md` — Mermaid sequence diagram for diagnosis start
- `docs/sequence-diagrams/consulta-publica-status.md` — Mermaid sequence diagram for public status query
- `docs/sequence-diagrams/fluxo-completo-os-lifecycle.md` — Mermaid state diagram for full OS lifecycle with role annotations

<threat_model>
No threats — documentation only.
</threat_model>

<verification>
- `node -e "const fs=require('fs'); ['criar-os','iniciar-diagnostico','consulta-publica-status'].forEach(f=>{const c=fs.readFileSync('docs/sequence-diagrams/'+f+'.md','utf8'); console.assert(c.includes('sequenceDiagram'),f+' missing sequenceDiagram')})"`
- Visual verification: open each `.md` file and confirm Mermaid renders correctly
</verification>

<success_criteria>
- [ ] 4 diagram files created in `docs/sequence-diagrams/`
- [ ] Each file is valid Mermaid format
- [ ] Diagrams reflect actual implementation architecture (Clean Architecture layers)
- [ ] OS lifecycle state diagram covers all 8 states with role annotations
</success_criteria>

<output>
Create `.planning/phases/01-auth-os-foundation/01-06-SUMMARY.md` when done.
</output>
