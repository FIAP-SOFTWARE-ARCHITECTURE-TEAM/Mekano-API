---
phase: 03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-
plan: 05
type: execute
wave: 3
depends_on: [03.1-04]
files_modified:
  - mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java
  - mekano-application/src/main/java/com/fiap/mekano/application/service/orcamento/OrcamentoService.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/listener/OSEntregueListener.java
  - mekano-domain/src/main/java/com/fiap/mekano/domain/model/OrdemDeServico.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/job/SlaExpiryJob.java
  - mekano-application/src/test/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoServiceTest.java
  - mekano-application/src/test/java/com/fiap/mekano/application/service/orcamento/OrcamentoServiceTest.java
  - mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/listener/OSEntregueListenerTest.java
  - mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/job/SlaExpiryJobTest.java
autonomous: true
requirements: [OS-12]
user_setup: []

must_haves:
  truths:
    - "Toda transição de OS (criar, diagnosticar, orçar, aprovar, executar, finalizar, entregar, cancelar) grava linha em os_audit_logs"
    - "OSEntregueListener observa EntregaConfirmadaEvent (evento realmente publicado) e grava ENTREGA_REALIZADA"
    - "SlaExpiryJob cancela a OS por SLA quando o orçamento expira (motivo 'SLA expirado') e audita CANCELAR"
  artifacts:
    - path: "mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java"
      provides: "publicação de OsTransitionedEvent nas transições"
      contains: "OsAuditEventPublisher"
    - path: "mekano-application/src/main/java/com/fiap/mekano/application/service/orcamento/OrcamentoService.java"
      provides: "auditoria APROVAR/CANCELAR (reprovar)"
      contains: "OsAuditAction.APROVAR"
    - path: "mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/job/SlaExpiryJob.java"
      provides: "cancelamento automático por SLA + auditoria"
      contains: "cancelarPorSLA"
  key_links:
    - from: "OrdemDeServicoService (cada transição)"
      to: "OsAuditEventPublisher.publish → OsAuditLogObserver → os_audit_logs"
      via: "OsTransitionedEvent (AFTER_SUCCESS)"
      pattern: "osAuditEventPublisher"
    - from: "SlaExpiryJob.expirarOrcamentosVencidos"
      to: "OrdemDeServico.cancelarPorSLA + OsAuditEventPublisher"
      via: "ordemDeServicoRepository.findById(orcamento.getOrdemServicoUuid())"
      pattern: "cancelarPorSLA"
---

<objective>
Ativar a trilha de auditoria de OS (D-11, D-12) e o cancelamento automático por SLA (OS-12, D-13).

Purpose: A auditoria v1.0 mostra `OsAuditEventPublisher` nunca invocado no fluxo principal — o audit trail só recebe PAGAMENTO_CONFIRMADO (via listener). Além disso, `OSEntregueListener` observa `OSEntregueEvent` que NUNCA é publicado (a OS publica `EntregaConfirmadaEvent`) — entrega não é auditada. E `SlaExpiryJob` expira orçamentos mas NÃO cancela a OS (OS-12 unsatisfied). Decisões: D-11 (auditar todas as transições), D-12 (ENTREGA_REALIZADA via listener do evento real), D-13 (job cancela OS com motivo "SLA expirado").
Output: Auditoria completa por transição; job de SLA cancela OS e audita; testes de wiring.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-/03.1-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java
@mekano-application/src/main/java/com/fiap/mekano/application/service/os/OsAuditEventPublisher.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/os/OsAuditAction.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/audit/OsAuditLogObserver.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/listener/OSEntregueListener.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/event/EntregaConfirmadaEvent.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/job/SlaExpiryJob.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/model/OrdemDeServico.java
@mekano-application/src/main/java/com/fiap/mekano/application/service/orcamento/OrcamentoService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-11/D-12 — auditar todas as transições da OS (OrdemDeServicoService + OrcamentoService + OSEntregueListener)</name>
  <files>
    mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java
    mekano-application/src/main/java/com/fiap/mekano/application/service/orcamento/OrcamentoService.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/listener/OSEntregueListener.java
    mekano-application/src/test/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoServiceTest.java
    mekano-application/src/test/java/com/fiap/mekano/application/service/orcamento/OrcamentoServiceTest.java
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/listener/OSEntregueListenerTest.java
  </files>

  <read_first>
    mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java (transições: create, iniciarDiagnostico, finalizarDiagnostico, iniciarExecucao, finalizarExecucao, cancelar, entregar)
    mekano-application/src/main/java/com/fiap/mekano/application/service/os/OsAuditEventPublisher.java (publish(osUuid, acao, usuarioEmail, observacao, metadata))
    mekano-domain/src/main/java/com/fiap/mekano/domain/os/OsAuditAction.java (CRIAR, DIAGNOSTICAR, ORCAR, APROVAR, EXECUTAR, FINALIZAR, ENTREGAR, CANCELAR, PAGAMENTO_CONFIRMADO, ENTREGA_REALIZADA)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/audit/OsAuditLogObserver.java (observa OsTransitionedEvent AFTER_SUCCESS)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/listener/OSEntregueListener.java (observa OSEntregueEvent — EVENTO ERRADO; a OS publica EntregaConfirmadaEvent)
    mekano-domain/src/main/java/com/fiap/mekano/domain/event/EntregaConfirmadaEvent.java (campos: osUuid, recebidoPor, ... — ler para mapear observacao/dataEntrega)
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/listener/OSEntregueListenerTest.java
  </read_first>

  <action>
    1. OrdemDeServicoService: injetar `OsAuditEventPublisher osAuditEventPublisher` no construtor.
       Publicar (após cada save/@Transactional com sucesso — o observer escreve AFTER_SUCCESS):
       - `create` → publish(os.getId(), OsAuditAction.CRIAR, null, OsAuditAction.CRIAR.getObservacaoDefault(), Map.of())
       - `iniciarDiagnostico` → DIAGNOSTICAR
       - `finalizarDiagnostico` → ORCAR (metadata: Map.of("itens", itens.size()))
       - `iniciarExecucao` → EXECUTAR (metadata: Map.of("mecanico", mecanicoUuid.toString()))
       - `finalizarExecucao` → FINALIZAR
       - `cancelar` → CANCELAR (observacao = motivo fornecido)
       - `entregar` → ENTREGAR (observacao = recebidoPor)
       usuarioEmail = null em todos (services não têm identidade — coluna nullable; o job de SLA usa "sistema").
    2. OrcamentoService: injetar `OsAuditEventPublisher`:
       - `aprovar` → publish(orcamento.getOrdemServicoUuid(), OsAuditAction.APROVAR, null, obsDefault, Map.of())
         (publicar quando getOrdemServicoUuid() != null);
       - `reprovar` → publish(osUuid, OsAuditAction.CANCELAR, null, "Orçamento reprovado pelo cliente", Map.of())
         (não existe ação REPROVAR — CANCELAR cobre a transição p/ CANCELADA).
    3. OSEntregueListener (D-12): trocar o evento observado de `OSEntregueEvent` para
       `EntregaConfirmadaEvent` (o evento REALMENTE publicado por OrdemDeServicoService.entregar).
       - Assinatura: `void onEntregaConfirmada(@Observes(during = TransactionPhase.AFTER_SUCCESS) EntregaConfirmadaEvent event)`
       - Escrever `OsAuditAction.ENTREGA_REALIZADA` com osUuid = event.osUuid(), usuarioEmail "sistema",
         observacao = event.recebidoPor() (ou "Entrega realizada"), metadata com dataEntrega
         (mapear o campo de data do record — ler o arquivo).
       - OsAuditLogObserver já grava CRIAR/DIAGNOSTICAR/... via OsTransitionedEvent; ENTREGA_REALIZADA
         fica por conta deste listener (padrão do PAGAMENTO_CONFIRMADO via PagamentoConfirmadoListener).
    4. Testes:
       - OrdemDeServicoServiceTest (estender): para CADA transição, verify(osAuditEventPublisher).publish(
         any(UUID), eq(acao), isNull(), any(), any()) — mock do publisher;
       - OrcamentoServiceTest: aprovar → verify publish com OsAuditAction.APROVAR; reprovar → CANCELAR;
       - OSEntregueListenerTest (ATUALIZAR): construir EntregaConfirmadaEvent (não OSEntregueEvent) →
         verify(auditRepository).save com ENTREGA_REALIZADA e osUuid correto.
  </action>

  <acceptance_criteria>
    - grep em OrdemDeServicoService: `osAuditEventPublisher` usado nas 7 transições (7 publicações)
    - grep em OrcamentoService: `OsAuditAction.APROVAR` e `CANCELAR`
    - grep em OSEntregueListener: `EntregaConfirmadaEvent` (sem `OSEntregueEvent`)
    - `./mvnw test -pl mekano-application -am -Dtest="OrdemDeServicoServiceTest" && ./mvnw test -pl mekano-application -am -Dtest="OrcamentoServiceTest" && ./mvnw test -pl mekano-infrastructure -am -Dtest="OSEntregueListenerTest"` → BUILD SUCCESS
  </acceptance_criteria>

  <verify>
    <automated>./mvnw test -pl mekano-application -am && ./mvnw test -pl mekano-infrastructure -am -Dtest="OSEntregueListenerTest"</automated>
  </verify>

  <done>Todas as transições da OS gravam auditoria; entrega auditada via evento real; testes de wiring verdes.</done>
</task>

<task type="auto">
  <name>Task 2: OS-12/D-13 — SlaExpiryJob cancela a OS expirada e audita</name>
  <files>
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/job/SlaExpiryJob.java
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/OrdemDeServico.java
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/job/SlaExpiryJobTest.java
  </files>

  <read_first>
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/job/SlaExpiryJob.java (hoje: só orcamento.expirar() + save — NÃO cancela a OS)
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/OrdemDeServico.java (cancelarPorSLA() linha 272 — verificar motivo gravado; getStatus/StatusOS)
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/Orcamento.java (getOrdemServicoUuid(); getItens() com ItemOrcamento.getPecaId() — preenchido no plano 03.1-02)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/OrdemDeServicoRepositoryPort.java (findById(UUID))
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/PecaRepositoryPort.java (liberarReserva(UUID, Integer) — adicionado no plano 03.1-02)
    mekano-application/src/main/java/com/fiap/mekano/application/service/os/OsAuditEventPublisher.java
    mekano-domain/src/test/java/com/fiap/mekano/domain/model/OrdemDeServicoTest.java (teste existente de cancelarPorSLA — linha 156)
  </read_first>

  <action>
    Per D-13 (motivo "SLA expirado"), D-14 (cancelamento por SLA libera reserva de estoque se houver) e OS-12 (sistema cancela OS automaticamente se orçamento expirar por SLA):
    1. OrdemDeServico.cancelarPorSLA(): garantir que grava motivo "SLA expirado" (D-13). Se hoje grava
       outro texto, alterar para "SLA expirado" e ajustar o teste de domínio correspondente (OrdemDeServicoTest).
    2. SlaExpiryJob: trocar injeção de campo por construtor e adicionar dependências:
       `OrdemDeServicoRepositoryPort ordemDeServicoRepository` + `OsAuditEventPublisher osAuditEventPublisher`
       + `PecaRepositoryPort pecaRepository`.
       Em `expirarOrcamentosVencidos`, para cada orcamento expirado:
       - `orcamento.expirar(); orcamentoRepository.save(orcamento);` (existente)
       - `orcamento.getOrdemServicoUuid() != null` → `ordemDeServicoRepository.findById(osUuid)`:
         se presente e `status == StatusOS.AGUARDANDO_APROVACAO` → `os.cancelarPorSLA();`
         `ordemDeServicoRepository.save(os);`
         `osAuditEventPublisher.publish(osUuid, OsAuditAction.CANCELAR, "sistema",
           "Cancelamento automático por SLA (72h)", Map.of())`.
       - Liberar reserva de estoque (D-14): para cada item do orçamento com `getPecaId() != null` →
         `pecaRepository.liberarReserva(item.getPecaId(), item.getQuantidade().intValue())`
         (mesma semântica do cancelar do OrdemDeServicoService no plano 03.1-04 — libera reserva,
         NÃO credita saldo físico).
       - Cuidado com transação: método continua @Transactional — tudo na mesma transação (rollback total
         se algo falhar).
    3. SlaExpiryJobTest (novo, infra — padrão OsAuditLogObserverTest/@QuarkusTest ou Mockito simples:
       seguir o padrão dos testes de infra; se usar Mockito puro, converter job p/ construtor já feito):
       - orçamento expirado com OS AGUARDANDO_APROVACAO → verify(os).cancelarPorSLA chamado +
         verify(osAuditEventPublisher).publish com CANCELAR e usuarioEmail "sistema";
       - orçamento expirado com OS em outro status (ex: EM_EXECUCAO) → NÃO cancela;
       - orçamento sem ordemServicoUuid → nenhuma chamada a ordemDeServicoRepository;
       - OS cancelada por SLA com itens de peça → verify(pecaRepository).liberarReserva por item.
  </action>

  <acceptance_criteria>
    - grep em SlaExpiryJob: `cancelarPorSLA` + `OsAuditEventPublisher` + `liberarReserva`
    - grep em OrdemDeServico.cancelarPorSLA: motivo "SLA expirado" (ou teste de domínio cobrindo)
    - `./mvnw test -pl mekano-domain && ./mvnw test -pl mekano-infrastructure -am -Dtest="SlaExpiryJobTest"` → BUILD SUCCESS
  </acceptance_criteria>

  <verify>
    <automated>./mvnw test -pl mekano-domain && ./mvnw test -pl mekano-infrastructure -am -Dtest="SlaExpiryJobTest"</automated>
  </verify>

  <done>Orçamento vencido por SLA cancela a OS automaticamente (motivo "SLA expirado") e grava auditoria CANCELAR com usuário "sistema".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services → OsAuditEventPublisher → OsAuditLogObserver | trilha de auditoria (escrita em REQUIRES_NEW) |
| SlaExpiryJob (scheduler) | execução automática com identidade "sistema" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-035-01 | Repudiation | transições da OS | mitigate | Cada transição grava os_audit_logs via OsTransitionedEvent (AFTER_SUCCESS — só grava se a transação de negócio teve sucesso) |
| T-035-02 | Tampering | OSEntregueListener evento errado | mitigate | Observar EntregaConfirmadaEvent (evento real) — elimina auditoria fantasma de OSEntregueEvent nunca publicado |
| T-035-03 | Spoofing | SlaExpiryJob | mitigate | Identidade fixa "sistema" na auditoria — distingue ação automática de ação de usuário (usuarioEmail null nos services) |
| T-035-04 | Tampering | cancelamento por SLA | mitigate | Guarda de status AGUARDANDO_APROVACAO antes de cancelarPorSLA — não cancela OS em execução/entregue |
</threat_model>

<verification>
- `./mvnw verify -pl mekano-rest -am` verde
- E2E manual (dev): criar OS → diagnosticar → orçar → aprovar → executar → finalizar → pagar → entregar →
  GET /api/v1/os/{uuid}/audit retorna 9+ entradas (CRIAR, DIAGNOSTICAR, ORCAR, APROVAR, EXECUTAR,
  FINALIZAR, PAGAMENTO_CONFIRMADO, ENTREGAR, ENTREGA_REALIZADA)
</verification>

<success_criteria>
- D-11: todas as transições auditadas
- D-12: entrega auditada com evento real
- OS-12/D-13: SLA cancela OS automaticamente com auditoria "sistema"
</success_criteria>

<output>
Create `.planning/phases/03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-/03.1-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Modified classes (main):**
- `OrdemDeServicoService` — injeta OsAuditEventPublisher; 7 publicações de auditoria (CRIAR..CANCELAR/ENTREGAR)
- `OrcamentoService` — auditoria APROVAR (aprovar) e CANCELAR (reprovar)
- `OSEntregueListener` — observa EntregaConfirmadaEvent (evento real) → ENTREGA_REALIZADA
- `OrdemDeServico` — cancelarPorSLA com motivo "SLA expirado" (D-13)
- `SlaExpiryJob` — construtor injection; cancela OS AGUARDANDO_APROVACAO + auditoria CANCELAR "sistema"

**Test files:**
- `OrdemDeServicoServiceTest`, `OrcamentoServiceTest` (verify de publicações), `OSEntregueListenerTest` (evento corrigido), `SlaExpiryJobTest` (novo)
