---
phase: 03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-
plan: 03
type: execute
wave: 2
depends_on: [03.1-02]
files_modified:
  - mekano-application/src/main/java/com/fiap/mekano/application/service/nfentrada/NfEntradaService.java
  - mekano-application/src/test/java/com/fiap/mekano/application/service/nfentrada/NfEntradaServiceTest.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/observer/EstoqueMinimoObserver.java
  - mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/observer/EstoqueMinimoObserverTest.java
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/UpdatePecaCommand.java
  - mekano-application/src/main/java/com/fiap/mekano/application/service/peca/PecaService.java
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/UpdatePecaRequest.java
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/PecaDtoMapper.java
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/PecaResource.java
  - mekano-rest/src/test/java/com/fiap/mekano/rest/api/PecaResourceTest.java
autonomous: true
requirements: [EST-02, EST-07, EST-09]
user_setup: []

must_haves:
  truths:
    - "Credito de saldo via NF de entrada dispara EstoqueMinimoAtingidoEvent quando disponível <= estoque mínimo"
    - "EstoqueMinimoObserver gera requisição com quantidade calculada (estoqueMinimo - disponivel) e não 100 fixo"
    - "PUT /api/v1/pecas/{id} edita codigo/descricao/valorUnitario/estoqueMinimo (saldo preservado)"
    - "DELETE /api/v1/pecas/{id} soft-delete admin"
  artifacts:
    - path: "mekano-application/src/main/java/com/fiap/mekano/application/service/nfentrada/NfEntradaService.java"
      provides: "publicação do evento após crédito"
      contains: "EstoqueMinimoAtingidoEvent"
    - path: "mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/observer/EstoqueMinimoObserver.java"
      provides: "quantidade calculada da reposição"
      contains: "estoqueMinimo()"
    - path: "mekano-application/src/main/java/com/fiap/mekano/application/service/peca/PecaService.java"
      provides: "updatePeca"
      contains: "updatePeca"
    - path: "mekano-rest/src/main/java/com/fiap/mekano/rest/api/PecaResource.java"
      provides: "PUT/DELETE em /pecas/{id}"
      contains: "@PUT"
  key_links:
    - from: "NfEntradaService.registrar"
      to: "EstoqueMinimoAtingidoEvent → EstoqueMinimoObserver"
      via: "eventPublisher.publish depois do creditarSaldo"
      pattern: "eventPublisher.*publish"
    - from: "PecaResource @PUT /{id}"
      to: "PecaService.updatePeca"
      via: "Peca.reconstitute preservando saldoAtual/saldoReservado"
      pattern: "updatePeca"
---

<objective>
Fechar EST-07 (verificação de estoque mínimo no crédito de saldo) e EST-09 (alerta com quantidade de reposição calculada — D-05), e completar o CRUD de peças (EST-02: PUT/DELETE no PecaResource).

Purpose: A auditoria v1.0 aponta NfEntradaService.registrar sem checagem de mínimo (EST-07 unsatisfied), EstoqueMinimoObserver com qty fixa 100 (EST-09/D-05), e PecaResource sem PUT/DELETE (EST-02 partial). D-05 exige quantidade de reposição calculada a partir do evento; o evento já carrega (pecaId, saldoAtual=disponivel, estoqueMinimo) — o observer deve usar esses campos.
Output: NF de entrada gera evento de mínimo; observer cria requisição com qty = estoqueMinimo - disponivel; PecaResource com PUT/DELETE para admin.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-/03.1-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@mekano-application/src/main/java/com/fiap/mekano/application/service/nfentrada/NfEntradaService.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/observer/EstoqueMinimoObserver.java
@mekano-application/src/main/java/com/fiap/mekano/application/service/peca/PecaService.java
@mekano-rest/src/main/java/com/fiap/mekano/rest/api/PecaResource.java
@mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreatePecaRequest.java
@mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/PecaDtoMapper.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/model/Peca.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/event/EstoqueMinimoAtingidoEvent.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: EST-07 evento de estoque mínimo no crédito de saldo + EST-09/D-05 quantidade calculada</name>
  <files>
    mekano-application/src/main/java/com/fiap/mekano/application/service/nfentrada/NfEntradaService.java
    mekano-application/src/test/java/com/fiap/mekano/application/service/nfentrada/NfEntradaServiceTest.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/observer/EstoqueMinimoObserver.java
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/observer/EstoqueMinimoObserverTest.java
  </files>

  <read_first>
    mekano-application/src/main/java/com/fiap/mekano/application/service/nfentrada/NfEntradaService.java (registrar — credita saldo na linha 48, NÃO injeta EventPublisher)
    mekano-application/src/main/java/com/fiap/mekano/application/service/peca/PecaService.java (debitarSaldo linhas 53-64 — padrão EXATO de publish do evento; PecaService já injeta EventPublisher)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/observer/EstoqueMinimoObserver.java (qty fixa 100 na linha 20)
    mekano-domain/src/main/java/com/fiap/mekano/domain/event/EstoqueMinimoAtingidoEvent.java (record (UUID pecaId, Integer saldoAtual, Integer estoqueMinimo))
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/Peca.java (disponivel() e isEstoqueMinimoAtingido() — implementados no plano 03.1-02)
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/audit/OsAuditLogObserverTest.java (padrão de teste de observer com @InjectMock)
  </read_first>

  <action>
    1. NfEntradaService (EST-07):
       - Adicionar `EventPublisher eventPublisher` ao construtor (mekano-application → mekano-domain port out;
         mesmo tipo já injetado em PecaService/OrdemDeServicoService).
       - Em `registrar`, DEPOIS do `pecaRepository.creditarSaldo(requisicao.getPecaId(), ...)` (linha 48):
         `pecaRepository.buscarPorId(requisicao.getPecaId())` → se `peca.isEstoqueMinimoAtingido()` (usa
         disponivel per D-05) → `eventPublisher.publish(new EstoqueMinimoAtingidoEvent(
         peca.getId(), peca.disponivel().intValue(), peca.getEstoqueMinimo().intValue()))`.
         (Busca extra tolerante: se a peça não existir (null), seguir sem evento — crédito já ocorreu.)
    2. EstoqueMinimoObserver (EST-09, D-05):
       - Substituir qty fixa 100 por cálculo com os campos do evento:
         `int qtd = event.estoqueMinimo() - event.saldoAtual();` (saldoAtual == disponivel per publish)
         `if (qtd <= 0) return;` — só cria requisição quando há reposição de fato.
         `var command = new CreateRequisicaoCompraCommand(event.pecaId(), qtd, MotivoRequisicao.ESTOQUE_MINIMO);`
       - Manter @Transactional e injeção de RequisicaoCompraService (trocar @Inject por construtor para
         testabilidade, seguindo convenção do módulo).
    3. Testes:
       - NfEntradaServiceTest (novo, application, Mockito — padrão UserServiceTest):
         * registrar com peça abaixo do mínimo → verify(eventPublisher).publish(EstoqueMinimoAtingidoEvent)
           com saldoAtual == disponivel e estoqueMinimo correto;
         * registrar com peça acima do mínimo → verifyNever(publish);
         * registrar com peça inexistente → não lança (fluxo de crédito segue normal).
       - EstoqueMinimoObserverTest (novo, infra, @QuarkusTest + @InjectMock de RequisicaoCompraService —
         padrão OsAuditLogObserverTest):
         * evento (pecaId, saldoAtual=2, estoqueMinimo=5) → criar chamado com qtd=3 e motivo ESTOQUE_MINIMO;
         * evento (saldoAtual >= estoqueMinimo) → criar NÃO chamado.
  </action>

  <acceptance_criteria>
    - grep em NfEntradaService: `eventPublisher` + `EstoqueMinimoAtingidoEvent` presentes após o crédito
    - grep em EstoqueMinimoObserver: cálculo usa `event.estoqueMinimo() - event.saldoAtual()` (sem "100")
    - `./mvnw test -pl mekano-application -am -Dtest="NfEntradaServiceTest"` e
      `./mvnw test -pl mekano-infrastructure -am -Dtest="EstoqueMinimoObserverTest"` → BUILD SUCCESS
  </acceptance_criteria>

  <verify>
    <automated>./mvnw test -pl mekano-application -am -Dtest="NfEntradaServiceTest" && ./mvnw test -pl mekano-infrastructure -am -Dtest="EstoqueMinimoObserverTest"</automated>
  </verify>

  <done>Crédito de saldo via NF dispara alerta quando disponível <= mínimo; observer gera requisição com quantidade calculada (não 100 fixo).</done>
</task>

<task type="auto">
  <name>Task 2: EST-02 CRUD completo de peças — UpdatePecaCommand + updatePeca + PUT/DELETE resource</name>
  <files>
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/UpdatePecaCommand.java
    mekano-application/src/main/java/com/fiap/mekano/application/service/peca/PecaService.java
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/UpdatePecaRequest.java
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/PecaDtoMapper.java
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/PecaResource.java
    mekano-rest/src/test/java/com/fiap/mekano/rest/api/PecaResourceTest.java
  </files>

  <read_first>
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/Peca.java (reconstitute 8-arg — plano 03.1-02 adicionou saldoReservado; NÃO há método atualizar — usar reconstitute preservando saldo)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/CreatePecaCommand.java (padrão de command de peça — atualizar NÃO inclui saldo/estoque mínimo? NAO: UpdatePecaCommand INCLUI estoqueMinimo editável, exclui saldo)
    mekano-application/src/main/java/com/fiap/mekano/application/service/peca/PecaService.java (criar/buscarPorId/excluir existentes; excluir com guarda de OS vinculada)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/PecaResource.java (@RolesAllowed("admin") na classe; POST/GETs existentes)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/CreatePecaRequest.java (padrão de validações)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/mapper/PecaDtoMapper.java (toCreateCommand — adicionar toUpdate)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/ServicoResource.java (padrão PUT/DELETE com 200/204 e PathParam)
    mekano-rest/src/test/java/com/fiap/mekano/rest/api/PecaResourceTest.java (padrão E2E com @TestSecurity)
  </read_first>

  <action>
    Per EST-02 (editar, consultar, excluir peças — saldo nunca negativo):
    1. UpdatePecaCommand (domain/port/in, record): `(UUID id, String codigo, String descricao,
       BigDecimal valorUnitario, Long estoqueMinimo)` — SEM saldoAtual/saldoReservado (imutáveis via API).
    2. PecaService.updatePeca(UUID id, UpdatePecaCommand command) @Transactional:
       - `Peca atual = buscarPorId(id);` (404 se ausente)
       - `Peca atualizada = Peca.reconstitute(id, command.codigo(), command.descricao(),
         command.valorUnitario(), atual.getSaldoAtual(), command.estoqueMinimo(),
         atual.getSaldoReservado(), atual.getCreatedAt());` — saldo preservado (nunca negativo pois
         reconstitute valida)
       - `pecaRepository.salvar(atualizada); return atualizada;`
       - Validações de codigo/descricao/valor propagam AppException(400) do reconstitute.
    3. UpdatePecaRequest (rest/api/dto): Lombok @Getter @Setter @NoArgsConstructor com
       `@NotBlank codigo, @NotBlank descricao, @NotNull @DecimalMin("0.00") valorUnitario,
       Long estoqueMinimo` (espelhar CreatePecaRequest).
    4. PecaDtoMapper: `UpdatePecaCommand toUpdateCommand(UUID id, UpdatePecaRequest request);`
       (MapStruct cdi; o mapper resolve o mapeamento por nome).
    5. PecaResource:
       - `@PUT @Path("/{id}")` → `Response atualizar(@PathParam("id") UUID id, @Valid UpdatePecaRequest
         request)` → 200 + PecaResponse (reusar toResponse);
       - `@DELETE @Path("/{id}")` → `Response excluir(@PathParam("id") UUID id)` → `pecaService.excluir(id)` → 204;
       - Anotações OpenAPI @Operation/@APIResponse (padrão do arquivo).
    6. PecaResourceTest — estender (padrão existente, @TestMethodOrder + @TestSecurity("admin")):
       - PUT com novo codigo/descricao/valor/estoqueMinimo → 200 e resposta refletindo mudanças;
       - PUT preserva saldoAtual (criar peça via POST, PUT, verificar saldoAtual inalterado na resposta);
       - PUT sem token → 401; com role atendente → 403 (admin-only herdado);
       - DELETE → 204 e GET /{id} subsequente → 404 (soft delete);
       - DELETE de peça vinculada a OS ativa → 409 (guarda existente do PecaService.excluir).
  </action>

  <acceptance_criteria>
    - grep em PecaService: `updatePeca` presente com reconstitute preservando getSaldoAtual/getSaldoReservado
    - grep em PecaResource: `@PUT` e `@DELETE` em `/{id}`
    - `./mvnw test -pl mekano-rest -am -Dtest="PecaResourceTest"` → BUILD SUCCESS
  </acceptance_criteria>

  <verify>
    <automated>./mvnw test -pl mekano-rest -am -Dtest="PecaResourceTest"</automated>
  </verify>

  <done>Admin edita e exclui peças via REST; saldo nunca editável/negativo; 401/403/404/409 cobertos por teste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → PUT/DELETE /pecas/{id} | mutação de cadastro autenticada |
| NfEntradaService → EstoqueMinimoAtingidoEvent | evento interno de domínio (CDI, mesma JVM) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-033-01 | Tampering | PUT /pecas/{id} | mitigate | UpdatePecaCommand sem saldo/saldoReservado; reconstitute preserva do registro — saldo imutável via API (teste E2E prova saldo preservado) |
| T-033-02 | Spoofing | PUT/DELETE /pecas/{id} | mitigate | @RolesAllowed("admin") na classe — testes 401/403 |
| T-033-03 | Tampering | EstoqueMinimoObserver qty | mitigate | qtd calculada do evento com guarda qtd > 0 — nunca cria requisição de quantidade <= 0 |
| T-033-04 | Denial of Service | evento mínimo em loop (crédito→requisição→NF→crédito) | accept | Requisição criada exige NF de entrada manual para re-creditar; sem auto-loop infinito |
</threat_model>

<verification>
- `./mvnw verify -pl mekano-rest -am` verde (todos os módulos compilam e passam)
- EST-07/EST-09: fluxo NF → evento → observer → RequisiçãoCompra coberto por testes unitários (application + infra)
</verification>

<success_criteria>
- EST-07: crédito de saldo verifica mínimo e dispara evento
- EST-09/D-05: quantidade de reposição calculada (estoqueMinimo - disponivel)
- EST-02: PUT/DELETE funcionais com segurança e persistência
</success_criteria>

<output>
Create `.planning/phases/03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-/03.1-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New files:**
- `mekano-domain/src/main/java/com/fiap/mekano/domain/port/in/UpdatePecaCommand.java` — record (id, codigo, descricao, valorUnitario, estoqueMinimo)
- `mekano-rest/src/main/java/com/fiap/mekano/rest/api/dto/UpdatePecaRequest.java` — payload do PUT
- `mekano-application/src/test/java/com/fiap/mekano/application/service/nfentrada/NfEntradaServiceTest.java`
- `mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/observer/EstoqueMinimoObserverTest.java`

**Modified classes (main):**
- `NfEntradaService` — injeta EventPublisher; publica EstoqueMinimoAtingidoEvent após crédito quando disponível <= mínimo
- `EstoqueMinimoObserver` — qty = estoqueMinimo() - saldoAtual() (guarda > 0), construtor injection
- `PecaService` — novo método `updatePeca(UUID, UpdatePecaCommand)` (reconstitute preservando saldos)
- `PecaDtoMapper` — novo método `toUpdateCommand(UUID, UpdatePecaRequest)`
- `PecaResource` — novos endpoints `PUT /pecas/{id}` (200) e `DELETE /pecas/{id}` (204)
