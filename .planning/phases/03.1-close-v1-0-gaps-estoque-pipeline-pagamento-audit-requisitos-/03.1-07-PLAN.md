---
phase: 03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-
plan: 07
type: execute
wave: 4
depends_on: [03.1-03, 03.1-04, 03.1-05]
files_modified:
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImpl.java
  - mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java
  - mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java
  - mekano-application/src/main/java/com/fiap/mekano/application/service/EntregaService.java
  - mekano-rest/src/test/java/com/fiap/mekano/rest/api/OrdemDeServicoResourceTest.java
  - mekano-rest/src/test/java/com/fiap/mekano/rest/api/NfEntradaResourceTest.java
  - mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImplTest.java
  - mekano-application/src/test/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoServiceTest.java
  - mekano-application/src/test/java/com/fiap/mekano/application/service/EntregaServiceTest.java
autonomous: true
requirements: [OS-07, OS-16, OS-17, OS-18, EST-06]
user_setup: []

must_haves:
  truths:
    - "Create de OS valida existência de clienteId e veiculoId (404/422, não 201 com UUID aleatório)"
    - "findAllWithFilters tem testes de integração cobrindo filtros"
    - "GET /os/{id} detalhamento retorna dados reais (não placeholder hardcoded)"
    - "GET /os/tempo-medio retorna breakdown por tipo de serviço"
    - "NfEntradaResourceTest asserta crédito de saldo de estoque"
    - "EntregaService (dead code) removido ou integrado — sem código não-deployado"
  artifacts:
    - path: "mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java"
      provides: "validação de existência no create"
      contains: "clienteRepository"
    - path: "mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java"
      provides: "detalhamento real + breakdown por serviço"
      contains: "tempoMedioPorTipo"
    - path: "mekano-rest/src/test/java/com/fiap/mekano/rest/api/NfEntradaResourceTest.java"
      provides: "assert de crédito de saldo"
      contains: "creditarSaldo"
  key_links:
    - from: "OrdemDeServicoService.create"
      to: "ClienteRepositoryPort + VeiculoRepositoryPort"
      via: "findById lança AppException 404 se inexistente"
      pattern: "AppException"
    - from: "OrdemDeServicoResource.detalhamento"
      to: "dados reais da OS + itens do orçamento"
      via: "substituição do placeholder hardcoded (linha 253-254)"
      pattern: "emptyList"
---

<objective>
Fechar os 5 gaps residuais "partial" da auditoria v1.0 que os planos 03.1-01 a 05 não cobrem: OS-16 (findAllWithFilters sem testes), OS-17 (detalhamento com placeholder hardcoded em OrdemDeServicoResource.java:253-254), OS-18 (tempo médio sem breakdown por tipo de serviço), OS-07 (create de OS sem validação cliente/veículo), EST-06 (NfEntradaResourceTest sem assert de crédito) + remoção do dead code EntregaService (cross-phase issue #7).

Purpose: Sem este plano, a claim "37/37 Complete" do plano 03.1-06 seria factualmente incorreta (os 5 IDs ficariam sem tasks cobertoras). Decisão do usuário: "Adicionar tasks".
Output: Validações de create, testes de filtro, detalhamento real, breakdown por tipo, assert de crédito e eliminação do dead code EntregaService.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-/03.1-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImpl.java
@mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java
@mekano-application/src/main/java/com/fiap/mekano/application/service/EntregaService.java
@mekano-rest/src/test/java/com/fiap/mekano/rest/api/NfEntradaResourceTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: OS-07 — validação de existência cliente/veículo no create de OS</name>
  <files>
    mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java
    mekano-application/src/test/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoServiceTest.java
  </files>

  <read_first>
    mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java (create — hoje não valida existência; injeta clienteRepository? veiculoRepository?)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/ClienteRepositoryPort.java (findById(UUID) → Optional)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/VeiculoRepositoryPort.java (findById(UUID) → Optional)
    mekano-application/src/test/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoServiceTest.java (padrão Mockito existente — pode ser arquivo novo "criar se não existir")
  </read_first>

  <action>
    Em `OrdemDeServicoService.create(...)`:
    1. Se o serviço não injeta ClienteRepositoryPort/VeiculoRepositoryPort, adicionar ao construtor.
    2. ANTES de montar a OS: validar existência —
       `clienteRepository.findById(command.clienteId()).orElseThrow(() -> new AppException(404, Messages.get("cliente.naoEncontrado", command.clienteId())))`
       e idem para `veiculoRepository.findById(command.veiculoId())` (404 se ausente).
       Usar o mesmo padrão de mensagem/estilo já usado no projeto (verificar Messages e como outros
       services lançam 404 — ex: UserService).
    3. Testes (OrdemDeServicoServiceTest — estender ou criar): 
       - create com cliente inexistente → AppException 404 e save NUNCA chamado;
       - create com veiculo inexistente → AppException 404;
       - create com cliente e veiculo existentes → save chamado (fluxo atual preservado).
  </action>

  <acceptance_criteria>
    - grep em OrdemDeServicoService: `clienteRepository.findById` + `veiculoRepository.findById` no create (com orElseThrow 404)
    - `./mvnw test -pl mekano-application -am -Dtest="OrdemDeServicoServiceTest"` → BUILD SUCCESS
  </acceptance_criteria>

  <verify>
    <automated>./mvnw test -pl mekano-application -am -Dtest="OrdemDeServicoServiceTest"</automated>
  </verify>

  <done>Create de OS com clienteId/veiculoId inexistentes → 404; fluxo válido preservado com testes.</done>
</task>

<task type="auto">
  <name>Task 2: OS-16/OS-18 — testes de findAllWithFilters + breakdown tempo médio por tipo de serviço</name>
  <files>
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImpl.java
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java
    mekano-rest/src/test/java/com/fiap/mekano/rest/api/OrdemDeServicoResourceTest.java
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImplTest.java
  </files>

  <read_first>
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImpl.java (findAllWithFilters linhas 117-148; tempo médio linhas 157-186 — hoje apenas média global)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/OrdemDeServicoRepositoryPort.java (assinatura de findAllWithFilters e tempoMedio)
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java (endpoint de tempo médio — ver assinatura/response record)
    mekano-rest/src/test/java/com/fiap/mekano/rest/api/OrdemDeServicoResourceTest.java (padrão dos testes E2E)
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/OrdemDeServicoRepositoryImplTest.java (padrão de teste de repo com @QuarkusTest/H2 — criar se não existir)
  </read_first>

  <action>
    1. `findAllWithFilters`: NÃO alterar implementação (já existe) — apenas cobrir com testes de integração:
       OrdemDeServicoRepositoryImplTest (novo, infra, @QuarkusTest com H2 — padrão dos testes de repo existentes):
       - sem filtros → todas as OS;
       - por status (ex: EM_EXECUCAO) → apenas as do status;
       - por clienteId/veiculoId → apenas as correspondentes;
       - combinação status + cliente → interseção correta.
       (Conferir os filtros reais da assinatura do port antes de escrever os casos.)
    2. Tempo médio por tipo de serviço (OS-18):
       - Verificar como a query atual calcula a média global (linhas 157-186) e como o endpoint expõe.
       - Estender para agrupar por tipo de serviço: retornar breakdown (tipoServico → tempoMedio) além da
         média global. Manter compatibilidade do contrato existente (adicionar campo/lista, não remover
         o campo atual). Se o endpoint/resource record precisar de novo campo, adicionar no response record
         correspondente (mekano-rest DTOs — lembrar: Output = record).
       - Testes: repository test com 2+ OS de tipos diferentes → asserta o agrupamento correto.
    3. Verificar OrdemDeServicoResourceTest: se o teste de detalhamento/lista ainda não cobre os filtros,
       adicionar um caso E2E de listagem com filtro por status.
  </action>

  <acceptance_criteria>
    - OrdemDeServicoRepositoryImplTest (novo) com casos de findAllWithFilters → `./mvnw test -pl mekano-infrastructure -am -Dtest="OrdemDeServicoRepositoryImplTest"` → BUILD SUCCESS
    - Tempo médio exposto por tipo de serviço (grep em OrdemDeServicoRepositoryImpl: agrupamento por tipo; grep no resource/record do endpoint)
    - `./mvnw test -pl mekano-rest -am -Dtest="OrdemDeServicoResourceTest"` → BUILD SUCCESS
  </acceptance_criteria>

  <verify>
    <automated>./mvnw test -pl mekano-infrastructure -am -Dtest="OrdemDeServicoRepositoryImplTest" && ./mvnw test -pl mekano-rest -am -Dtest="OrdemDeServicoResourceTest"</automated>
  </verify>

  <done>findAllWithFilters com cobertura de testes; tempo médio com breakdown por tipo de serviço; contratos compatíveis.</done>
</task>

<task type="auto">
  <name>Task 3: OS-17 — detalhamento real (remover placeholder hardcoded) + EST-06 assert de crédito + dead code EntregaService</name>
  <files>
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java
    mekano-rest/src/test/java/com/fiap/mekano/rest/api/OrdemDeServicoResourceTest.java
    mekano-rest/src/test/java/com/fiap/mekano/rest/api/NfEntradaResourceTest.java
    mekano-application/src/main/java/com/fiap/mekano/application/service/EntregaService.java
    mekano-application/src/test/java/com/fiap/mekano/application/service/EntregaServiceTest.java
  </files>

  <read_first>
    mekano-rest/src/main/java/com/fiap/mekano/rest/api/OrdemDeServicoResource.java (detalhamento linhas 253-254 — placeholder hardcoded + emptyList(); endpoint tempo médio)
    mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java (busca real da OS + orçamento/itens — usado nos planos 03.1-04/05)
    mekano-rest/src/test/java/com/fiap/mekano/rest/api/OrdemDeServicoResourceTest.java (padrão E2E)
    mekano-rest/src/test/java/com/fiap/mekano/rest/api/NfEntradaResourceTest.java (fluxo de NF — hoje sem assert de saldo)
    mekano-application/src/main/java/com/fiap/mekano/application/service/EntregaService.java (dead code — nunca invocado; guard difere 409 vs 422 real em OrdemDeServicoService.entregar)
    mekano-application/src/test/java/com/fiap/mekano/application/service/EntregaServiceTest.java (teste que valida comportamento não-deployado)
  </read_first>

  <action>
    1. Detalhamento (OS-17): substituir o placeholder hardcoded + `emptyList()` do endpoint de
       detalhamento (OrdemDeServicoResource.java:253-254) por dados reais: buscar a OS via service,
       e os itens do orçamento quando `os.getOrcamentoUuid() != null` (padrão usado em iniciarExecucao
       no plano 03.1-04: `orcamentoRepository.findByUuid` ou exposição via service — seguir a arquitetura
       de camadas: o resource NÃO deve chamar repository diretamente; adicionar método no
       OrdemDeServicoService se necessário).
       - Teste E2E (OrdemDeServicoResourceTest): criar OS + orçamento com itens → GET detalhamento →
         asserta itens reais no body (não placeholder, não lista vazia).
    2. EST-06: NfEntradaResourceTest — após o fluxo de criação de NF existente, adicionar assert de que
       o saldo da peça foi creditado: GET na peça (PecaResource) ou consulta via resposta → asserta
       saldo maior que antes da NF (ex: saldo inicial 0 → após NF com qtd 10 → saldo 10).
       Se NfEntradaResourceTest não existir, criar cobrindo o fluxo e o crédito.
    3. Dead code EntregaService (cross-phase #7): a entrega real está em
       `OrdemDeServicoService.entregar` (guard 422). Remover `EntregaService` (application service)
       e `EntregaServiceTest` — confirmar antes via grep que nenhum outro código referencia
       `EntregaService` (resources/services/observers). Se houver referência, redirecionar para
       `OrdemDeServicoService.entregar` e então remover. Manter o teste de entrega do
       OrdemDeServicoService/Resource (fluxo real continua coberto).
  </action>

  <acceptance_criteria>
    - grep em OrdemDeServicoResource: detalhamento NÃO contém mais o placeholder hardcoded nem `emptyList()` (sem `"placeholder"` e sem `Collections.emptyList()`/`List.of()` no detalhamento)
    - NfEntradaResourceTest com assert de saldo creditado (grep `saldo` no teste)
    - grep em mekano-application/src/main: `EntregaService` ausente (classe removida) e nenhuma referência restante
    - `./mvnw test -pl mekano-rest -am -Dtest="OrdemDeServicoResourceTest,NfEntradaResourceTest"` → BUILD SUCCESS
    - `./mvnw verify -pl mekano-rest -am` → BUILD SUCCESS (suíte completa)
  </acceptance_criteria>

  <verify>
    <automated>./mvnw test -pl mekano-rest -am -Dtest="OrdemDeServicoResourceTest,NfEntradaResourceTest" && ./mvnw verify -pl mekano-rest -am</automated>
  </verify>

  <done>Detalhamento com dados reais; crédito de saldo assertado no teste de NF; EntregaService dead code removido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create OS → validação de existência | cliente/veículo falsificados no payload |
| detalhamento OS → dados do orçamento | placeholder substituído por dados reais |
| tempo médio por tipo | agregação de dados de execução |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-037-01 | Spoofing | create de OS | mitigate | Validação de existência cliente/veículo → 404; impossível criar OS com UUID aleatório |
| T-037-02 | Information Disclosure | detalhamento da OS | mitigate | Endpoint mantém @RolesAllowed (não é público — plano 03.1-01 não o alterou); só remove placeholder |
| T-037-03 | Tampering | tempo médio por tipo | mitigate | Agregação no repositório (leitura pura), sem escrita — sem vetor de mutação |
| T-037-04 | Repudiation | dead code EntregaService | mitigate | Remoção elimina comportamento não-deployado com guard divergente (409 vs 422) — entrega real única em OrdemDeServicoService.entregar |
</threat_model>

<verification>
- `./mvnw verify -pl mekano-rest -am` verde (suíte completa)
- grep confirma remoção do placeholder, presence de testes de filtro/credito
</verification>

<success_criteria>
- OS-07: create valida existência (404)
- OS-16: findAllWithFilters testado
- OS-17: detalhamento real
- OS-18: tempo médio por tipo
- EST-06: crédito assertado
- Cross-phase #7: EntregaService removido
</success_criteria>

<output>
Create `.planning/phases/03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-/03.1-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Modified classes (main):**
- `OrdemDeServicoService.create` — valida existência de clienteId/veiculoId (AppException 404)
- `OrdemDeServicoRepositoryImpl` — tempo médio com breakdown por tipo de serviço (agrupamento)
- `OrdemDeServicoResource` — detalhamento com dados reais (placeholder removido); response record do tempo médio pode ganhar campo de breakdown
- `EntregaService` — REMOVIDO (dead code; entrega real em OrdemDeServicoService.entregar)

**Test files:**
- `OrdemDeServicoRepositoryImplTest` (novo — findAllWithFilters + tempo médio por tipo)
- `OrdemDeServicoServiceTest` (estender — validação 404 no create)
- `NfEntradaResourceTest` (estender ou novo — assert de crédito de saldo)
- `OrdemDeServicoResourceTest` (estender — detalhamento real + filtro na listagem)
- `EntregaServiceTest` — REMOVIDO junto com a classe
