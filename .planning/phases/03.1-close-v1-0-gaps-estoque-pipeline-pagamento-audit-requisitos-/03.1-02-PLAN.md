---
phase: 03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mekano-infrastructure/src/main/resources/db/migration/V34__add_saldo_reservado_to_pecas.sql
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/PecaEntity.java
  - mekano-domain/src/main/java/com/fiap/mekano/domain/model/Peca.java
  - mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/PecaRepositoryPort.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImpl.java
  - mekano-application/src/main/java/com/fiap/mekano/application/service/peca/PecaService.java
  - mekano-domain/src/main/java/com/fiap/mekano/domain/model/ItemOrcamento.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/OrcamentoEntityMapperImpl.java
  - mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java
  - mekano-domain/src/test/java/com/fiap/mekano/domain/model/PecaTest.java
  - mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImplTest.java
  - mekano-application/src/test/java/com/fiap/mekano/application/service/peca/PecaServiceTest.java
  - mekano-application/src/test/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoServiceTest.java
autonomous: true
requirements: [EST-03, EST-08]
user_setup: []

must_haves:
  truths:
    - "Peça possui saldoReservado persistido (coluna saldo_reservado, default 0)"
    - "Reserva é atômica: saldoReservado += qtd somente se (saldo - saldoReservado) >= qtd"
    - "Débito de reserva é atômico: saldo -= qtd e saldoReservado -= qtd somente se saldoReservado >= qtd"
    - "Liberação de reserva é atômica: saldoReservado -= qtd somente se saldoReservado >= qtd"
    - "Estoque mínimo considera saldo disponível (saldo - saldoReservado)"
    - "Itens de orçamento carregam pecaId (serializado no itens_json, retrocompatível)"
  artifacts:
    - path: "mekano-infrastructure/src/main/resources/db/migration/V34__add_saldo_reservado_to_pecas.sql"
      provides: "coluna saldo_reservado na tabela pecas"
      contains: "saldo_reservado"
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/model/Peca.java"
      provides: "campo saldoReservado + disponivel() + min por disponível"
      contains: "saldoReservado"
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/PecaRepositoryPort.java"
      provides: "contratos de reserva/débito/liberação"
      contains: "reservarSaldo"
    - path: "mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImpl.java"
      provides: "SQL atômico de reserva/débito/liberação"
      contains: "saldo_reservado"
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/model/ItemOrcamento.java"
      provides: "pecaId opcional no item"
      contains: "pecaId"
  key_links:
    - from: "PecaRepositoryImpl.reservarSaldo"
      to: "tabela pecas"
      via: "UPDATE nativo com guarda (saldo - saldo_reservado) >= qtd"
      pattern: "saldo_reservado = saldo_reservado \\+"
    - from: "PecaRepositoryImpl.toDomain"
      to: "Peca.reconstitute"
      via: "passagem de saldoReservado"
      pattern: "reconstitute"
---

<objective>
Criar a infraestrutura de reserva de estoque: coluna `saldo_reservado` (V34), campo no domínio `Peca`, operações atômicas no `PecaRepositoryImpl` (reservar, debitar reservado, liberar reserva), estoque mínimo calculado sobre saldo disponível, e `pecaId` nos itens de orçamento (serialização retrocompatível do `itens_json`).

Purpose: Sem esta base, o pipeline EST-03/EST-08 (reserva na aprovação, débito na execução) permanece dead code. Decisões D-04, D-05 e D-07 (base de dados para a requisição por pecaId). A auditoria aponta `PecaOrcamentoObserver.java:17-21` chamando `debitarSaldo` (semântica errada) — este plano habilita a semântica correta.
Output: V34 + Peca.saldoReservado + PecaRepositoryPort/Impl com 3 operações atômicas + ItemOrcamento.pecaId + mapper 4-campos + finalizarDiagnostico propagando pecaId.
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-/03.1-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@mekano-domain/AGENTS.md
@mekano-infrastructure/AGENTS.md
@mekano-domain/src/main/java/com/fiap/mekano/domain/model/Peca.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/PecaEntity.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImpl.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/PecaRepositoryPort.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/model/ItemOrcamento.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/OrcamentoEntityMapperImpl.java
@mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java
@mekano-infrastructure/src/main/resources/db/migration/V9__create_pecas_table.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coluna saldoReservado + domínio + port + repo atômico (D-04, D-05)</name>
  <files>
    mekano-infrastructure/src/main/resources/db/migration/V34__add_saldo_reservado_to_pecas.sql
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/PecaEntity.java
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/Peca.java
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/PecaRepositoryPort.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImpl.java
    mekano-domain/src/test/java/com/fiap/mekano/domain/model/PecaTest.java
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImplTest.java
  </files>

  <read_first>
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/Peca.java (campos final, create/reconstitute, isEstoqueMinimoAtingido)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/PecaEntity.java (@Data, campos públicos, colunas explícitas)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImpl.java (debitarSaldo/creditarSaldo com @Transactional(MANDATORY) + SQL nativo, toDomain, salvar, listarAbaixoEstoqueMinimo)
    mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/PecaRepositoryPort.java (nomes PT-BR)
    mekano-infrastructure/src/main/resources/db/migration/V9__create_pecas_table.sql (estilo H2-compatível)
    mekano-infrastructure/src/main/resources/db/migration/V23__fix_pecas_requisicoes_columns.sql (padrão ALTER TABLE usado no projeto)
  </read_first>

  <action>
    Per D-04/D-05 — reserva modelada como coluna saldoReservado; estoque mínimo considera o disponível.

    1. Criar V34__add_saldo_reservado_to_pecas.sql (Flyway; próximo número livre é V34; NÃO repetir versão — gotcha G12):
       `ALTER TABLE pecas ADD COLUMN saldo_reservado BIGINT NOT NULL DEFAULT 0;`
       (BIGINT puro — H2 compatível, sem BIGSERIAL; coluna única por ALTER — gotcha H2).
    2. PecaEntity: adicionar `@Column(name = "saldo_reservado", nullable = false) public Integer saldoReservado = 0;`
       (mesmo estilo @Data do arquivo; Integer por consistência com saldo).
    3. Peca (domínio):
       - Novo campo `private final Long saldoReservado;`
       - `create(...)`: builder com `.saldoReservado(0L)`
       - `reconstitute(...)`: novo parâmetro `Long saldoReservado` (validar null → 0L; não-negativo) —
         ATENÇÃO: atualizar TODOS os call sites de reconstitute (PecaRepositoryImpl.toDomain no mesmo
         task; testes existentes de PecaTest; grep por `Peca.reconstitute` no repositório inteiro)
       - Novo método `public Long disponivel() { return saldoAtual - saldoReservado; }`
       - `isEstoqueMinimoAtingido()` passa a usar disponível: `estoqueMinimo != null && disponivel() < estoqueMinimo`
    4. PecaRepositoryPort: adicionar 3 métodos (retorno boolean = sucesso da operação atômica):
       - `boolean reservarSaldo(UUID pecaId, Integer quantidade);`
       - `boolean debitarSaldoReservado(UUID pecaId, Integer quantidade);`
       - `boolean liberarReserva(UUID pecaId, Integer quantidade);`
    5. PecaRepositoryImpl (padrão atual: @Transactional(TxType.MANDATORY) + em.createNativeQuery):
       - `reservarSaldo`: `UPDATE pecas SET saldo_reservado = saldo_reservado + :qtd WHERE uuid = :uuid AND (saldo - saldo_reservado) >= :qtd` → rowsUpdated > 0
       - `debitarSaldoReservado`: `UPDATE pecas SET saldo = saldo - :qtd, saldo_reservado = saldo_reservado - :qtd WHERE uuid = :uuid AND saldo_reservado >= :qtd` → rowsUpdated > 0
       - `liberarReserva`: `UPDATE pecas SET saldo_reservado = saldo_reservado - :qtd WHERE uuid = :uuid AND saldo_reservado >= :qtd` → rowsUpdated > 0
       - `salvar`: `entity.saldoReservado = peca.getSaldoReservado() == null ? 0 : peca.getSaldoReservado().intValue();`
       - `toDomain`: passar `entity.saldoReservado == null ? 0L : entity.saldoReservado.longValue()` no reconstitute
       - `listarAbaixoEstoqueMinimo`: filtro passa a `e.estoqueMinimo > 0 && (e.saldo - e.saldoReservado) < e.estoqueMinimo`
    6. PecaService: adicionar 3 wrappers @Transactional (padrão do módulo, delegação simples ao port):
       - `public boolean reservarSaldo(UUID pecaId, Integer quantidade) { return pecaRepository.reservarSaldo(pecaId, quantidade); }`
       - `public boolean debitarSaldoReservado(UUID pecaId, Integer quantidade) { return pecaRepository.debitarSaldoReservado(pecaId, quantidade); }`
       - `public boolean liberarReserva(UUID pecaId, Integer quantidade) { return pecaRepository.liberarReserva(pecaId, quantidade); }`
    7. Testes:
       - PecaTest (domain): reconstitute com saldoReservado; `isEstoqueMinimoAtingido` considera disponível
         (ex: saldo=10, reservado=8, minimo=5 → disponivel=2 < 5 → true; saldo=10, reservado=2 → false)
       - PecaRepositoryImplTest (infra, @QuarkusTest + @TestTransaction, H2): reservar com saldo suficiente
         → true e saldo_reservado incrementado; reservar acima do disponível → false sem alteração;
         debitarSaldoReservado → saldo e saldo_reservado decrescem; debitar sem reserva → false;
         liberarReserva → saldo_reservado decresce; listarAbaixoEstoqueMinimo considera disponível
       - PecaServiceTest (application, Mockito): wrappers delegam ao port (verify 1 chamada + retorno) —
         criar se não existir (o arquivo pode não existir hoje)
  </action>

  <acceptance_criteria>
    - V34 existe com `ALTER TABLE pecas ADD COLUMN saldo_reservado BIGINT NOT NULL DEFAULT 0`
    - grep `saldo_reservado` em PecaRepositoryImpl mostra os 3 UPDATEs com as guards
      (saldo - saldo_reservado) >= :qtd e saldo_reservado >= :qtd
    - Peca.java contém `disponivel()` e `saldoReservado`; isEstoqueMinimoAtingido usa disponivel()
    - Todos os call sites de `Peca.reconstitute` atualizados (grep — nenhum com assinatura antiga)
    - `./mvnw test -pl mekano-domain` e `./mvnw test -pl mekano-infrastructure -am -Dtest="PecaRepositoryImplTest"` → BUILD SUCCESS
  </acceptance_criteria>

  <verify>
    <automated>./mvnw test -pl mekano-domain && ./mvnw test -pl mekano-infrastructure -am -Dtest="PecaRepositoryImplTest"</automated>
  </verify>

  <done>Peça persiste saldo_reservado; reservar/debitar/liberar são atômicos com guarda; estoque mínimo usa saldo disponível; testes de domínio e integração verdes.</done>
</task>

<task type="auto">
  <name>Task 2: ItemOrcamento.pecaId + serialização + finalizarDiagnostico (D-06/D-07 base)</name>
  <files>
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/ItemOrcamento.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/OrcamentoEntityMapperImpl.java
    mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java
    mekano-application/src/test/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoServiceTest.java
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/mapper/OrcamentoEntityMapperImplTest.java
  </files>

  <read_first>
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/ItemOrcamento.java (construtor 3 args com validação)
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/OrcamentoEntityMapperImpl.java (serializarItens/desserializarItens — pipe/;-separado com escape)
    mekano-application/src/main/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoService.java (finalizarDiagnostico linhas 96-120 — branches PECA/SERVICO)
  </read_first>

  <action>
    Per D-06/D-07 (base): o evento OrcamentoAprovadoEvent precisa do pecaId por item; hoje o
    ItemOrcamento não o carrega. Propagar o UUID da peça desde o diagnóstico.

    1. ItemOrcamento (domain): adicionar `private final UUID pecaId;` (nullable — itens de SERVICO não têm).
       - Construtor existente de 3 args (descricao, quantidade, valorUnitario) delega para o novo
         construtor com pecaId = null (retrocompatível — não quebrar testes/call sites existentes).
       - Novo construtor: `ItemOrcamento(String descricao, Long quantidade, BigDecimal valorUnitario, UUID pecaId)`.
    2. OrcamentoEntityMapperImpl:
       - `serializarItens`: append `'|' + (item.getPecaId() != null ? item.getPecaId() : "")` após o valorUnitario
         (formato: descricao|quantidade|valor|pecaId; ; separa itens)
       - `desserializarItens`: `FIELD_PATTERN.split(part, 4)`; campos[3] null/blank → pecaId null,
         senão `UUID.fromString(campos[3].strip())` (retrocompatível com linhas legadas de 3 campos)
    3. OrdemDeServicoService.finalizarDiagnostico (linhas 101-114):
       - branch PECA: `itens.add(new ItemOrcamento(peca.getDescricao(), item.quantidade(), peca.getValorUnitario(), peca.getId()));`
       - branch SERVICO: manter `new ItemOrcamento(servico.getNome(), item.quantidade(), servico.getValor())` (pecaId null)
    4. Testes:
       - OrcamentoEntityMapperImplTest (novo, infra): round-trip toEntity→toDomain preserva pecaId;
         JSON legado de 3 campos ("desc|1|10.00") desserializa com pecaId null
       - OrdemDeServicoServiceTest (novo ou estendido, application, Mockito): finalizarDiagnostico com
         item PECA → orcamento item capturado possui pecaId da peça retornada pelo mock do
         PecaRepositoryPort; item SERVICO → pecaId null
  </action>

  <acceptance_criteria>
    - ItemOrcamento com construtor 4 args (pecaId) e 3 args delegando (grep `UUID pecaId`)
    - OrcamentoEntityMapperImpl.serializarItens emite 4 campos e desserializarItens usa split(part, 4)
    - finalizarDiagnostico branch PECA passa `peca.getId()` (grep na linha do new ItemOrcamento)
    - `./mvnw test -pl mekano-application -am -Dtest="OrdemDeServicoServiceTest"` e
      `./mvnw test -pl mekano-infrastructure -am -Dtest="OrcamentoEntityMapperImplTest"` → BUILD SUCCESS
  </acceptance_criteria>

  <verify>
    <automated>./mvnw test -pl mekano-application -am -Dtest="OrdemDeServicoServiceTest" && ./mvnw test -pl mekano-infrastructure -am -Dtest="OrcamentoEntityMapperImplTest"</automated>
  </verify>

  <done>ItemOrcamento carrega pecaId (null para serviços); itens_json serializa/desserializa 4 campos sem quebrar linhas legadas; finalizarDiagnostico propaga o UUID da peça.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application service → PecaRepositoryImpl | comandos de escrita de saldo cruzam aqui (chamadas internas) |
| banco → domínio (reconstitute) | valores persistidos reentram no domínio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-032-01 | Tampering | reservarSaldo/debitarSaldoReservado/liberarReserva | mitigate | SQL atômico com guarda no WHERE (Pitfall 3 do ROADMAP): `(saldo - saldo_reservado) >= :qtd` e `saldo_reservado >= :qtd` — impossível saldo ou reserva negativos sob concorrência |
| T-032-02 | Information Disclosure | desserializar itens_json com UUID inválido | mitigate | `UUID.fromString` com try/catch → lançar AppException(400) e não silenciar; linhas legadas (3 campos) seguem válidas |
| T-032-03 | Data Integrity | reconstitute com saldoReservado negativo | mitigate | Validação no reconstitute: saldoReservado null → 0L; negativo → AppException(400) (mesmo padrão de saldoAtual) |
| T-032-04 | Tampering | Flyway V34 em H2/PostgreSQL | mitigate | ALTER TABLE simples, BIGINT + DEFAULT 0, sem BIGSERIAL, uma coluna por ALTER (gotchas H2 documentados no AGENTS.md) |
</threat_model>

<verification>
- `./mvnw verify -pl mekano-rest -am` verde (módulos dependentes compilam com a nova assinatura de reconstitute)
- Migração V34 aplica em dev (docker-compose + `./mvnw quarkus:dev`) e em H2 de teste
- Nenhum teste existente de Peca/Orcamento quebrado (assinaturas preservadas ou atualizadas)
</verification>

<success_criteria>
- EST-03 (base): operação de reserva atômica disponível no repo
- EST-08 (base): operação de débito de reserva atômica disponível no repo
- D-05: estoque mínimo = função do saldo disponível (domínio + listagem)
- D-06/D-07 (base): pecaId disponível no ItemOrcamento e no itens_json
</success_criteria>

<output>
Create `.planning/phases/03.1-close-v1-0-gaps-estoque-pipeline-pagamento-audit-requisitos-/03.1-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New files:**
- `mekano-infrastructure/src/main/resources/db/migration/V34__add_saldo_reservado_to_pecas.sql` — `ALTER TABLE pecas ADD COLUMN saldo_reservado BIGINT NOT NULL DEFAULT 0`
- `mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/mapper/OrcamentoEntityMapperImplTest.java` — round-trip com/sem pecaId
- `mekano-application/src/test/java/com/fiap/mekano/application/service/ordemdeservico/OrdemDeServicoServiceTest.java` — finalizarDiagnostico propaga pecaId

**Modified classes (main):**
- `PecaEntity` — campo `saldoReservado` (Integer, default 0, coluna saldo_reservado)
- `Peca` (domain) — campo `saldoReservado` (Long), método `disponivel()`, `isEstoqueMinimoAtingido()` sobre disponível, `reconstitute` com novo parâmetro
- `PecaRepositoryPort` — novos métodos `reservarSaldo`, `debitarSaldoReservado`, `liberarReserva` (boolean)
- `PecaRepositoryImpl` — 3 UPDATEs nativos atômicos, salvar/toDomain com saldoReservado, listarAbaixoEstoqueMinimo por disponível
- `ItemOrcamento` (domain) — campo `pecaId` (nullable) + construtor 4 args
- `OrcamentoEntityMapperImpl` — serialização 4 campos `descricao|qtd|valor|pecaId` (retrocompatível)
- `OrdemDeServicoService.finalizarDiagnostico` — branch PECA passa `peca.getId()`

**Database object:**
- Tabela `pecas` ganha coluna `saldo_reservado`
