---
phase: 06-quality-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - mekano-rest/pom.xml
autonomous: true
requirements: [QLD-01]
user_setup: []

must_haves:
  truths:
    - "`./mvnw verify -pl mekano-rest -am` runs JaCoCo check and passes 80% LINE coverage"
    - "Unified coverage report available at mekano-rest/target/site/jacoco-aggregate/index.html"
    - "Generated MapStruct MapperImpl classes are excluded from coverage calculation"
    - "PanacheRepository classes are excluded from coverage calculation"
  artifacts:
    - path: "pom.xml"
      provides: "Updated JaCoCo check exclusions (MapperImpl + PanacheRepository)"
      contains: "**/*MapperImpl.class"
    - path: "mekano-rest/pom.xml"
      provides: "JaCoCo report-aggregate execution"
      contains: "report-aggregate"
  key_links:
    - from: "mekano-rest/pom.xml"
      to: "pom.xml (root)"
      via: "Same JaCoCo version, complementary exclusions"
      pattern: "jacoco-maven-plugin"
    - from: "mekano-rest/target/site/jacoco-aggregate"
      to: "all .exec files from all modules"
      via: "report-aggregate merges module coverage data"
---

<objective>
Configure JaCoCo aggregated coverage reporting across all 4 Maven modules

**Purpose:** The current JaCoCo configuration only generates per-module BUNDLE reports, giving false positives when individual modules exceed 80% but the project total is below 80%. Adding `report-aggregate` to mekano-rest (which depends on all other modules) provides a single unified coverage report.

**Output:**
- Updated root `pom.xml` with additional exclusions in `jacoco-check`
- Updated `mekano-rest/pom.xml` with new `report-aggregate` execution
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@pom.xml
@mekano-rest/pom.xml
@.planning/phases/06-quality-bug-fixes/06-RESEARCH.md
@.planning/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MapperImpl and PanacheRepository exclusions to root pom.xml jacoco-check</name>
  <files>pom.xml</files>
  <read_first>
    @pom.xml (lines 144-174 — jacoco-check execution with current excludes)
  </read_first>
  <action>
    In root `pom.xml`, inside the `jacoco-check` execution's `<configuration><excludes>`, add TWO new `<exclude>` entries AFTER the existing `**/*Entity.class` exclude:
    
    1. `<exclude>**/*MapperImpl.class</exclude>` — excludes MapStruct-generated mapper implementations
    2. `<exclude>**/*PanacheRepository*.class</exclude>` — excludes Panache repository proxy classes
    
    These exclusions prevent generated code and Panache proxies from counting against the 80% LINE coverage minimum. The existing 7 excludes (Dto, DTO, Request, Response, ExceptionMapper, Config, Resource, Entity) remain unchanged.
    
    Per D-02: exclusions must be added to BOTH check (root) AND report-aggregate (mekano-rest) — separate `<configuration>` blocks.
  </action>
  <verify>
    <automated>grep -c "MapperImpl" pom.xml && grep -c "PanacheRepository" pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `pom.xml` contains `<exclude>**/*MapperImpl.class</exclude>` inside `jacoco-check` execution
    - `pom.xml` contains `<exclude>**/*PanacheRepository*.class</exclude>` inside `jacoco-check` execution
    - All original 7 exclusions remain intact
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add report-aggregate execution to mekano-rest/pom.xml with exclusions</name>
  <files>mekano-rest/pom.xml</files>
  <read_first>
    @mekano-rest/pom.xml (lines 151-233 — <build><plugins> section)
  </read_first>
  <action>
    In `mekano-rest/pom.xml`, add a JaCoCo `report-aggregate` execution inside `<build><plugins>` (after the jandex plugin, before the maven-compiler-plugin). The execution config:

    - Execution ID: `jacoco-report-aggregate`
    - Phase: `verify`
    - Goal: `report-aggregate`
    - Configuration exclusions: `**/*MapperImpl.class`, `**/*PanacheRepository*.class`
    - Do NOT use `<rules>` or `<check>` — this is a report-only execution. The actual coverage gate (`jacoco:check` with 80% LINE) remains in the root pom.xml.
    
    Per RESEARCH.md Pitfall 2: exclusion mismatch between report-aggregate and check causes coverage count discrepancy. Both must have identical excludes.
    
    Per D-01: report-aggregate MUST be in mekano-rest because it has compile dependencies on all other modules (domain, application, infrastructure), making it the natural aggregation point.
    
    Reference pattern from RESEARCH.md §Pattern 1, with the XML structure from lines 195-215.
  </action>
  <verify>
    <automated>grep -c "report-aggregate" mekano-rest/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `mekano-rest/pom.xml` contains a `jacoco-maven-plugin` execution with `report-aggregate` goal
    - Exclusions include both `**/*MapperImpl.class` and `**/*PanacheRepository*.class`
    - `./mvnw verify -pl mekano-rest -am` completes successfully (JaCoCo check + report-aggregate both run)
    - Coverage gate passes at ≥80% LINE
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build process | JaCoCo instrumentation runs during build — no runtime trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | JaCoCo exclusion config | mitigate | Exclusions verified by grep pattern match before commit. Full `verify` suite runs to confirm no exclusion error. |
</threat_model>

<verification>
1. `./mvnw compile -pl mekano-rest -am` — compiles without errors
2. `./mvnw verify -pl mekano-rest -am` — full test suite + JaCoCo check + report-aggregate all pass
3. `ls mekano-rest/target/site/jacoco-aggregate/index.html` — aggregate report generated
</verification>

<success_criteria>
- [ ] Both pom.xml files updated with correct exclusions
- [ ] `./mvnw verify -pl mekano-rest -am` passes with ≥80% LINE coverage
- [ ] Aggregate report generated at mekano-rest/target/site/jacoco-aggregate/
- [ ] No MapperImpl or PanacheRepository classes counted in coverage
</success_criteria>

<output>
Create `.planning/phases/06-quality-bug-fixes/06-01-SUMMARY.md` when done
</output>