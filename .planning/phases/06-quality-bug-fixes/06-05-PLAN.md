---
phase: 06-quality-bug-fixes
plan: 05
type: execute
wave: 4
depends_on: [06-03, 06-04]
files_modified: []
autonomous: true
requirements: [QLD-02]
user_setup: []
must_haves:
  truths:
    - "ItemOrcamento is either moved to valueobject/ or deprecated with javadoc in model/"
    - "Any additional tech debt items found during execution are fixed"
    - "Full test suite passes after all changes"
  artifacts: []
  key_links: []
---

<objective>
Open-ended code review: fix remaining tech debt items discovered during execution

**Purpose:** Per D-13, the code review is open-ended — the executor can find and fix additional items beyond the planned list. This plan handles any findings discovered during execution of Plans 06-01 through 06-04, plus the discretion items explicitly deferred to this plan.

**Items deferred to this plan (discretion-based):**
1. **ItemOrcamento package move** (D-10) — Move `ItemOrcamento` from `domain/model/` to `domain/valueobject/`. This is a Value Object (immutable, no UUID, no lifecycle, `@EqualsAndHashCode` by value). Moving it requires updating ~50 import statements across the codebase. TWO approaches available:
   - **Full move**: Move file, update ALL imports in ALL files that reference `domain.model.ItemOrcamento`
   - **Deprecate + copy**: Keep original in model/ with `@Deprecated` javadoc pointing to new location, create new VO in valueobject/ with same code. Consumer files gradually migrate.
   
2. **Any compile warnings or code quality issues** discovered by the executor

**Open-ended items (executor discretion):**
- Inconsistent `PanacheRepository<E>` vs `PanacheRepositoryBase<E, Long>` usage
- `AppException` carrying HTTP `int status` (domain leak of infra concern — analyze, don't change)
- Test assertion style inconsistency (JUnit5 `assertEquals` vs AssertJ `assertThat`)
- Any other tech debt items the executor identifies

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-quality-bug-fixes/06-RESEARCH.md
@.planning/phases/06-quality-bug-fixes/06-CONTEXT.md
@mekano-domain/src/main/java/com/fiap/mekano/domain/model/ItemOrcamento.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move ItemOrcamento to valueobject/ package (or deprecate)</name>
  <files>
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/ItemOrcamento.java
    mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/ItemOrcamento.java
    (plus ~50 files with imports to update)
  </files>
  <read_first>
    @ItemOrcamento.java
  </read_first>
  <action>
    Per D-10 and discretion area: `ItemOrcamento` is a Value Object (immutable, `@EqualsAndHashCode` by value, no UUID, no lifecycle) but lives in `model/` package. 

    **Choose ONE approach:**

    **Option A (Full move):**
    - Create `domain/valueobject/ItemOrcamento.java` with the exact same code (package changed to `com.fiap.mekano.domain.valueobject`)
    - Update ALL import statements across the codebase from `com.fiap.mekano.domain.model.ItemOrcamento` to `com.fiap.mekano.domain.valueobject.ItemOrcamento`
    - grep found ~50+ import sites in ~15 files
    - Delete `domain/model/ItemOrcamento.java`

    **Option B (Deprecate + copy — less risky):**
    - Add `@Deprecated` javadoc to `domain/model/ItemOrcamento.java` pointing to valueobject/ as the new location
    - Create `domain/valueobject/ItemOrcamento.java` with identical code
    - Do NOT update any existing imports — they continue to work via the deprecated class
    - Future phases naturally migrate imports

    **Recommendation:** Option A if confident in grep accuracy; Option B if risk aversion. Both pass `./mvnw compile`.
  </action>
  <verify>
    <automated>./mvnw compile -pl mekano-rest -am 2>&1 | Select-String "BUILD SUCCESS"</automated>
  </verify>
  <acceptance_criteria>
    - `ItemOrcamento` exists in `domain/valueobject/`
    - All imports updated (Option A) or model class deprecated (Option B)
    - `./mvnw compile -pl mekano-rest -am` passes
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Open-ended findings — fix any remaining tech debt discovered during execution</name>
  <files></files>
  <read_first>
  </read_first>
  <action>
    During execution of Plans 06-01 through 06-04 and Task 1 of this plan, the executor may have discovered additional tech debt items. Candidates include:
    
    - Inconsistent `PanacheRepository<E>` vs `PanacheRepositoryBase<E, Long>` across repos
    - `AppException` leaking HTTP status (domain concern analysis — note only, do NOT change)
    - Any compilation warnings that should be suppressed or fixed
    - Any dead imports or unused variables
    
    **Present a summary of findings to the user** with proposed fixes. Let the user approve before implementing.
    
    If NO additional findings, present "No additional issues found" and mark task complete with empty work.
  </action>
  <verify>
    <human-check>
      Reviewer reviews the findings summary from the executor. Types "approved" to confirm, or provides direction on which items to fix.
    </human-check>
  </verify>
  <acceptance_criteria>
    - All approved findings are fixed
    - `./mvnw test -pl mekano-rest -am` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package move | Changing package of a class used across modules — import updates required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | ItemOrcamento package move | mitigate | `./mvnw compile -pl mekano-rest -am` catches all broken imports. Full test suite validates runtime behavior. Option B (copy + deprecate) eliminates risk entirely. |
</threat_model>

<verification>
1. `./mvnw compile -pl mekano-rest -am` — itemOrcamento move compiles
2. `./mvnw test -pl mekano-rest -am` — full test suite green
3. `./mvnw verify -pl mekano-rest -am` — JaCoCo 80% gate passes
</verification>

<success_criteria>
- [ ] ItemOrcamento moved or deprecated
- [ ] Any approved open-ended findings fixed
- [ ] `./mvnw verify -pl mekano-rest -am` passes (compile + tests + coverage gate)
</success_criteria>

<output>
Create `.planning/phases/06-quality-bug-fixes/06-05-SUMMARY.md` when done
</output>