---
phase: 06-quality-bug-fixes
plan: 04
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ClienteRepositoryImpl.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImpl.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/RequisicaoCompraRepositoryImpl.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/NfEntradaRepositoryImpl.java
autonomous: true
requirements: [QLD-02]
user_setup: []
must_haves:
  truths:
    - "ClienteRepositoryImpl has @Retry, @Timeout, @CacheResult, @CacheInvalidate annotations on appropriate methods"
    - "PecaRepositoryImpl has @Retry and @Timeout added (already has CacheResult/CacheInvalidate from prior work)"
    - "RequisicaoCompraRepositoryImpl has @Retry, @Timeout, @CacheResult, @CacheInvalidate"
    - "NfEntradaRepositoryImpl has @Retry, @Timeout, @CacheResult, @CacheInvalidate"
    - "All cache names already exist in CacheNames.java — no new cache config needed"
  artifacts:
    - path: "mekano-infrastructure/.../ClienteRepositoryImpl.java"
      provides: "FT + Cache annotations added"
      contains: "@CacheResult(cacheName = \"clientes\")"
    - path: "mekano-infrastructure/.../PecaRepositoryImpl.java"
      provides: "@Retry + @Timeout added to existing annotations"
      contains: "@Timeout(value = 5, unit = ChronoUnit.SECONDS)"
  key_links:
    - from: "ClienteRepositoryImpl"
      to: "CacheNames.CLIENTES"
      via: "@CacheResult(cacheName = CacheNames.CLIENTES)"
      pattern: "CacheNames"
---
<objective>
Add Fault Tolerance (@Retry, @Timeout) and Cache (@CacheResult, @CacheInvalidate) to remaining repository implementations

**Purpose:** 3 repository implementations (Cliente, RequisicaoCompra, NfEntrada) lack ALL fault tolerance and caching annotations. PecaRepositoryImpl has caching (`@CacheResult`/`@CacheInvalidate`) but lacks `@Retry`/`@Timeout`. Adding these annotations makes all 7 repositories consistent and improves resilience against transient database failures.

**VERIFIED:** All required cache names already exist in `CacheNames.java` (CLIENTES, PECAS, REQUISICOES, NF_ENTRADAS) and `cache-config.yml` (expire-after-write=60s, max-size=100-200). No new cache config or POM dependencies needed. The `quarkus-smallrye-fault-tolerance` and `quarkus-cache` dependencies are already declared in `mekano-infrastructure/pom.xml`.

**Reference pattern:** VeiculoRepositoryImpl — `@Timeout(5, SECONDS)` on write methods, `@Retry(maxRetries=3)` + `@CacheResult` on read methods, `@CacheInvalidate` on write methods.

**Output:**
- 4 repository impl files with complete FT/Cache annotations
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ClienteRepositoryImpl.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImpl.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/RequisicaoCompraRepositoryImpl.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/NfEntradaRepositoryImpl.java
@.planning/phases/06-quality-bug-fixes/06-RESEARCH.md (Pattern 3 — FT/Cache reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @Retry and @Timeout to PecaRepositoryImpl + full FT/Cache to NfEntradaRepositoryImpl</name>
  <files>
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImpl.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/NfEntradaRepositoryImpl.java
  </files>
  <read_first>
    @PecaRepositoryImpl.java
    @NfEntradaRepositoryImpl.java
  </read_first>
  <action>
    Per D-12, add fault tolerance and cache annotations to two repos:

    **A. PecaRepositoryImpl** (already has `@CacheResult`/`@CacheInvalidate` — just add `@Retry`+`@Timeout`):
    
    Add imports: `import org.eclipse.microprofile.faulttolerance.Retry; import org.eclipse.microprofile.faulttolerance.Timeout; import java.util.concurrent.Conceitos.Unidade;` 

    Actually: `import org.eclipse.microprofile.faulttolerance.Retry;` and `import org.eclipse.microprofile.faulttolerance.Timeout;` plus `import java.util.concurrent.TimeUnit;`
    
    - Add `@Timeout(value = 5, unit = ChronoUnit.SECONDS)` on `salvar()` (now `save()`) — already has `@CacheInvalidate`
    - Add `@Retry(maxRetries = 3)` on `findById()` (was `buscarPorId()`) — already has `@CacheResult`
    - Add `@Timeout(value = 5, unit = ChronoUnit.SECONDS)` on `debitarSaldo()`, `creditarSaldo()`, `reservarSaldo()`, `debitarSaldoReservado()`, `liberarReserva()` — all already have `@CacheInvalidate`
    
    Use `java.time.temporal.ChronoUnit` for the TimeUnit parameter (matching VeiculoRepositoryImpl pattern).

    **B. NfEntradaRepositoryImpl** (no annotations yet — add full set):
    
    Add imports: `org.eclipse.microprofile.faulttolerance.Retry`, `org.eclipse.microprofile.faulttolerance.Timeout`, `io.quarkus.cache.CacheResult`, `io.quarkus.cache.CacheInvalidate`, `com.fiap.mekano.infrastructure.cache.CacheNames`, `java.time.temporal.ChronoUnit`
    
    - `save()`: `@Transactional` + `@Timeout(5, SECONDS)` + `@CacheInvalidate(cacheName = CacheNames.NF_ENTRADAS)`
    - `findById()`: `@Retry(maxRetries = 3)` + `@Timeout(5, SECONDS)` + `@CacheResult(cacheName = CacheNames.NF_ENTRADAS)`
    - `findAll()`: `@Timeout(5, SECONDS)`
    - `countAll()`: `@Timeout(5, SECONDS)`
    
    Use the method names as they exist AFTER Plan 06-02 rename (`save()`, `findById()`).
  </action>
  <verify>
    <automated>./mvnw compile -pl mekano-rest -am 2>&1 | Select-String "BUILD SUCCESS"</automated>
  </verify>
  <acceptance_criteria>
    - `PecaRepositoryImpl.save()` has `@Timeout(5, SECONDS)` — method name reflects rename from Plan 06-02
    - `PecaRepositoryImpl.findById()` has `@Retry(maxRetries=3)` 
    - `NfEntradaRepositoryImpl.save()` has `@Timeout` + `@CacheInvalidate` with correct CacheNames
    - `NfEntradaRepositoryImpl.findById()` has `@Retry` + `@Timeout` + `@CacheResult` with correct CacheNames
    - `./mvnw compile -pl mekano-rest -am` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add full FT/Cache to ClienteRepositoryImpl and RequisicaoCompraRepositoryImpl</name>
  <files>
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/ClienteRepositoryImpl.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/RequisicaoCompraRepositoryImpl.java
  </files>
  <read_first>
    @ClienteRepositoryImpl.java
    @RequisicaoCompraRepositoryImpl.java
  </read_first>
  <action>
    Add complete FT + Cache annotations to two repos that currently have NONE:

    **A. ClienteRepositoryImpl**:
    
    Add imports: `Retry`, `Timeout`, `CacheResult`, `CacheInvalidate`, `CacheNames`, `ChronoUnit`
    
    - `save(Cliente)`: `@Timeout(5, SECONDS)` + `@CacheInvalidate(cacheName = CacheNames.CLIENTES)`
    - `findById(UUID)`: `@Retry(maxRetries = 3)` + `@Timeout(5, SECONDS)` + `@CacheResult(cacheName = CacheNames.CLIENTES)`
    - `findByCpf(String)`: `@Timeout(5, SECONDS)` + `@CacheResult(cacheName = CacheNames.CLIENTES)`
    - `findAll(...)`: `@Timeout(5, SECONDS)`
    - `countAll()`: `@Timeout(5, SECONDS)`
    - `markAsDeleted(UUID)`: `@Timeout(5, SECONDS)` + `@CacheInvalidate(cacheName = CacheNames.CLIENTES)`

    **B. RequisicaoCompraRepositoryImpl**:
    
    Add imports: `Retry`, `Timeout`, `CacheResult`, `CacheInvalidate`, `CacheNames`, `ChronoUnit`
    
    - `save(RequisicaoCompra)`: `@Timeout(5, SECONDS)` + `@CacheInvalidate(cacheName = CacheNames.REQUISICOES)`
    - `findById(UUID)`: `@Retry(maxRetries = 3)` + `@Timeout(5, SECONDS)` + `@CacheResult(cacheName = CacheNames.REQUISICOES)`
    - `findAll(...)`: `@Timeout(5, SECONDS)`
    - `countAll()`: `@Timeout(5, SECONDS)`
    - `atualizar(...)`: `@Timeout(5, SECONDS)` + `@CacheInvalidate(cacheName = CacheNames.REQUISICOES)`

    Method names reflect renames from Plan 06-02: `save()` not `salvar()`, `findById()` not `buscarPorId()`.
    
    Follow exact same pattern as VeiculoRepositoryImpl: `@Retry` on read methods, `@Timeout` on all methods, `@CacheResult` on read methods, `@CacheInvalidate` on write methods.
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-infrastructure -am 2>&1 | Select-String "BUILD SUCCESS"</automated>
  </verify>
  <acceptance_criteria>
    - ClienteRepositoryImpl has all 4 annotation types on appropriate methods
    - RequisicaoCompraRepositoryImpl has all 4 annotation types on appropriate methods
    - Method names match the renamed versions from Plan 06-02
    - `./mvnw test -pl mekano-infrastructure -am` passes (integration tests validate FT/Cache behavior)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Database | FT annotations add retry/timeout — no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Denial of Service | @Timeout annotation | mitigate | 5-second timeout prevents hung connections from blocking the pool. Existing @Retry + @Timeout tests in VeiculoFaultToleranceTest serve as reference pattern. |
| T-06-06 | Tampering | @CacheResult on read methods | mitigate | CacheInvalidate on all write methods ensures stale data is purged. expire-after-write=60s in cache-config.yml is a secondary invalidation bound. |
</threat_model>

<verification>
1. `./mvnw compile -pl mekano-rest -am` — annotations compile
2. `./mvnw test -pl mekano-infrastructure -am` — integration tests validate FT (VeiculoFaultToleranceTest already exists as reference)
3. `./mvnw test -pl mekano-rest -am` — full test suite, including VeiculoFaultToleranceTest and FaultToleranceTest
</verification>

<success_criteria>
- [ ] All 4 repo impls have complete FT + Cache annotations matching VeiculoRepositoryImpl pattern
- [ ] Cache names reference existing CacheNames constants (not hardcoded strings)
- [ ] `./mvnw test -pl mekano-rest -am` passes
</success_criteria>

<output>
Create `.planning/phases/06-quality-bug-fixes/06-04-SUMMARY.md` when done
</output>