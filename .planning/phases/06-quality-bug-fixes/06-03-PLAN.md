---
phase: 06-quality-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/PecaEntityMapper.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/RequisicaoCompraEntityMapper.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/NfEntradaEntityMapper.java
  - mekano-domain/src/main/java/com/fiap/mekano/domain/model/StatusPagamento.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/PecaEntity.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/RequisicaoCompraEntity.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/NfEntradaEntity.java
  - mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Placa.java
  - mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/PlacaVeiculo.java
  - mekano-domain/src/test/java/com/fiap/mekano/domain/valueobject/PlacaTest.java
autonomous: true
requirements: [QLD-02]
user_setup: []
must_haves:
  truths:
    - "3 empty mapper classes removed without breaking CDI injection (verified: zero references outside self)"
    - "domain.model.StatusPagamento removed (verified: zero references — domain.os.StatusPagamento is the active one)"
    - "PecaEntity, RequisicaoCompraEntity, NfEntradaEntity use @Getter/@Setter with private fields (not @Data)"
    - "PlacaVeiculo gains formatted() method from Placa.java; Placa.java and PlacaTest.java deleted"
  artifacts:
    - path: "mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/"
      provides: "3 empty mapper files deleted"
      missing: ["PecaEntityMapper.java", "RequisicaoCompraEntityMapper.java", "NfEntradaEntityMapper.java"]
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/model/StatusPagamento.java"
      provides: "Deleted (dead code)"
      missing: [""]
  key_links:
    - from: "PecaEntity.java"
      to: "PecaRepositoryImpl.toDomain()"
      via: "Direct field access (entity.uuid, entity.codigo) — @Data→@Getter/@Setter breaks these"
      pattern: "entity\\."
---
<objective>
Remove dead code, standardize entity style, and unify duplicated VOs

**Purpose:** Three categories of low-risk cleanup: (1) remove empty mapper stubs and unused StatusPagamento enum, (2) convert `@Data` entities to `@Getter/@Setter` for encapsulation consistency, (3) merge Placa.java functionality into PlacaVeiculo.java and delete the duplicate.

**VERIFIED before planning:**
- 3 empty mappers have ZERO cross-file references (grep confirmed)
- `domain.model.StatusPagamento` has ZERO production references (all code uses `domain.os.StatusPagamento`)
- `Placa.java` has ZERO non-test production references (Veiculo.java uses `PlacaVeiculo`)
- PecaRepositoryImpl already has `@CacheResult`/`@CacheInvalidate` on read/write — entity style change needs direct field access updates

**Output:**
- 4 files deleted (3 mappers + 1 StatusPagamento)
- 3 entity files restyled
- 2 VO files unified (1 deleted, 1 enhanced)
</objective>

<execution_context>
@C:/Users/victo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/victo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/PecaEntityMapper.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/RequisicaoCompraEntityMapper.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/NfEntradaEntityMapper.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/model/StatusPagamento.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/PecaEntity.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/RequisicaoCompraEntity.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/NfEntradaEntity.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Placa.java
@mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/PlacaVeiculo.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/PecaRepositoryImpl.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/NfEntradaRepositoryImpl.java
@mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/RequisicaoCompraRepositoryImpl.java
@.planning/phases/06-quality-bug-fixes/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 3 empty mapper classes and dead StatusPagamento enum</name>
  <files>
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/PecaEntityMapper.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/RequisicaoCompraEntityMapper.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/NfEntradaEntityMapper.java
    mekano-domain/src/main/java/com/fiap/mekano/domain/model/StatusPagamento.java
  </files>
  <read_first>
    @PecaEntityMapper.java
    @NfEntradaEntityMapper.java
    @RequisicaoCompraEntityMapper.java
    @StatusPagamento.java (domain/model)
  </read_first>
  <action>
    Delete 4 files that are verified dead code:
    
    1. **PecaEntityMapper.java** — empty class (`@ApplicationScoped` with zero methods). Verified: ZERO references outside its own file.
    2. **RequisicaoCompraEntityMapper.java** — empty class. Verified: ZERO references.
    3. **NfEntradaEntityMapper.java** — empty class. Verified: ZERO references.
    4. **domain/model/StatusPagamento.java** — simple enum (PENDENTE, CONFIRMADO) with ZERO production references. The ACTIVE StatusPagamento is in `domain/os/` with transition logic (used by OrdemDeServico, CobrancaGeradaEvent, tests).

    Per D-08 and D-11. No other changes needed — no imports to remove since nothing references these files.
  </action>
  <verify>
    <automated>Test-Path "mekano-infrastructure\src\main\java\com\fiap\mekano\infrastructure\mapper\PecaEntityMapper.java" -PathType Leaf; $? -eq $false</automated>
  </verify>
  <acceptance_criteria>
    - 4 files deleted from filesystem (confirmed by `Test-Path`)
    - `./mvnw compile -pl mekano-rest -am` passes (no broken imports)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Convert entity style from @Data to @Getter/@Setter for 3 entities</name>
  <files>
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/PecaEntity.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/RequisicaoCompraEntity.java
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/NfEntradaEntity.java
  </files>
  <read_first>
    @PecaEntity.java
    @NfEntradaEntity.java
    @RequisicaoCompraEntity.java
    @PecaRepositoryImpl.java
    @NfEntradaRepositoryImpl.java
    @RequisicaoCompraRepositoryImpl.java
  </read_first>
  <action>
    Per D-07, convert 3 entities from `@Data` to `@Getter/@Setter` with private fields, matching the established pattern in UserEntity, ClienteEntity, VeiculoEntity, and ServicoEntity.

    For each entity file:
    - Replace `@Data` with `@Getter @Setter`
    - Change all `public` fields to `private`

    **CRITICAL: Update all direct field access in repository impls.** Because @Data generates public fields, and @Getter/@Setter generates private fields + accessor methods, every `entity.fieldName` access in the corresponding repository impls MUST be updated to `entity.getFieldName()` and `entity.setFieldName()`. The 3 repository impls and their `toDomain()` methods are the primary consumers affected.

    | Entity | Repo Impl | Direct field access patterns |
    |--------|-----------|------------------------------|
    | PecaEntity | PecaRepositoryImpl | `entity.uuid`, `entity.codigo`, `entity.descricao`, `entity.valorUnitario`, `entity.saldo`, `entity.saldoReservado`, `entity.estoqueMinimo` |
    | RequisicaoCompraEntity | RequisicaoCompraRepositoryImpl | `entity.uuid`, `entity.pecaId`, `entity.quantidade`, `entity.status`, `entity.motivo` |
    | NfEntradaEntity | NfEntradaRepositoryImpl | `entity.uuid`, `entity.chaveAcesso`, `entity.valorTotal`, `entity.pecaId`, `entity.requisicaoCompraId`, `entity.dataRecebimento` |

    The `toDomain()` static methods in each RepoImpl also access fields directly — update to `entity.getXxx()`.

    Keep `@NoArgsConstructor`, `@AllArgsConstructor`, `@Builder` on the entity (they work with @Getter/@Setter). Keep `extends BaseEntity` (which has its own @Getter/@Setter pattern — no change needed there).
  </action>
  <verify>
    <automated>./mvnw compile -pl mekano-rest -am 2>&1 | Select-String "BUILD SUCCESS"</automated>
  </verify>
  <acceptance_criteria>
    - `PecaEntity.java` uses `@Getter @Setter` with all fields `private`
    - `RequisicaoCompraEntity.java` uses `@Getter @Setter` with all fields `private`
    - `NfEntradaEntity.java` uses `@Getter @Setter` with all fields `private`
    - `PecaRepositoryImpl.java` uses `entity.getXxx()` form (not `entity.xxx`)
    - `NfEntradaRepositoryImpl.java` uses `entity.getXxx()` form
    - `RequisicaoCompraRepositoryImpl.java` uses `entity.getXxx()` form
    - `./mvnw compile -pl mekano-rest -am` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Unify Placa and PlacaVeiculo VOs — merge formatted() into PlacaVeiculo, delete Placa.java</name>
  <files>
    mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/PlacaVeiculo.java
    mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Placa.java
    mekano-domain/src/test/java/com/fiap/mekano/domain/valueobject/PlacaTest.java
  </files>
  <read_first>
    @Placa.java
    @PlacaVeiculo.java
  </read_first>
  <action>
    Per D-09, unify the two plate VOs. `Placa.java` is dead in production (zero non-test imports). `PlacaVeiculo.java` is the active VO used by `Veiculo.java`, `VeiculoService.java`, `PlacaVeiculoMapper.java`, and tests.

    **VERIFIED regex differences:**
    - `Placa.java`: `[A-Z]{3}[0-9]{4}` (old) + `[A-Z]{2}[0-9]{4}[A-Z]{2}` (Mercosul AB-1234-CD, 8 chars)
    - `PlacaVeiculo.java`: `[A-Z]{3}[0-9]{4}` (old) + `[A-Z]{3}[0-9][A-Z][0-9]{2}` (Mercosul ABC-1D23, 7 chars)

    **Action:**
    
    1. **Update PlacaVeiculo.java** — Add the `formatted()` method from Placa.java (useful for display). Expand the regex pattern to ALSO accept `[A-Z]{2}[0-9]{4}[A-Z]{2}` (AB-1234-CD format). The regex becomes: `^(?:[A-Z]{3}[0-9]{4}|[A-Z]{3}[0-9][A-Z][0-9]{2}|[A-Z]{2}[0-9]{4}[A-Z]{2})$`
    
    2. **Delete Placa.java** and **PlacaTest.java** — both are dead code for production. The PlacaTest cases are covered by PlacaVeiculoTest (which already tests both old format and Mercosul).
    
    Use `@EqualsAndHashCode` `@ToString` (already present on both). Keep the existing constructor validation pattern (already correct).
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-domain -Dtest=PlacaVeiculoTest 2>&1 | Select-String "BUILD SUCCESS"</automated>
  </verify>
  <acceptance_criteria>
    - `PlacaVeiculo.java` has `formatted()` method supporting both old (ABC-1234) and Mercosul (AB-1234-CD, ABC-1D23) formats
    - `PlacaVeiculo.java` regex accepts all 3 patterns: LLLNNNN, LLLNLNN, LLNNNNLL
    - `Placa.java` and `PlacaTest.java` deleted
    - `./mvnw test -pl mekano-domain` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entity field access | Changing from public fields (accessed directly) to private fields (via getters) affects all repository impls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Entity @Data→@Getter/@Setter | mitigate | All direct `entity.field` accesses in corresponding RepositoryImpl updated to `entity.getField()`. Compile check catches any missed. Full test suite validates behavior. |
| T-06-04 | Tampering | VO unification regex change | mitigate | PlacaVeiculoTest already covers old+ABC1D23 patterns. New AB1234CD pattern verified as matching Placa.java's regex. Test added for the new pattern. |
</threat_model>

<verification>
1. `./mvnw compile -pl mekano-rest -am` — entity style + field access changes compile
2. `./mvnw test -pl mekano-domain` — VO tests pass with merged regex
3. `./mvnw test -pl mekano-infrastructure -am` — repository integration tests pass
4. `./mvnw test -pl mekano-rest -am` — full test suite
</verification>

<success_criteria>
- [ ] 4 dead files deleted, compile still passes
- [ ] 3 entities converted, all field accesses updated in repos, compile passes
- [ ] PlacaVeiculo enhanced, Placa.java deleted, all tests pass
- [ ] `./mvnw test -pl mekano-rest -am` passes
</success_criteria>

<output>
Create `.planning/phases/06-quality-bug-fixes/06-03-SUMMARY.md` when done
</output>