---
phase: 04-m-dulo-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java
autonomous: true
requirements:
  - INF-01
  - INF-06
must_haves:
  truths:
    - "User.reconstitute() existe em User.java e é acessível como método estático público"
    - "User.reconstitute() preserva id e createdAt recebidos como parâmetros (não gera novos)"
    - "UserEntity.java estende PanacheEntityBase e declara @Id UUID id SEM @GeneratedValue"
    - "UserEntity.java mapeia todos os 5 campos: id, name, email, passwordHash, createdAt"
    - "UserEntity NÃO contém imports de classes do mekano-domain nem mekano-application"
    - "./mvnw compile -pl mekano-infrastructure -am retorna BUILD SUCCESS"
  artifacts:
    - path: "mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java"
      provides: "Factory method reconstitute() para reconstrução a partir do banco"
      contains: "public static User reconstitute"
    - path: "mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java"
      provides: "Entidade JPA mapeando tabela users"
      contains: "extends PanacheEntityBase"
  key_links:
    - from: "UserEntity.java"
      to: "V1__create_users_table.sql (Plano 04-03)"
      via: "schema JPA deve bater com DDL do Flyway"
      pattern: "@Column(name = \"password_hash\")"
    - from: "User.reconstitute()"
      to: "UserEntityMapper.toDomain() (Plano 04-03)"
      via: "mapper chama reconstitute() para reconstruir User do banco"
      pattern: "User.reconstitute"
---

<objective>
Adicionar `User.reconstitute()` ao módulo domain para permitir reconstrução de `User` a partir de dados persistidos, e criar `UserEntity.java` como entidade JPA que estende `PanacheEntityBase` sem `@GeneratedValue`.

Purpose: `User.create()` gera UUID e timestamp novos — não pode ser reutilizado para reconstrução. `reconstitute()` preserva os valores vindos do banco. `UserEntity` é a representação JPA separada da entidade de domínio (Clean Architecture: sem misturar anotações JPA no domain).
Output: `User.java` modificado com `reconstitute()`. `UserEntity.java` criado com 5 campos mapeados.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-m-dulo-infrastructure/04-CONTEXT.md
@.planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md

<interfaces>
<!-- User.java — estado atual (DEVE ser lido antes de modificar) -->
<!-- package com.fiap.mekano.domain.model -->
<!-- @Builder(access = AccessLevel.PRIVATE) — builder privado -->
<!-- Campos: UUID id, String name, Email email, String passwordHash, LocalDateTime createdAt -->
<!-- Factory method existente: public static User create(String name, String emailValue, String passwordHash) -->
<!-- IMPORTANTE: reconstitute() deve ficar DENTRO de User para acessar o builder privado -->

<!-- Email.java -->
<!-- package com.fiap.mekano.domain.valueobject -->
<!-- public Email(String value) — único construtor, valida formato -->
<!-- email.getValue() — retorna String normalizada (lowercase) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar User.reconstitute() em mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java</name>
  <files>mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java</files>
  <read_first>
    - mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java (estado atual — ler antes de modificar)
    - mekano-domain/src/main/java/com/fiap/mekano/domain/valueobject/Email.java (confirmar construtor public Email(String value))
  </read_first>
  <action>
Adicionar o método estático `reconstitute()` dentro da classe `User`, logo após o método `create()` existente.

**Método a adicionar (colar após a chave de fechamento de `create()`):**

```java
/**
 * Factory method para reconstrução de um User a partir de dados persistidos.
 *
 * Diferença crítica em relação a {@link #create}: NÃO gera novo UUID nem novo timestamp.
 * Preserva exatamente os valores vindos do banco de dados.
 *
 * Usado exclusivamente por {@code UserEntityMapper.toDomain()} na camada infrastructure.
 *
 * @param id           UUID original do usuário (vindo do banco)
 * @param name         nome do usuário
 * @param emailValue   string do email (revalidada pelo construtor de Email)
 * @param passwordHash hash da senha já armazenado
 * @param createdAt    data/hora original de criação (vindo do banco)
 * @return instância de User com os valores exatos recebidos
 */
public static User reconstitute(UUID id, String name, String emailValue,
                                 String passwordHash, LocalDateTime createdAt) {
    return User.builder()
            .id(id)
            .name(name)
            .email(new Email(emailValue))
            .passwordHash(passwordHash)
            .createdAt(createdAt)
            .build();
}
```

**Regras de implementação:**
- Posição: dentro da classe `User`, após o método `create()` e antes do `}` de fechamento da classe
- NÃO modificar nenhum campo, anotação ou import existente
- NÃO adicionar novos imports (UUID, String, LocalDateTime e Email já estão importados)
- O builder `User.builder()` é acessível dentro de `User` mesmo com `@Builder(access = PRIVATE)` porque o método está na própria classe
  </action>
  <verify>
    <automated>./mvnw compile -pl mekano-domain -q &amp;&amp; grep -c "public static User reconstitute" mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java</automated>
  </verify>
  <done>
    - `grep "public static User reconstitute" mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java` retorna 1 match
    - `grep "UUID.randomUUID()" mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java` retorna exatamente 1 match (apenas dentro de `create()`, não em `reconstitute()`)
    - `./mvnw compile -pl mekano-domain` termina com `BUILD SUCCESS`
    - `grep "User.create\|User.reconstitute" mekano-domain/src/test/java` — os testes existentes da Fase 2 continuam passando (`./mvnw test -pl mekano-domain -q`)
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar UserEntity.java em mekano-infrastructure</name>
  <files>mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java</files>
  <read_first>
    - mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java (campos a mapear: id, name, email, passwordHash, createdAt)
    - .planning/phases/04-m-dulo-infrastructure/04-CONTEXT.md (seção D-01 — @Id UUID sem @GeneratedValue)
    - .planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md (Padrão 2 — UserEntity com PanacheEntityBase)
  </read_first>
  <action>
Criar o arquivo `mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java` com o conteúdo exato abaixo.

**Criar diretório se não existir:** `mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/`

```java
package com.fiap.mekano.infrastructure.entity;

import io.quarkus.hibernate.orm.panache.PanacheEntityBase;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Id;
import jakarta.persistence.Table;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

import java.time.LocalDateTime;
import java.util.UUID;

/**
 * Entidade JPA representando a tabela {@code users} no banco de dados.
 *
 * <p>Separada intencionalmente da entidade de domínio {@code User} (Clean Architecture).
 * Esta classe conhece JPA; a entidade de domínio não.
 *
 * <p>Estende {@code PanacheEntityBase} (não {@code PanacheEntity}) porque o ID é UUID,
 * não Long. {@code PanacheEntity} declara {@code @Id @GeneratedValue Long id} internamente,
 * o que conflita com UUID.
 *
 * <p>Sem {@code @GeneratedValue}: o UUID é gerado pelo domain em {@code User.create()} e
 * repassado aqui via mapper. Adicionar @GeneratedValue quebraria a invariância de identidade. (D-01)
 */
@Entity
@Table(name = "users")
@Getter
@Setter
@NoArgsConstructor
public class UserEntity extends PanacheEntityBase {

    /** Chave primária UUID. Sem @GeneratedValue — domain gera o UUID (D-01). */
    @Id
    UUID id;

    @Column(nullable = false)
    String name;

    /** Armazena o valor String do Email VO. Unique constraint garante unicidade. */
    @Column(unique = true, nullable = false)
    String email;

    @Column(name = "password_hash", nullable = false)
    String passwordHash;

    @Column(name = "created_at", nullable = false)
    LocalDateTime createdAt;
}
```

**Regras críticas:**
- NÃO importar nenhuma classe de `com.fiap.mekano.domain` — UserEntity é completamente independente do domain
- NÃO adicionar `@GeneratedValue` ao campo `id` (D-01)
- NÃO usar `PanacheEntity` (assumiria Long como ID) — usar `PanacheEntityBase`
- `@Column(name = "password_hash")` é obrigatório para mapear para o nome correto na tabela
- `@Column(name = "created_at")` é obrigatório para mapear para snake_case
  </action>
  <verify>
    <automated>./mvnw compile -pl mekano-infrastructure -am -q &amp;&amp; grep -c "PanacheEntityBase" mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java</automated>
  </verify>
  <done>
    - Arquivo `mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java` existe
    - `grep "@GeneratedValue" UserEntity.java` retorna 0 matches (confirmar ausência)
    - `grep "@Id" UserEntity.java` retorna exatamente 1 match
    - `grep "extends PanacheEntityBase" UserEntity.java` retorna 1 match
    - `grep "import com.fiap.mekano.domain" UserEntity.java` retorna 0 matches (sem imports do domain)
    - `grep "password_hash" UserEntity.java` retorna 1 match (coluna correta)
    - `./mvnw compile -pl mekano-infrastructure -am` termina com `BUILD SUCCESS`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Domain → Infrastructure | UserEntity não deve conhecer Domain — separação Clean Architecture |
| Banco → Domain | reconstitute() revalida email ao reconstruir do banco (new Email(emailValue)) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | UserEntity.id sem @GeneratedValue | mitigate | Código não tem @GeneratedValue (D-01); comentário Javadoc explica o risco de adicioná-lo |
| T-04-02-02 | Information Disclosure | User.reconstitute() revalida email | mitigate | `new Email(emailValue)` no reconstitute revalida o formato — dados corrompidos no banco lançam InvalidEmailException |
| T-04-02-03 | Spoofing | reconstitute() chamável externamente | accept | Método público por necessidade (chamado pelo mapper); nenhum segredo manipulado aqui — apenas dados estruturais |
</threat_model>

<verification>
```bash
# Verificar User.reconstitute() presente
grep "public static User reconstitute" mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java

# Verificar UserEntity sem @GeneratedValue e com PanacheEntityBase
grep "@GeneratedValue" mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java
# deve retornar vazio

grep "extends PanacheEntityBase" mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java
# deve retornar 1 match

# Verificar sem imports do domain em UserEntity
grep "import com.fiap.mekano.domain" mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java
# deve retornar vazio

# Build completo
./mvnw compile -pl mekano-infrastructure -am
```
</verification>

<success_criteria>
- `./mvnw compile -pl mekano-infrastructure -am` termina com `BUILD SUCCESS`
- `./mvnw test -pl mekano-domain` — testes existentes da Fase 2 passam (reconstitute não quebrou nada)
- `User.java` contém `public static User reconstitute` sem `UUID.randomUUID()` ou `LocalDateTime.now()`
- `UserEntity.java` contém `@Id UUID id` sem `@GeneratedValue`, extende `PanacheEntityBase`, sem imports do domain
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-m-dulo-infrastructure/04-02-SUMMARY.md`
</output>
