---
phase: 04-m-dulo-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - mekano-infrastructure/src/main/resources/db/migration/V1__create_users_table.sql
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapper.java
autonomous: true
requirements:
  - INF-03
  - INF-04
  - INF-05
must_haves:
  truths:
    - "V1__create_users_table.sql existe com naming exato (capital V, double underscore)"
    - "Tabela users tem colunas: id UUID PK, name VARCHAR NOT NULL, email VARCHAR UNIQUE NOT NULL, password_hash VARCHAR NOT NULL, created_at TIMESTAMP NOT NULL"
    - "UserEntityMapper.java tem @Mapper(componentModel = \"cdi\")"
    - "UserEntityMapper.toDomain() é default method chamando User.reconstitute()"
    - "UserEntityMapper.toEntity() é método abstrato (gerado pelo MapStruct)"
    - "default String emailToString(Email email) e default Email emailFromString(String value) presentes"
    - "./mvnw compile -pl mekano-infrastructure -am retorna BUILD SUCCESS"
  artifacts:
    - path: "mekano-infrastructure/src/main/resources/db/migration/V1__create_users_table.sql"
      provides: "Migration DDL criando tabela users"
      contains: "CREATE TABLE users"
    - path: "mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapper.java"
      provides: "Interface MapStruct CDI para conversão UserEntity ↔ User"
      contains: "@Mapper(componentModel = \"cdi\")"
  key_links:
    - from: "V1__create_users_table.sql coluna password_hash"
      to: "UserEntity.java @Column(name = \"password_hash\")"
      via: "Hibernate schema-management.strategy=validate verifica correspondência"
      pattern: "password_hash"
    - from: "UserEntityMapper.toDomain()"
      to: "User.reconstitute() (Plano 04-02)"
      via: "chamada direta ao factory method do domain"
      pattern: "User.reconstitute"
---

<objective>
Criar a migration Flyway `V1__create_users_table.sql` com DDL da tabela `users` e a interface `UserEntityMapper.java` com MapStruct CDI convertendo `UserEntity ↔ User` via default methods.

Purpose: A migration define o schema no banco; o mapper converte entre representação JPA e domínio. Esses dois artefatos são independentes entre si e do Plano 04-02 — executam em paralelo no Wave 2.
Output: `V1__create_users_table.sql` em `src/main/resources/db/migration/`. `UserEntityMapper.java` em `src/main/java/.../mapper/`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-m-dulo-infrastructure/04-CONTEXT.md
@.planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md

<interfaces>
<!-- UserEntity.java (criado no Plano 04-02 — ler se já existir, senão usar a spec) -->
<!-- Campos: UUID id, String name, String email, String passwordHash, LocalDateTime createdAt -->
<!-- @Column(name = "password_hash") e @Column(name = "created_at") já mapeados -->

<!-- User.reconstitute() (adicionado no Plano 04-02) -->
<!-- Assinatura: public static User reconstitute(UUID id, String name, String emailValue, String passwordHash, LocalDateTime createdAt) -->

<!-- Email.java -->
<!-- email.getValue() retorna String; new Email(String value) valida e cria -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar V1__create_users_table.sql em src/main/resources/db/migration/</name>
  <files>mekano-infrastructure/src/main/resources/db/migration/V1__create_users_table.sql</files>
  <read_first>
    - .planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md (Padrão 7 — Flyway naming convention)
    - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java (se existir — confirmar nome das colunas)
  </read_first>
  <action>
Criar o diretório `mekano-infrastructure/src/main/resources/db/migration/` se não existir.

Criar o arquivo `V1__create_users_table.sql` com o conteúdo exato abaixo.

**ATENÇÃO ao nome do arquivo:**
- `V` MAIÚSCULO (capital V) — `v` minúsculo faz a migration ser ignorada silenciosamente
- Dois underscores `__` (double underscore) — um underscore faz a migration ser ignorada silenciosamente
- Nome completo: `V1__create_users_table.sql`

```sql
-- V1__create_users_table.sql
-- Migration: criação da tabela users
-- Corresponde ao mapeamento JPA de UserEntity (INF-01, INF-04)

CREATE TABLE users
(
    id            UUID         NOT NULL,
    name          VARCHAR(255) NOT NULL,
    email         VARCHAR(255) NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at    TIMESTAMP    NOT NULL,
    CONSTRAINT pk_users PRIMARY KEY (id),
    CONSTRAINT uq_users_email UNIQUE (email)
);
```

**Por que `password_hash` e `created_at` com underscore:**
- `UserEntity` declara `@Column(name = "password_hash")` — migration DEVE usar o mesmo nome
- `UserEntity` declara `@Column(name = "created_at")` — migration DEVE usar o mesmo nome
- `quarkus.hibernate-orm.schema-management.strategy=validate` valida a correspondência no startup — coluna com nome diferente causa falha imediata

**Por que UUID NOT NULL sem DEFAULT:**
- PostgreSQL aceita `UUID` como tipo nativo
- O valor é sempre fornecido pelo domain (via `User.create()` → `UUID.randomUUID()`)
- Sem `DEFAULT gen_random_uuid()` — o domain é a única fonte de UUIDs
  </action>
  <verify>
    <automated>Test-Path "mekano-infrastructure/src/main/resources/db/migration/V1__create_users_table.sql"</automated>
  </verify>
  <done>
    - Arquivo `V1__create_users_table.sql` existe no path exato `mekano-infrastructure/src/main/resources/db/migration/`
    - `grep "CREATE TABLE users" mekano-infrastructure/src/main/resources/db/migration/V1__create_users_table.sql` retorna 1 match
    - `grep "password_hash" mekano-infrastructure/src/main/resources/db/migration/V1__create_users_table.sql` retorna 1 match
    - `grep "created_at" mekano-infrastructure/src/main/resources/db/migration/V1__create_users_table.sql` retorna 1 match
    - `grep "UNIQUE" mekano-infrastructure/src/main/resources/db/migration/V1__create_users_table.sql` retorna 1 match (constraint de email)
    - Nome do arquivo começa com `V1__` (V maiúsculo, dois underscores) — verificar com `ls` ou `dir`
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar UserEntityMapper.java em mekano-infrastructure</name>
  <files>mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapper.java</files>
  <read_first>
    - .planning/phases/04-m-dulo-infrastructure/04-CONTEXT.md (D-04 — default methods para Email VO)
    - .planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md (Padrão 3 — MapStruct com toDomain() como default method)
    - mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java (confirmar assinatura de reconstitute())
    - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java (confirmar getters disponíveis)
  </read_first>
  <action>
Criar o diretório `mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/` se não existir.

Criar o arquivo `UserEntityMapper.java` com o conteúdo exato abaixo.

```java
package com.fiap.mekano.infrastructure.mapper;

import com.fiap.mekano.domain.model.User;
import com.fiap.mekano.domain.valueobject.Email;
import com.fiap.mekano.infrastructure.entity.UserEntity;
import org.mapstruct.Mapper;

/**
 * MapStruct mapper CDI para conversão entre {@link UserEntity} (JPA) e {@link User} (domain).
 *
 * <p>Estratégia de conversão:
 * <ul>
 *   <li>{@code toEntity(User)} — gerado automaticamente pelo MapStruct (UserEntity tem @NoArgsConstructor + @Setter)</li>
 *   <li>{@code toDomain(UserEntity)} — implementado manualmente como default method porque
 *       {@code User} usa {@code @Builder(access = PRIVATE)} — MapStruct não consegue usar o builder diretamente (D-04)</li>
 *   <li>{@code emailToString(Email)} — conversão Email VO → String; detectada automaticamente por MapStruct ao gerar toEntity()</li>
 *   <li>{@code emailFromString(String)} — conversão inversa; não usada diretamente (toDomain é manual), mantida por completude</li>
 * </ul>
 *
 * <p>componentModel = "cdi": bean CDI gerenciado pelo Quarkus Arc (STATE.md Decision 4).
 * NUNCA usar "spring" neste projeto.
 */
@Mapper(componentModel = "cdi")
public interface UserEntityMapper {

    /**
     * Converte entidade de domínio para entidade JPA.
     * Gerado pelo MapStruct: usa @NoArgsConstructor + setters de UserEntity.
     * MapStruct detecta emailToString() automaticamente para mapear User.email (Email) → UserEntity.email (String).
     *
     * @param user entidade de domínio
     * @return entidade JPA pronta para persistência
     */
    UserEntity toEntity(User user);

    /**
     * Reconstrói entidade de domínio a partir da entidade JPA.
     *
     * Implementado como default method porque User.@Builder(access = PRIVATE) impede
     * que MapStruct use o builder externamente. User.reconstitute() tem acesso interno
     * ao builder privado e preserva os valores originais de id e createdAt (D-03, D-04).
     *
     * @param entity entidade JPA vinda do banco
     * @return instância de User com valores exatos do banco
     */
    default User toDomain(UserEntity entity) {
        return User.reconstitute(
                entity.getId(),
                entity.getName(),
                entity.getEmail(),
                entity.getPasswordHash(),
                entity.getCreatedAt()
        );
    }

    /**
     * Conversão auxiliar: Email VO → String.
     * Detectada automaticamente pelo MapStruct ao gerar toEntity().
     *
     * @param email Value Object Email
     * @return string do email normalizada (lowercase)
     */
    default String emailToString(Email email) {
        return email.getValue();
    }

    /**
     * Conversão auxiliar: String → Email VO.
     * Mantida para completude; não usada diretamente pois toDomain() é implementado manualmente.
     *
     * @param value string do email
     * @return Email VO validado
     */
    default Email emailFromString(String value) {
        return new Email(value);
    }
}
```

**Regras críticas:**
- `componentModel = "cdi"` obrigatório — NUNCA "spring" (STATE.md Decision 4)
- `toDomain()` DEVE ser `default method` — não remover a palavra `default` (caso contrário MapStruct tentaria gerar e falharia com @Builder privado)
- `toEntity()` NÃO tem implementação — MapStruct a gera em compile-time
- Os 4 parâmetros em `reconstitute()` devem corresponder exatamente à assinatura: `(entity.getId(), entity.getName(), entity.getEmail(), entity.getPasswordHash(), entity.getCreatedAt())`
  </action>
  <verify>
    <automated>./mvnw compile -pl mekano-infrastructure -am -q &amp;&amp; grep -c "componentModel = \"cdi\"" mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapper.java</automated>
  </verify>
  <done>
    - Arquivo `UserEntityMapper.java` existe no path `mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/`
    - `grep "@Mapper(componentModel = \"cdi\")" UserEntityMapper.java` retorna 1 match
    - `grep "default User toDomain" UserEntityMapper.java` retorna 1 match
    - `grep "User.reconstitute" UserEntityMapper.java` retorna 1 match
    - `grep "UserEntity toEntity" UserEntityMapper.java` retorna 1 match (sem "default")
    - `grep "default String emailToString" UserEntityMapper.java` retorna 1 match
    - `grep "default Email emailFromString" UserEntityMapper.java` retorna 1 match
    - `./mvnw compile -pl mekano-infrastructure -am` termina com `BUILD SUCCESS` (MapStruct gerou implementação de toEntity())
    - Verificar arquivo gerado: `ls mekano-infrastructure/target/generated-sources/annotations/com/fiap/mekano/infrastructure/mapper/` deve conter `UserEntityMapperImpl.java`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| SQL → JPA | Nomes de colunas no DDL devem corresponder exatamente aos @Column do UserEntity |
| Domain Email ↔ String | emailToString/emailFromString são pontos de conversão — email inválido no banco lança exceção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Flyway naming silencioso | mitigate | Nome `V1__create_users_table.sql` documentado e comentado; v minúsculo ou single underscore = migration ignorada |
| T-04-03-02 | Tampering | Email inválido no banco | mitigate | `emailFromString(value)` chama `new Email(value)` que revalida — email corrompido no banco falha rapidamente |
| T-04-03-03 | Information Disclosure | Sem password_hash em toEntity | accept | Mapper copia todos os campos incluindo passwordHash; campo controlado pelo domain que já recebeu BCrypt hash |
</threat_model>

<verification>
```bash
# Verificar migration
ls mekano-infrastructure/src/main/resources/db/migration/
# deve mostrar V1__create_users_table.sql

grep "CREATE TABLE users\|password_hash\|created_at\|UNIQUE" \
  mekano-infrastructure/src/main/resources/db/migration/V1__create_users_table.sql

# Verificar mapper
grep "@Mapper\|default User toDomain\|UserEntity toEntity\|emailToString\|emailFromString" \
  mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapper.java

# Verificar geração MapStruct
./mvnw compile -pl mekano-infrastructure -am
ls mekano-infrastructure/target/generated-sources/annotations/com/fiap/mekano/infrastructure/mapper/
```
</verification>

<success_criteria>
- `V1__create_users_table.sql` existe com nome correto (V maiúsculo, double underscore)
- SQL cria tabela `users` com colunas `password_hash` e `created_at` em snake_case (correspondendo a `@Column` do UserEntity)
- `UserEntityMapper.java` tem `@Mapper(componentModel = "cdi")`, `default User toDomain()` e `UserEntity toEntity()` abstrato
- `./mvnw compile -pl mekano-infrastructure -am` BUILD SUCCESS com `UserEntityMapperImpl.java` gerado em `target/generated-sources`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-m-dulo-infrastructure/04-03-SUMMARY.md`
</output>
