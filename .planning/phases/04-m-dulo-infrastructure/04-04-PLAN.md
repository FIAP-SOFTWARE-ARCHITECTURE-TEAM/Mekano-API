---
phase: 04-m-dulo-infrastructure
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java
autonomous: true
requirements:
  - INF-02
must_haves:
  truths:
    - "UserRepositoryImpl é @ApplicationScoped e implementa UserRepositoryPort"
    - "UserRepositoryImpl extends PanacheRepositoryBase<UserEntity, UUID>"
    - "save() é @Transactional e usa persist(entity) + flush() (D-02)"
    - "save() retorna User (mapper.toDomain(entity)) — não void"
    - "findById() retorna Optional<User> via findByIdOptional().map(mapper::toDomain)"
    - "findByEmail() retorna Optional<User> via find(\"email\", email).firstResultOptional()"
    - "existsByEmail() retorna boolean via count(\"email\", email) > 0"
    - "@Transactional está APENAS em save() — não na interface UserRepositoryPort"
    - "./mvnw compile -pl mekano-infrastructure -am retorna BUILD SUCCESS"
  artifacts:
    - path: "mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java"
      provides: "Implementação concreta de UserRepositoryPort usando Panache"
      contains: "PanacheRepositoryBase<UserEntity, UUID>"
  key_links:
    - from: "UserRepositoryImpl.save()"
      to: "UserEntityMapper.toEntity() + toDomain()"
      via: "@Inject UserEntityMapper mapper"
      pattern: "mapper.toEntity\|mapper.toDomain"
    - from: "UserRepositoryImpl"
      to: "UserRepositoryPort (domain)"
      via: "implements UserRepositoryPort"
      pattern: "implements.*UserRepositoryPort"
---

<objective>
Criar `UserRepositoryImpl.java` como bean CDI `@ApplicationScoped` que implementa `UserRepositoryPort` usando `PanacheRepositoryBase<UserEntity, UUID>`, com `@Transactional` apenas no método `save()`.

Purpose: É o adaptador que conecta o domain (interface `UserRepositoryPort`) ao banco via Panache. `persist() + flush()` garante captura imediata de constraint violations (D-02). `@Transactional` apenas em `save()` — operações de leitura não necessitam de transação explícita.
Output: `UserRepositoryImpl.java` compilado como bean CDI com os 4 métodos do port implementados.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-m-dulo-infrastructure/04-CONTEXT.md
@.planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md
@.planning/phases/04-m-dulo-infrastructure/04-02-SUMMARY.md
@.planning/phases/04-m-dulo-infrastructure/04-03-SUMMARY.md

<interfaces>
<!-- UserRepositoryPort — interface do domain que DEVE ser implementada -->
<!-- Métodos obrigatórios: -->
<!--   User save(User user) -->
<!--   Optional<User> findById(UUID id) -->
<!--   Optional<User> findByEmail(String email) -->
<!--   boolean existsByEmail(String email) -->

<!-- UserEntity — campos disponíveis via Lombok @Getter/@Setter -->
<!--   UUID getId(), String getName(), String getEmail(), String getPasswordHash(), LocalDateTime getCreatedAt() -->

<!-- UserEntityMapper — bean CDI (@Inject) -->
<!--   UserEntity toEntity(User user) -->
<!--   User toDomain(UserEntity entity) -->

<!-- PanacheRepositoryBase<E, Id> — métodos herdados relevantes: -->
<!--   void persist(E entity) -->
<!--   void flush() -->
<!--   Optional<E> findByIdOptional(Id id) -->
<!--   PanacheQuery<E> find(String query, Object... params) -->
<!--   long count(String query, Object... params) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar UserRepositoryImpl.java em mekano-infrastructure</name>
  <files>mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java</files>
  <read_first>
    - mekano-domain/src/main/java/com/fiap/mekano/domain/port/out/UserRepositoryPort.java (contrato exato dos 4 métodos)
    - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/entity/UserEntity.java (campos disponíveis)
    - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/mapper/UserEntityMapper.java (métodos toEntity/toDomain)
    - .planning/phases/04-m-dulo-infrastructure/04-CONTEXT.md (D-02 — persist+flush+@Transactional)
    - .planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md (Padrão 5 — UserRepositoryImpl completo)
  </read_first>
  <action>
Criar o diretório `mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/` se não existir.

Criar o arquivo `UserRepositoryImpl.java` com o conteúdo exato abaixo.

```java
package com.fiap.mekano.infrastructure.repository;

import com.fiap.mekano.domain.model.User;
import com.fiap.mekano.domain.port.out.UserRepositoryPort;
import com.fiap.mekano.infrastructure.entity.UserEntity;
import com.fiap.mekano.infrastructure.mapper.UserEntityMapper;
import io.quarkus.hibernate.orm.panache.PanacheRepositoryBase;
import jakarta.enterprise.context.ApplicationScoped;
import jakarta.inject.Inject;
import jakarta.transaction.Transactional;

import java.util.Optional;
import java.util.UUID;

/**
 * Implementação concreta de {@link UserRepositoryPort} usando Quarkus Panache.
 *
 * <p>Padrão Panache Repository: esta classe é um bean CDI separado da entidade.
 * Estende {@code PanacheRepositoryBase<UserEntity, UUID>} (não {@code PanacheRepository<UserEntity>}
 * que assume Long como tipo do ID).
 *
 * <p>Regras de transação:
 * <ul>
 *   <li>{@code save()} — {@code @Transactional}: garante que persist()+flush() executam em uma
 *       transação e que constraint violations são capturadas imediatamente (D-02)</li>
 *   <li>Métodos de leitura — sem {@code @Transactional}: Quarkus/JPA gerencia automaticamente
 *       se houver transação ativa no chamador</li>
 * </ul>
 */
@ApplicationScoped
public class UserRepositoryImpl
        implements PanacheRepositoryBase<UserEntity, UUID>, UserRepositoryPort {

    @Inject
    UserEntityMapper mapper;

    /**
     * Persiste um novo usuário ou atualiza um existente.
     *
     * <p>{@code flush()} após {@code persist()} garante execução imediata do INSERT,
     * capturando violações de constraint (ex: email duplicado) dentro da transação
     * antes de retornar ao chamador (D-02).
     *
     * @param user entidade de domínio a ser salva
     * @return entidade de domínio reconstruída após persistência
     */
    @Override
    @Transactional
    public User save(User user) {
        UserEntity entity = mapper.toEntity(user);
        persist(entity);
        flush();
        return mapper.toDomain(entity);
    }

    /**
     * Busca usuário pelo UUID.
     *
     * @param id UUID do usuário
     * @return Optional com o User se encontrado, ou Optional.empty()
     */
    @Override
    public Optional<User> findById(UUID id) {
        return findByIdOptional(id).map(mapper::toDomain);
    }

    /**
     * Busca usuário pelo email.
     *
     * <p>Usa Panache HQL abreviado: {@code find("email", email)} equivale a
     * {@code SELECT u FROM UserEntity u WHERE u.email = ?1}.
     *
     * @param email string do email (normalizada)
     * @return Optional com o User se encontrado, ou Optional.empty()
     */
    @Override
    public Optional<User> findByEmail(String email) {
        return find("email", email).firstResultOptional().map(mapper::toDomain);
    }

    /**
     * Verifica existência de usuário pelo email usando COUNT.
     *
     * <p>Mais eficiente que {@code findByEmail().isPresent()} — não carrega a entidade completa.
     *
     * @param email string do email
     * @return true se existe usuário com esse email, false caso contrário
     */
    @Override
    public boolean existsByEmail(String email) {
        return count("email", email) > 0;
    }
}
```

**Regras críticas:**
- `@ApplicationScoped` obrigatório — bean CDI para injeção
- `implements PanacheRepositoryBase<UserEntity, UUID>` (não PanacheRepository<UserEntity> que assume Long)
- `implements UserRepositoryPort` — implementa o contrato do domain
- `@Transactional` SOMENTE em `save()` — NÃO na interface, NÃO nos métodos de leitura (STATE.md Decision 5)
- `persist(entity)` seguido de `flush()` — ambos obrigatórios em `save()` (D-02)
- `save()` retorna `User` (não void) — contrato do `UserRepositoryPort`
- NÃO importar `jakarta.ws.rs` ou qualquer classe de `mekano-application`
  </action>
  <verify>
    <automated>./mvnw compile -pl mekano-infrastructure -am -q &amp;&amp; grep -c "PanacheRepositoryBase" mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java</automated>
  </verify>
  <done>
    - Arquivo `UserRepositoryImpl.java` existe no path correto
    - `grep "@ApplicationScoped" UserRepositoryImpl.java` retorna 1 match
    - `grep "implements PanacheRepositoryBase<UserEntity, UUID>, UserRepositoryPort" UserRepositoryImpl.java` retorna 1 match
    - `grep "@Transactional" UserRepositoryImpl.java` retorna exatamente 1 match (apenas em save())
    - `grep "persist(entity)" UserRepositoryImpl.java` retorna 1 match
    - `grep "flush()" UserRepositoryImpl.java` retorna 1 match
    - `grep "mapper.toEntity\|mapper.toDomain\|mapper::toDomain" UserRepositoryImpl.java` retorna 3+ matches
    - `grep "import jakarta.ws.rs\|import com.fiap.mekano.application" UserRepositoryImpl.java` retorna 0 matches
    - `./mvnw compile -pl mekano-infrastructure -am` termina com `BUILD SUCCESS`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Application → Infrastructure | UserRepositoryImpl recebe User do domain; @Transactional garante atomicidade |
| Infrastructure → Database | flush() força execução SQL imediata — constraint violations surfaceiam dentro da transação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | @Transactional em save() apenas | mitigate | @Transactional explícito apenas em save(); comentário inline explica a decisão; métodos de leitura sem transação por design |
| T-04-04-02 | Denial | Constraint violation silenciosa sem flush() | mitigate | flush() após persist() garante que PersistenceException é lançada dentro da mesma transação; sem flush, a exception poderia vir em momento inesperado |
| T-04-04-03 | Elevation of Privilege | @ApplicationScoped expõe bean para injeção | accept | Escopo CDI padrão; bean apenas manipula User domain objects sem dados privilegiados além do passwordHash (já hash BCrypt) |
</threat_model>

<verification>
```bash
# Verificar anotações e implementação
grep "@ApplicationScoped\|PanacheRepositoryBase\|UserRepositoryPort\|@Transactional\|persist\|flush" \
  mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java

# Verificar @Transactional em save() apenas (deve retornar 1)
grep -c "@Transactional" \
  mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java

# Verificar sem imports proibidos
grep "import jakarta.ws.rs\|import com.fiap.mekano.application" \
  mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java
# deve retornar vazio

# Build completo
./mvnw compile -pl mekano-infrastructure -am
```
</verification>

<success_criteria>
- `./mvnw compile -pl mekano-infrastructure -am` termina com `BUILD SUCCESS`
- `UserRepositoryImpl.java` é `@ApplicationScoped`, extends `PanacheRepositoryBase<UserEntity, UUID>`, implements `UserRepositoryPort`
- `@Transactional` aparece exatamente 1 vez (apenas em `save()`)
- `save()` chama `persist(entity)` E `flush()` em sequência
- Sem imports de `jakarta.ws.rs` ou `mekano-application`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-m-dulo-infrastructure/04-04-SUMMARY.md`
</output>
