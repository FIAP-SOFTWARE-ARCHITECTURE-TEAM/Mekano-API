---
phase: 04-m-dulo-infrastructure
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - mekano-infrastructure/src/test/resources/application.properties
  - mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImplTest.java
  - mekano-adapter/src/main/resources/application.properties
autonomous: true
requirements:
  - INF-05
  - INF-07
must_haves:
  truths:
    - "mekano-infrastructure/src/test/resources/application.properties existe e NÃO contém quarkus.datasource.jdbc.url"
    - "application.properties tem quarkus.flyway.migrate-at-start=true"
    - "application.properties tem %test.quarkus.flyway.clean-at-start=true"
    - "application.properties tem quarkus.hibernate-orm.schema-management.strategy=validate"
    - "UserRepositoryImplTest.java tem @QuarkusTest na classe"
    - "Cada método de teste tem @TestTransaction para rollback automático"
    - "Testes cobrem: save+findByEmail, existsByEmail false, round-trip sem campos null"
    - "./mvnw test -pl mekano-infrastructure -am retorna BUILD SUCCESS com DevServices ativo"
  artifacts:
    - path: "mekano-infrastructure/src/test/resources/application.properties"
      provides: "Configuração mínima para DevServices PostgreSQL em testes do módulo infrastructure"
      contains: "quarkus.flyway.migrate-at-start=true"
    - path: "mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImplTest.java"
      provides: "Testes de integração @QuarkusTest para UserRepositoryImpl"
      contains: "@QuarkusTest"
    - path: "mekano-adapter/src/main/resources/application.properties"
      provides: "Propriedades Flyway/Hibernate de teste para execução completa do adapter (Fase 5)"
      contains: "%test.quarkus.flyway.clean-at-start=true"
  key_links:
    - from: "application.properties sem quarkus.datasource.jdbc.url"
      to: "Quarkus DevServices (Testcontainers)"
      via: "ausência de jdbc.url ativa DevServices automaticamente"
      pattern: "quarkus.datasource.db-kind"
    - from: "@TestTransaction em cada teste"
      to: "banco de dados limpo entre testes"
      via: "rollback automático após cada método"
      pattern: "@TestTransaction"
---

<objective>
Criar `application.properties` de testes com DevServices ativado via ausência de `jdbc.url`, e `UserRepositoryImplTest.java` com `@QuarkusTest` + `@TestTransaction` cobrindo os 4 métodos do repositório.

Purpose: O `mekano-adapter/src/main/resources/application.properties` **não está no classpath** quando `./mvnw test -pl mekano-infrastructure -am` executa. O módulo infrastructure precisa de seu próprio arquivo de configuração de testes. Sem `jdbc.url`, Quarkus ativa DevServices (PostgreSQL via Testcontainers).
Output: application.properties em `src/test/resources/`. `UserRepositoryImplTest.java` com 3 testes de integração + rollback automático.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-m-dulo-infrastructure/04-CONTEXT.md
@.planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md
@.planning/phases/04-m-dulo-infrastructure/04-04-SUMMARY.md

<interfaces>
<!-- UserRepositoryImpl — bean CDI @ApplicationScoped para @Inject nos testes -->
<!-- Métodos: User save(User user), Optional<User> findByEmail(String), boolean existsByEmail(String) -->

<!-- User.create() — factory method do domain -->
<!-- Assinatura: public static User create(String name, String emailValue, String passwordHash) -->
<!-- Retorna User com UUID e createdAt gerados automaticamente -->

<!-- @QuarkusTest — sobe container Quarkus + DevServices para o teste -->
<!-- @TestTransaction — rollback automático após cada @Test method -->
<!-- @Inject — injeta beans CDI nos campos do teste -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar mekano-infrastructure/src/test/resources/application.properties</name>
  <files>mekano-infrastructure/src/test/resources/application.properties</files>
  <read_first>
    - .planning/phases/04-m-dulo-infrastructure/04-CONTEXT.md (D-05 — conteúdo exato do arquivo)
    - .planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md (Padrão 8 — DevServices para testes)
    - mekano-adapter/src/main/resources/application.properties (referência — mas NÃO copiar jdbc.url)
  </read_first>
  <action>
Criar o diretório `mekano-infrastructure/src/test/resources/` se não existir.

Criar o arquivo `mekano-infrastructure/src/test/resources/application.properties` com o conteúdo exato abaixo.

```properties
# =============================================================================
# mekano-infrastructure — Test Configuration
# =============================================================================
# Este arquivo existe porque mekano-adapter/src/main/resources/application.properties
# NÃO está no classpath quando ./mvnw test -pl mekano-infrastructure -am executa.
# Cada módulo deve ser autossuficiente para seus próprios @QuarkusTest. (D-05)

# Tipo do banco — necessário para DevServices saber qual container subir
quarkus.datasource.db-kind=postgresql

# CRÍTICO: ausência de quarkus.datasource.jdbc.url → DevServices ativa automaticamente
# Quarkus sobe PostgreSQL via Testcontainers (Docker) sem configuração adicional.
# NÃO adicionar quarkus.datasource.jdbc.url aqui.

# Flyway: migrar automaticamente ao subir o contexto de teste (INF-05)
quarkus.flyway.migrate-at-start=true

# Hibernate: validar que o schema criado pelo Flyway corresponde ao mapeamento JPA
# Se UserEntity tiver campo sem coluna correspondente → startup falha imediatamente
quarkus.hibernate-orm.schema-management.strategy=validate

# Perfil test: limpa o schema antes de cada test run para garantir idempotência
%test.quarkus.flyway.clean-at-start=true

# Perfil test: imagem PostgreSQL específica para reproducibilidade
%test.quarkus.datasource.devservices.image-name=docker.io/library/postgres:16-alpine
```

**Regras críticas:**
- NÃO incluir `quarkus.datasource.jdbc.url` — a ausência é intencional e necessária para DevServices ativar
- `quarkus.datasource.db-kind=postgresql` é necessário para DevServices saber qual container subir
- `%test.quarkus.flyway.clean-at-start=true` usa prefixo de perfil `%test` — aplicado apenas nos testes
  </action>
  <verify>
    <automated>Test-Path "mekano-infrastructure/src/test/resources/application.properties" &amp;&amp; (Select-String -Path "mekano-infrastructure/src/test/resources/application.properties" -Pattern "jdbc.url" -Quiet) -eq $false</automated>
  </verify>
  <done>
    - Arquivo `mekano-infrastructure/src/test/resources/application.properties` existe
    - `grep "quarkus.datasource.jdbc.url" mekano-infrastructure/src/test/resources/application.properties` retorna 0 matches (ausência confirmada)
    - `grep "quarkus.flyway.migrate-at-start=true" mekano-infrastructure/src/test/resources/application.properties` retorna 1 match
    - `grep "schema-management.strategy=validate" mekano-infrastructure/src/test/resources/application.properties` retorna 1 match
    - `grep "clean-at-start=true" mekano-infrastructure/src/test/resources/application.properties` retorna 1 match
    - `grep "quarkus.datasource.db-kind=postgresql" mekano-infrastructure/src/test/resources/application.properties` retorna 1 match
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar UserRepositoryImplTest.java com @QuarkusTest + @TestTransaction</name>
  <files>mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImplTest.java</files>
  <read_first>
    - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImpl.java (métodos disponíveis)
    - mekano-domain/src/main/java/com/fiap/mekano/domain/model/User.java (User.create() factory method)
    - .planning/phases/04-m-dulo-infrastructure/04-RESEARCH.md (Padrão 9 — @QuarkusTest + @TestTransaction)
  </read_first>
  <action>
Criar o diretório `mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/` se não existir.

Criar o arquivo `UserRepositoryImplTest.java` com o conteúdo exato abaixo.

```java
package com.fiap.mekano.infrastructure.repository;

import com.fiap.mekano.domain.model.User;
import io.quarkus.test.TestTransaction;
import io.quarkus.test.junit.QuarkusTest;
import jakarta.inject.Inject;
import org.junit.jupiter.api.Test;

import java.util.Optional;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Testes de integração para {@link UserRepositoryImpl}.
 *
 * <p>Usa {@code @QuarkusTest}: sobe o container Quarkus completo com DevServices
 * (PostgreSQL via Testcontainers), executa migrations Flyway e valida o schema JPA.
 *
 * <p>Usa {@code @TestTransaction} em cada método: abre transação antes do teste e
 * faz rollback automático ao final — banco fica limpo para o próximo teste sem
 * necessidade de setup/teardown manual.
 *
 * <p>Pré-requisito: Docker deve estar disponível para DevServices subir o container PostgreSQL.
 */
@QuarkusTest
class UserRepositoryImplTest {

    @Inject
    UserRepositoryImpl repository;

    @Test
    @TestTransaction
    void save_devePersistirERetornarUserComEmailCorreto() {
        // Arrange
        User user = User.create("João Silva", "joao@fiap.br", "$2a$10$hashbcrypt");

        // Act
        User salvo = repository.save(user);

        // Assert — buscar no banco confirma persistência real
        Optional<User> encontrado = repository.findByEmail("joao@fiap.br");
        assertThat(encontrado).isPresent();
        assertThat(encontrado.get().getEmail().getValue()).isEqualTo("joao@fiap.br");
        assertThat(salvo.getId()).isEqualTo(user.getId());
    }

    @Test
    @TestTransaction
    void existsByEmail_deveRetornarFalse_quandoEmailNaoExiste() {
        // Act
        boolean existe = repository.existsByEmail("naoexiste@fiap.br");

        // Assert
        assertThat(existe).isFalse();
    }

    @Test
    @TestTransaction
    void save_deveRetornarUserSemCamposNull_roundTrip() {
        // Arrange
        User original = User.create("Maria Oliveira", "maria@fiap.br", "$2a$10$hash");

        // Act
        User salvo = repository.save(original);

        // Assert — todos os campos devem estar presentes após round-trip entity↔domain
        assertThat(salvo.getId()).isNotNull();
        assertThat(salvo.getId()).isEqualTo(original.getId());
        assertThat(salvo.getName()).isEqualTo("Maria Oliveira");
        assertThat(salvo.getEmail()).isNotNull();
        assertThat(salvo.getEmail().getValue()).isEqualTo("maria@fiap.br");
        assertThat(salvo.getPasswordHash()).isNotNull();
        assertThat(salvo.getPasswordHash()).isEqualTo("$2a$10$hash");
        assertThat(salvo.getCreatedAt()).isNotNull();
        assertThat(salvo.getCreatedAt()).isEqualTo(original.getCreatedAt());
    }
}
```

**Regras críticas:**
- `@QuarkusTest` obrigatório na classe — sobe contexto CDI + DevServices
- `@TestTransaction` obrigatório em CADA método `@Test` (não na classe) — rollback automático
- `@Inject UserRepositoryImpl` (tipo concreto) — não UserRepositoryPort (para acessar diretamente)
- Usar `assertThat` do AssertJ (já disponível via quarkus-junit5 transitivamente)
- Não usar `@BeforeEach` para limpar dados — `@TestTransaction` cuida disso automaticamente
- O `save()` chama `persist()+flush()` internamente (D-02) — o teste não precisa gerenciar transação manualmente
  </action>
  <verify>
    <automated>./mvnw test -pl mekano-infrastructure -am 2>&amp;1 | Select-String "Tests run|BUILD"</automated>
  </verify>
  <done>
    - Arquivo `UserRepositoryImplTest.java` existe no path correto
    - `grep "@QuarkusTest" UserRepositoryImplTest.java` retorna 1 match (na classe)
    - `grep "@TestTransaction" UserRepositoryImplTest.java` retorna 3 matches (um por método de teste)
    - `grep "@Test" UserRepositoryImplTest.java` retorna 3 matches
    - `./mvnw test -pl mekano-infrastructure -am` termina com `BUILD SUCCESS`
    - Output do Maven mostra `Tests run: 3, Failures: 0, Errors: 0, Skipped: 0`
  </done>
</task>

<task type="auto">
  <name>Task 3: Complementar mekano-adapter/src/main/resources/application.properties com propriedades de teste Flyway/Hibernate</name>
  <files>mekano-adapter/src/main/resources/application.properties</files>
  <read_first>
    - mekano-adapter/src/main/resources/application.properties (estado atual — já tem migrate-at-start, %dev/%prod hibernate, %test devservices)
    - .planning/phases/04-m-dulo-infrastructure/04-CONTEXT.md (escopo item 8)
  </read_first>
  <action>
Adicionar as duas propriedades ausentes ao final do arquivo `mekano-adapter/src/main/resources/application.properties`:

```properties
# --- Perfil %test: limpa schema antes de cada test run (idempotência em testes de integração) ---
%test.quarkus.flyway.clean-at-start=true

# --- Perfil %test: validar schema JPA em testes (detecta divergência entre UserEntity e migration) ---
%test.quarkus.hibernate-orm.schema-management.strategy=validate
```

**Regras:**
- Adicionar ao FINAL do arquivo, após a linha `quarkus.swagger-ui.always-include=true`
- NÃO remover ou modificar nenhuma propriedade existente
- Verificar antes se as linhas já existem para evitar duplicidade (`grep "%test.quarkus.flyway.clean-at-start" mekano-adapter/src/main/resources/application.properties`)
  </action>
  <verify>
    <automated>grep -c "test.quarkus.flyway.clean-at-start" mekano-adapter/src/main/resources/application.properties</automated>
  </verify>
  <done>
    - `grep "%test.quarkus.flyway.clean-at-start=true" mekano-adapter/src/main/resources/application.properties` retorna 1 match
    - `grep "%test.quarkus.hibernate-orm.schema-management.strategy=validate" mekano-adapter/src/main/resources/application.properties` retorna 1 match
    - `./mvnw compile -pl mekano-adapter -am -q` termina com `BUILD SUCCESS` (sem erros de parsing no properties)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Test → DevServices | Sem jdbc.url hardcoded nos testes — DevServices gerencia o container PostgreSQL |
| Test → Database | @TestTransaction garante isolamento entre testes — sem dados persistindo entre runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | application.properties sem credenciais hardcoded | accept | DevServices gerencia credenciais automaticamente; arquivo não contém senhas reais |
| T-04-05-02 | Denial | DevServices requer Docker disponível | accept | Pré-requisito documentado; sem Docker o teste falha com mensagem clara do Testcontainers |
| T-04-05-03 | Tampering | %test.quarkus.flyway.clean-at-start=true | mitigate | Limpa apenas em perfil test, nunca em dev/prod; prefixo %test garante escopo correto |
</threat_model>

<verification>
```bash
# Verificar application.properties sem jdbc.url
grep "jdbc.url" mekano-infrastructure/src/test/resources/application.properties
# deve retornar vazio

# Verificar chaves obrigatórias
grep "migrate-at-start\|schema-management\|clean-at-start\|db-kind" \
  mekano-infrastructure/src/test/resources/application.properties

# Verificar testes
grep "@QuarkusTest\|@TestTransaction\|@Test" \
  mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/repository/UserRepositoryImplTest.java

# Executar testes de integração (requer Docker)
./mvnw test -pl mekano-infrastructure -am
```
</verification>

<success_criteria>
- `application.properties` existe em `src/test/resources/` sem `jdbc.url` e com as 4 chaves obrigatórias
- `UserRepositoryImplTest.java` tem `@QuarkusTest` na classe, `@TestTransaction` em cada um dos 3 testes
- `./mvnw test -pl mekano-infrastructure -am` termina com `BUILD SUCCESS` e `Tests run: 3, Failures: 0`
- DevServices sobe PostgreSQL automaticamente (log mostra `Started UserRepositoryImplTest` com banco real)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-m-dulo-infrastructure/04-05-SUMMARY.md`
</output>
