---
phase: 01-esqueleto-maven-multi-modulo
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01-PLAN.md"
files_modified:
  - mekano-domain/pom.xml
  - mekano-application/pom.xml
  - mekano-infrastructure/pom.xml
  - mekano-adapter/pom.xml
  - mekano-domain/src/main/java/com/fiap/mekano/domain/.gitkeep
  - mekano-domain/src/test/java/com/fiap/mekano/domain/.gitkeep
  - mekano-application/src/main/java/com/fiap/mekano/application/.gitkeep
  - mekano-application/src/test/java/com/fiap/mekano/application/.gitkeep
  - mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/.gitkeep
  - mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/.gitkeep
  - mekano-adapter/src/main/java/com/fiap/mekano/adapter/.gitkeep
  - mekano-adapter/src/test/java/com/fiap/mekano/adapter/.gitkeep
  - mekano-adapter/src/main/resources/.gitkeep
autonomous: true
requirements:
  - MOD-02
  - MOD-03
  - MOD-04
  - MOD-05
  - MOD-06
  - MOD-07
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04
  - EXT-05
  - EXT-10

must_haves:
  truths:
    - "mekano-domain/pom.xml tem packaging=jar, lombok provided+optional, zero outras deps de runtime"
    - "mekano-application/pom.xml tem packaging=jar, depende de domain, jandex-maven-plugin configurado, quarkus-arc para CDI"
    - "mekano-infrastructure/pom.xml tem packaging=jar, depende de domain, jandex-maven-plugin configurado"
    - "mekano-adapter/pom.xml tem packaging=quarkus e é o ÚNICO módulo com quarkus-maven-plugin"
    - "mekano-adapter/pom.xml lista todas as 6 extensões: quarkus-rest-jackson, hibernate-orm-panache, flyway, smallrye-openapi, hibernate-validator, jdbc-postgresql"
    - "Cada módulo tem diretório src/main/java e src/test/java com .gitkeep"
    - "./mvnw clean install -DskipTests compila todos os 4 módulos com BUILD SUCCESS"
  artifacts:
    - path: "mekano-domain/pom.xml"
      provides: "Módulo domain — packaging jar, apenas lombok"
      contains: "<packaging>jar</packaging>"
    - path: "mekano-application/pom.xml"
      provides: "Módulo application — packaging jar, domain + arc + jandex"
      contains: "jandex-maven-plugin"
    - path: "mekano-infrastructure/pom.xml"
      provides: "Módulo infrastructure — packaging jar, domain + jandex"
      contains: "jandex-maven-plugin"
    - path: "mekano-adapter/pom.xml"
      provides: "Módulo adapter — packaging quarkus, quarkus-maven-plugin, todas extensões"
      contains: "quarkus-rest-jackson"
  key_links:
    - from: "mekano-domain/pom.xml"
      to: "pom.xml (root)"
      via: "<parent> section"
      pattern: "<artifactId>mekano</artifactId>"
    - from: "mekano-adapter/pom.xml"
      to: "quarkus-maven-plugin"
      via: "<build><plugins>"
      pattern: "quarkus-maven-plugin.*extensions.*true"
    - from: "mekano-application, mekano-infrastructure, mekano-adapter"
      to: "META-INF/jandex.idx"
      via: "jandex-maven-plugin execution make-index"
      pattern: "make-index"
---

<objective>
Criar os quatro `pom.xml` dos sub-módulos e a estrutura de diretórios Java de cada módulo.

**Por que este plano:** Após o root POM estar correto (Plano 01), os sub-módulos precisam existir para o build compilar. Cada módulo tem regras rígidas de dependência (Clean Architecture) que são *enforced* aqui no nível do pom.xml.

**Output:**
- 4 arquivos `pom.xml` de sub-módulos com regras corretas de dependência
- Estrutura de diretórios `src/main/java` e `src/test/java` com `.gitkeep` em cada módulo
- `./mvnw clean install -DskipTests` compilando com BUILD SUCCESS
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/01-esqueleto-maven-multi-modulo/01-RESEARCH.md
@.planning/phases/01-esqueleto-maven-multi-modulo/01-01-SUMMARY.md

<interfaces>
<!-- Contratos dos POMs de módulo conforme RESEARCH.md verificado -->
<!-- Root POM parent a referenciar em todos os módulos:
  groupId: com.fiap
  artifactId: mekano
  version: 1.0.0-SNAPSHOT
-->
<!-- Versões gerenciadas no root (NÃO declarar version nos módulos filhos):
  - quarkus-bom → gerencia quarkus-arc, quarkus-rest-jackson, quarkus-hibernate-orm-panache,
                   quarkus-flyway, quarkus-smallrye-openapi, quarkus-hibernate-validator,
                   quarkus-jdbc-postgresql, quarkus-junit5, rest-assured
  - mapstruct 1.6.3 → via ${mapstruct.version}
  - lombok 1.18.36 → via ${lombok.version}
  - jandex-maven-plugin 3.5.3 → via ${jandex.version} em pluginManagement
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar POMs dos módulos domain, application e infrastructure</name>
  <files>
    mekano-domain/pom.xml
    mekano-application/pom.xml
    mekano-infrastructure/pom.xml
  </files>
  <read_first>
    - pom.xml — confirmar que root tem packaging=pom e módulos declarados (pré-requisito do Plano 01)
    - .planning/phases/01-esqueleto-maven-multi-modulo/01-RESEARCH.md — seções "Padrão 2", "Padrão 3", "Padrão 4"
    - .planning/REQUIREMENTS.md — MOD-02, MOD-03, MOD-04, MOD-07
  </read_first>
  <action>
Criar os três arquivos pom.xml a seguir. Os diretórios `mekano-domain/`, `mekano-application/`, `mekano-infrastructure/` não existem ainda — criar junto com os arquivos.

---

**Arquivo 1: `mekano-domain/pom.xml`**

Regras (MOD-02):
- packaging=jar
- parent aponta para root
- SOMENTE lombok como dependência (provided + optional)
- SEM jandex (domain não tem beans CDI)
- SEM quarkus-arc ou qualquer extensão Quarkus

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.fiap</groupId>
        <artifactId>mekano</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>mekano-domain</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <!-- Lombok: compile-only — geração de boilerplate sem vazar para dependentes -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
```

---

**Arquivo 2: `mekano-application/pom.xml`**

Regras (MOD-03, MOD-07):
- packaging=jar
- Depende de mekano-domain (compile)
- quarkus-arc para @ApplicationScoped nos use cases
- jandex-maven-plugin com execution make-index (CDI discovery — Gotcha 2)
- lombok provided+optional

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.fiap</groupId>
        <artifactId>mekano</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>mekano-application</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>com.fiap</groupId>
            <artifactId>mekano-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- CDI para @ApplicationScoped nos use cases (APP-01) -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- OBRIGATÓRIO: CDI beans (@ApplicationScoped) não seriam descobertos sem este index (MOD-07, Gotcha 2) -->
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>make-index</id>
                        <goals>
                            <goal>jandex</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
```

---

**Arquivo 3: `mekano-infrastructure/pom.xml`**

Regras (MOD-04, MOD-07):
- packaging=jar
- Depende SOMENTE de mekano-domain (NÃO depende de mekano-application)
- jandex-maven-plugin com execution make-index
- lombok provided+optional
- SEM dependências de Hibernate/Flyway/MapStruct agora (serão adicionadas na Fase 4)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.fiap</groupId>
        <artifactId>mekano</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>mekano-infrastructure</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>com.fiap</groupId>
            <artifactId>mekano-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- OBRIGATÓRIO: UserRepositoryImpl (Fase 4) precisará do index CDI (MOD-07, Gotcha 2) -->
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>make-index</id>
                        <goals>
                            <goal>jandex</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
```
  </action>
  <verify>
    <automated>
      # Verificar packaging=jar nos 3 módulos
      Select-String -Path "mekano-domain\pom.xml" -Pattern "<packaging>jar</packaging>"
      Select-String -Path "mekano-application\pom.xml" -Pattern "<packaging>jar</packaging>"
      Select-String -Path "mekano-infrastructure\pom.xml" -Pattern "<packaging>jar</packaging>"
      # Verificar que domain NÃO tem jandex
      $j = Select-String -Path "mekano-domain\pom.xml" -Pattern "jandex"
      if ($j) { Write-Error "FALHA: jandex no domain (não deve ter)" }
      # Verificar que application e infrastructure têm jandex
      Select-String -Path "mekano-application\pom.xml" -Pattern "jandex-maven-plugin"
      Select-String -Path "mekano-infrastructure\pom.xml" -Pattern "jandex-maven-plugin"
      # Verificar que domain não tem quarkus-arc
      $arc = Select-String -Path "mekano-domain\pom.xml" -Pattern "quarkus-arc"
      if ($arc) { Write-Error "FALHA: quarkus-arc no domain" }
    </automated>
  </verify>
  <acceptance_criteria>
    - `mekano-domain/pom.xml` existe e contém `<packaging>jar</packaging>`
    - `mekano-domain/pom.xml` contém `<optional>true</optional>` para lombok
    - `mekano-domain/pom.xml` NÃO contém `jandex`, `quarkus-arc`, `quarkus-rest`
    - `mekano-application/pom.xml` existe e contém `<artifactId>mekano-domain</artifactId>`
    - `mekano-application/pom.xml` contém `quarkus-arc`
    - `mekano-application/pom.xml` contém `jandex-maven-plugin` com execution `make-index`
    - `mekano-infrastructure/pom.xml` existe e contém `<artifactId>mekano-domain</artifactId>`
    - `mekano-infrastructure/pom.xml` contém `jandex-maven-plugin` com execution `make-index`
    - `mekano-infrastructure/pom.xml` NÃO contém `mekano-application` como dependência
    - Todos os 3 `pom.xml` contêm `<artifactId>mekano</artifactId>` na seção `<parent>`
  </acceptance_criteria>
  <done>Três POMs de módulo criados com packaging=jar, dependências corretas (Clean Architecture enforced), jandex configurado em application e infrastructure, domain sem nenhum CDI/Quarkus dep.</done>
</task>

<task type="auto">
  <name>Task 2: Criar mekano-adapter/pom.xml + estrutura de diretórios com .gitkeep</name>
  <files>
    mekano-adapter/pom.xml
    mekano-domain/src/main/java/com/fiap/mekano/domain/.gitkeep
    mekano-domain/src/test/java/com/fiap/mekano/domain/.gitkeep
    mekano-application/src/main/java/com/fiap/mekano/application/.gitkeep
    mekano-application/src/test/java/com/fiap/mekano/application/.gitkeep
    mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/.gitkeep
    mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/.gitkeep
    mekano-adapter/src/main/java/com/fiap/mekano/adapter/.gitkeep
    mekano-adapter/src/test/java/com/fiap/mekano/adapter/.gitkeep
    mekano-adapter/src/main/resources/.gitkeep
  </files>
  <read_first>
    - .planning/phases/01-esqueleto-maven-multi-modulo/01-RESEARCH.md — seção "Padrão 5: mekano-adapter/pom.xml"
    - .planning/REQUIREMENTS.md — MOD-05, MOD-06, MOD-07, EXT-01 a EXT-05, EXT-10
    - .planning/research/SUMMARY.md — seção "Critical Gotchas — Gotcha 1" e "Gotcha 2"
  </read_first>
  <action>
**Parte A: Criar `mekano-adapter/pom.xml`**

Regras CRÍTICAS (MOD-05, MOD-06, MOD-07, Gotcha 1):
- packaging=quarkus (ÚNICO módulo com este packaging)
- quarkus-maven-plugin com `<extensions>true</extensions>` e goals build/generate-code/generate-code-tests
- jandex-maven-plugin explícito (MOD-07)
- Depende dos 3 outros módulos internos
- Todas as 6 extensões declaradas (EXT-01..05, EXT-10)
- quarkus-junit5 e rest-assured para testes

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.fiap</groupId>
        <artifactId>mekano</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>mekano-adapter</artifactId>
    <packaging>quarkus</packaging>

    <dependencies>
        <!-- Módulos internos -->
        <dependency>
            <groupId>com.fiap</groupId>
            <artifactId>mekano-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fiap</groupId>
            <artifactId>mekano-application</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fiap</groupId>
            <artifactId>mekano-infrastructure</artifactId>
            <version>${project.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- EXT-01: REST reativo com Jackson JSON — substitui quarkus-rest do scaffold -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
        </dependency>
        <!-- EXT-02: Hibernate ORM + Panache — funcional na Fase 4 -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-orm-panache</artifactId>
        </dependency>
        <!-- EXT-03: Flyway migrations — funcional na Fase 4 -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-flyway</artifactId>
        </dependency>
        <!-- EXT-04: OpenAPI / Swagger UI em /q/swagger-ui -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-openapi</artifactId>
        </dependency>
        <!-- EXT-05: Bean Validation (@Valid, @NotBlank) -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-validator</artifactId>
        </dependency>
        <!-- EXT-10: Driver JDBC PostgreSQL + ativa DevServices em test -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jdbc-postgresql</artifactId>
        </dependency>

        <!-- Testes -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- ÚNICO módulo com quarkus-maven-plugin (MOD-06, Gotcha 1) -->
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Jandex explícito conforme MOD-07 -->
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>make-index</id>
                        <goals>
                            <goal>jandex</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
```

---

**Parte B: Criar estrutura de diretórios com .gitkeep**

Criar um arquivo `.gitkeep` vazio em cada diretório de código-fonte. Isso garante que o Git rastreie os diretórios e o Maven encontre `src/main/java` em cada módulo.

Diretórios a criar (criar o arquivo .gitkeep cria o diretório implicitamente):

```
mekano-domain/src/main/java/com/fiap/mekano/domain/.gitkeep
mekano-domain/src/test/java/com/fiap/mekano/domain/.gitkeep
mekano-application/src/main/java/com/fiap/mekano/application/.gitkeep
mekano-application/src/test/java/com/fiap/mekano/application/.gitkeep
mekano-infrastructure/src/main/java/com/fiap/mekano/infrastructure/.gitkeep
mekano-infrastructure/src/test/java/com/fiap/mekano/infrastructure/.gitkeep
mekano-adapter/src/main/java/com/fiap/mekano/adapter/.gitkeep
mekano-adapter/src/test/java/com/fiap/mekano/adapter/.gitkeep
mekano-adapter/src/main/resources/.gitkeep
```

Conteúdo de cada `.gitkeep`: arquivo vazio (0 bytes).

**Após criar todos os arquivos, executar o build de verificação:**
```powershell
.\mvnw.cmd clean install -DskipTests
```
Esperado: `BUILD SUCCESS` com os 4 módulos compilando.
  </action>
  <verify>
    <automated>
      # Verificar adapter packaging=quarkus
      Select-String -Path "mekano-adapter\pom.xml" -Pattern "<packaging>quarkus</packaging>"
      # Verificar quarkus-maven-plugin SOMENTE no adapter
      Select-String -Path "mekano-adapter\pom.xml" -Pattern "quarkus-maven-plugin"
      $r = Select-String -Path "pom.xml" -Pattern "quarkus-maven-plugin"
      if ($r) { Write-Error "FALHA: quarkus-maven-plugin ainda no root" }
      # Verificar todas as extensões no adapter
      @("quarkus-rest-jackson","quarkus-hibernate-orm-panache","quarkus-flyway","quarkus-smallrye-openapi","quarkus-hibernate-validator","quarkus-jdbc-postgresql") | ForEach-Object {
          $m = Select-String -Path "mekano-adapter\pom.xml" -Pattern $_
          if (-not $m) { Write-Error "FALHA: extensão $_ ausente do adapter" }
      }
      # Verificar diretórios de fonte criados
      Test-Path "mekano-domain\src\main\java\com\fiap\mekano\domain"
      Test-Path "mekano-adapter\src\main\resources"
      # Build de verificação
      .\mvnw.cmd clean install -DskipTests 2>&1 | Select-String "BUILD SUCCESS|BUILD FAILURE"
    </automated>
  </verify>
  <acceptance_criteria>
    - `mekano-adapter/pom.xml` existe e contém `<packaging>quarkus</packaging>`
    - `mekano-adapter/pom.xml` contém `quarkus-maven-plugin` com `<extensions>true</extensions>`
    - `mekano-adapter/pom.xml` contém `<goal>build</goal>`, `<goal>generate-code</goal>`, `<goal>generate-code-tests</goal>`
    - `mekano-adapter/pom.xml` contém `jandex-maven-plugin` com execution `make-index`
    - `mekano-adapter/pom.xml` contém todas as 6 extensões: `quarkus-rest-jackson`, `quarkus-hibernate-orm-panache`, `quarkus-flyway`, `quarkus-smallrye-openapi`, `quarkus-hibernate-validator`, `quarkus-jdbc-postgresql`
    - `mekano-adapter/pom.xml` contém as 3 dependências de módulo interno: `mekano-domain`, `mekano-application` (compile), `mekano-infrastructure` (runtime — Clean Architecture: adapter não pode importar infra diretamente)
    - `Test-Path "mekano-domain\src\main\java\com\fiap\mekano\domain\.gitkeep"` retorna `True`
    - `Test-Path "mekano-adapter\src\main\resources\.gitkeep"` retorna `True`
    - `.\mvnw.cmd clean install -DskipTests` termina com `BUILD SUCCESS` — executar SOMENTE após Plano 03 (application.properties deve existir primeiro)
  </acceptance_criteria>
  <done>mekano-adapter/pom.xml criado com packaging=quarkus, quarkus-maven-plugin exclusivo, jandex, e todas as 6 extensões da Fase 1. Todos os 8 diretórios de fonte criados com .gitkeep. Build multi-módulo compila com sucesso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| build → runtime classpath | Dependências declaradas nos POMs determinam o classpath em runtime |
| módulo → módulo | Fronteiras de Clean Architecture enforced por dependências explícitas no pom.xml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Dependências inter-módulo | mitigate | domain não depende de nada (MOD-02); infrastructure não depende de application (MOD-04) — enforced no pom.xml e verificável com `mvn dependency:tree` |
| T-02-02 | Elevation of Privilege | quarkus-maven-plugin em módulo errado | mitigate | Plugin declarado SOMENTE em mekano-adapter/pom.xml; acceptance_criteria verifica ausência no root (Gotcha 1) |
| T-02-03 | Denial of Service | CDI beans não descobertos (jandex ausente) | mitigate | jandex-maven-plugin configurado em application, infrastructure e adapter com execution make-index (MOD-07, Gotcha 2) |
</threat_model>

<verification>
Após completar ambas as tasks:

```powershell
# 1. Verificar estrutura de módulos
@("mekano-domain\pom.xml","mekano-application\pom.xml","mekano-infrastructure\pom.xml","mekano-adapter\pom.xml") | ForEach-Object {
    if (Test-Path $_) { "OK: $_ existe" } else { "FALHA: $_ ausente" }
}

# 2. Verificar packaging por módulo
Select-String -Path "mekano-domain\pom.xml" -Pattern "<packaging>jar"
Select-String -Path "mekano-adapter\pom.xml" -Pattern "<packaging>quarkus"

# 3. Verificar jandex em application e infrastructure (NÃO em domain)
Select-String -Path "mekano-application\pom.xml","mekano-infrastructure\pom.xml","mekano-adapter\pom.xml" -Pattern "jandex-maven-plugin"
$d = Select-String -Path "mekano-domain\pom.xml" -Pattern "jandex"
if ($d) { "FALHA: jandex no domain" } else { "OK: domain sem jandex" }

# 4. Build completo (executar APÓS o Plano 03 criar application.properties)
# IMPORTANTE: Não rodar aqui — movido para Plan 03 para garantir que application.properties exista
# .\mvnw.cmd clean install -DskipTests
```
</verification>

<success_criteria>
- 4 arquivos pom.xml de módulo existem com packaging correto (3x jar, 1x quarkus)
- quarkus-maven-plugin presente SOMENTE em mekano-adapter/pom.xml
- jandex-maven-plugin presente em application, infrastructure e adapter (NÃO em domain)
- mekano-adapter lista as 6 extensões EXT-01..05 + EXT-10
- 9 arquivos .gitkeep criados nos diretórios de fonte de cada módulo
- `./mvnw clean install -DskipTests` retorna BUILD SUCCESS — executar após Plano 03
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-esqueleto-maven-multi-modulo/01-02-SUMMARY.md` com:
- O que foi feito (4 POMs criados, estrutura de diretórios criada)
- Output do `./mvnw clean install -DskipTests` (BUILD SUCCESS / detalhes de módulos compilados)
- Decisões (seguiu exatamente Padrões 2-5 do RESEARCH.md)
- Artefatos criados
</output>
